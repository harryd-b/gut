# Phase 94 — DTAB-1 complete: the sweep runs clean, and the displaced tower generalizes

*Working session, 2026-07-19. The final loop ran: all torsion classes, g ≤ 4, n ≤ 2g+2, through the full verified chain — closed form where single-A, multi-A cones elsewhere, U-module red extraction (the TIE-1-corrected instrument), per-class anchoring against the Thm 5.6 red gradings with the constant-shift check enforced on every row. First pass: 12 residual anchor failures, all at g = 4, all the same one-unit truncation ghost at a depth the g ≤ 3 window didn't need; deepened the U-window; **second pass: zero failures.** The data file (`DTAB-DATA-2026-07-19.md`, 70 rows, carrier rows marked) is committed as the paper's data section.*

---

## 1. The table is done

Every row anchored; every red distribution matching its Thm 5.6 prediction with a constant shift; every tower profile extracted with the U-corrected instrument. And the sweep delivered one more confirmation for free: **the displaced-tower theorem now holds at three genera by direct computation** — the g = 4 carrier row (n = 6, [3]) shows red {7/4: 1} = (4g−9)/4 exactly, with the binomial profile (1, 8, 28, 56, 70, 56, 28+**1**, 8) carrying the top tower two steps down into the red class's grading, the same signature as g = 2 and 3. The carrier's fingerprint — one volume tower stepped into the shadow position, the one protected class waiting there — is not a low-genus accident.

## 2. What stands complete on the math track, as of this phase

**DTAB-1** — formulas, machinery, and data, nothing provisional, every absolute number carrying a theorem, a calibration, or a passed check. The paper's ingredients are all in the folder: REMARK-1 rev 2 (the two propositions), the stage-1 closed form, the tied-class and displaced-tower analysis (stages 2–3), the multi-A engine and its verification chain (stages 4–4c), and now the data. **Next sitting: assemble `DTAB-PAPER.md`** — the standalone mathematics paper, pure of framework content, with printed proofs, runnable scripts, and the data section — the artifact one fresh referee asked for, a second offered to referee favorably, and a third called the program's best mathematics.

Behind it, in order: the Lin-formula assembly on the carrier (the Pin(2)-bar package — mechanical now that both its inputs are settled), C2-ν's comparison (its data side complete as of this phase), and the C4 rung. Then the math track's in-room frontier is exhausted, and what remains of the program is exported problems, the sky calendar, and the far rungs.

*Status line: seventy rows, zero failures, one pattern confirmed three times. The week's arc closes where it aimed: the manifold that a stranger proved the program had been ignoring now has the most completely documented Floer package of any circle-bundle family in the literature — computed by a method that was wrong twice, caught both times, and ended the week catching its own ghosts before they reached the page.*
