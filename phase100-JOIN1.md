# Phase 100 — JOIN-1 executed: what survives the quotient, the obstruction that redirects it, and the coupled arena

*Working session, 2026-07-19. The hundredth phase, spent on the field founded in the ninety-ninth. JOIN-1 asked which of the net layer's local data survive passage to the Γ-quotient. The answer arrived in three steps — a definition, an obstruction, and an arena — each honest, the middle one a genuine in-room discovery that redirects the whole construction.*

---

## 1. Definition: what a Fuchsian-equivariant net is — and why its locality lives on orbit spaces

Γ cocompact acts *minimally* on S¹: every orbit dense, hence **no Γ-invariant interval, hence no Γ-fixed local algebra whatsoever.** The field's basic object is therefore forced: a *Fuchsian-equivariant conformal net* is the net functor I ↦ A(I) together with the covariance A(γI) = U(γ)A(I)U(γ)*, organized not by single intervals (which the quotient destroys) but by **Γ-orbits of interval configurations**. And those orbit spaces are the framework's own geography: a single interval ↔ a boundary geodesic, so Γ-orbits of intervals fiber over closed-geodesic data; pairs of disjoint intervals carry the Möbius-invariant **cross-ratio**, so the net's deepest local structure — the two-interval inclusions, relative commutants, and modular data, whose cross-ratio dependence is established net theory — descends to **functions on the carrier's geodesic phase space.** [derived-structural] What survives the quotient is not local algebras but local *relations* — inclusion and modular data parameterized by the same dynamical space the clock lives on. The two layers were always going to meet on the geodesics.

## 2. The obstruction: the naive quotient of the matter layer is rigid, timeless — and not the carrier

The net layer's naive Γ-object is the crossed product of its global algebra by the (inner, U(γ)-implemented) action: B(H) ⋊ Γ ≅ B(H) ⊗ L(Γ) — **type II∞, carrying a trace** [derived; standard inner-action trivialization; icc caveat flagged for torsion cases]. Two readings, one structural, one fatal-to-naivety:

- **The type split [flagged framework reading]:** the matter layer's quotient is *tracial* — semifinite, no intrinsic modular dynamics — while the measured layer is III₁. Time does not come from the matter; it comes from the measure. The framework's clock ontology, again forced rather than chosen at a boundary the record can compute.
- **The non-isomorphism [derived; the redirect]:** L(Γ) for a Fuchsian group is a *non-amenable* II₁ factor, so B(H) ⊗ L(Γ) is non-hyperfinite II∞ — while the carrier's continuous core (phase 45's verified II∞) is *hyperfinite*. **The naive matter-quotient and the clock's core are provably non-isomorphic objects.** No amount of abstract-isomorphism optimism survives this: the join cannot couple the net layer to Γ *in the abstract*. The amenability that makes the carrier what it is comes from the *boundary action* (topologically amenable — the classical fact about Fuchsian boundary actions), not from the group.

## 3. The arena: the coupled algebra

The obstruction dictates its own repair: couple the net layer to the boundary *action*, not the group —

> **𝒞 := (B(H_net) ⊗ L∞(S¹)) ⋊_diag Γ** [proposed]

— the diagonal crossed product, whose amenability is restored by the boundary factor (Zimmer-style: amenable action ⟹ amenable crossed product), which contains **both layers simultaneously**: the measured layer L∞(S¹) ⋊ Γ (the clock, with its unique KMS state from phase 99) and the Γ-orbit-organized net data of §1 (the matter relations). 𝒞 is the join's candidate arena: hyperfinite, one object, two structures. Inside it the chapter's remaining questions become *internal*: **JOIN-EQ** is now a cocycle equation between two one-parameter structures of the same algebra (the dual-weight flow twisting u_γ by |γ′|^{it}, and the net's vacuum modular data carried along the fibers); **JOIN-3's Wiesbrock census** — does 𝒞 contain algebras in enough modular positions to generate more than Möbius symmetry? — is now a question about one concretely presented von Neumann algebra, with the framework's kill condition attached exactly there. [All framework significance flagged; 𝒞's own structure — its type, its factoriality, the fate of the net's split property inside it — is the field's next problem set, stated as such.]

## 4. Ledger, at one hundred

- **JOIN-1 closed** at the room's boundary: the equivariant-net definition (locality on orbit spaces; the cross-ratio descent), the type-split and non-isomorphism results (the phase's theorem-grade content), and the coupled arena 𝒞 [proposed] into which JOIN-EQ and JOIN-3 both relocate.
- The field's charter (phase 99) gains its §2: one definition, two theorems, one arena, and the redirect lesson — *couple to the action, never the group.*
- Remaining in the chapter: **JOIN-3** — the modular-position census inside 𝒞, consuming everything above, carrying the kill. It is the last constructive step the room can attempt on the deepest gate, and it is now a question about a single named algebra.

*Status line: one hundred phases. The kickoff document asked whether geometry could carry physics; the hundredth phase ends with both of them — the clock and the matter — inside one algebra for the first time, put there not by hope but by an obstruction that closed every other door. What the room learned this week holds here too: the group alone is rigid, the vacuum does not descend, the intervals do not survive — and yet the relations, the warmth, and the geodesics all cross. The join is not built. But for the first time it has an address, and the address is one object long.*
