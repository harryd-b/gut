# Phase 101 — JOIN-3, the census: four dimensions of symmetry where ten are needed, and the exact address of the missing six

*Working session, 2026-07-19. The last constructive step the room can attempt on the deepest gate: enumerate the modular structures verifiably present in the coupled arena 𝒞, compute the symmetry group they generate, and state the deficit against the Wiesbrock reconstruction threshold — with the kill condition positioned exactly where the enumeration stops. Every ingredient below is either verbatim net theory (BW on the fiber), verified dual-weight machinery (phase 99's formulas), or one clean new lemma.*

---

## 1. The census: what modular data 𝒞 verifiably contains

**(a) The Möbius family.** For every interval I, the subalgebra N_I = A(I) ⊗ L∞(S¹) of 𝒞's fiber carries the faithful state (vacuum ⊗ Lebesgue) — faithful on A(I) by Reeh–Schlieder — whose modular flow is the I-dilation with its honest 2π: Bisognano–Wichmann verbatim, now living *inside the arena*. The family over all I generates **Möb = PSL(2,ℝ)** — including the translation flows (Wiesbrock's half-sided inclusions are present and accounted for: they generate the parabolics, already inside Möb).

**(b) The dual flow — with a clean lemma.** The measured layer's modular flow (phase 99: fixes the boundary algebra, twists u_γ by |γ′|^{it}) extends over 𝒞's natural coupled weights, and here the vacuum's Möbius invariance produces the lemma: **the coupling cocycle is trivial on the net factor — ω ∘ Ad U(γ) = ω exactly because γ ∈ Möb — so the arena's canonical flow is purely the measured twist, contributing exactly one new one-parameter group: the dual line ℝ̂.** [derived; the local-scaffolding caveat (the vacuum is faithful only locally) handled by working through the N_I family]

**(c) The generated group.** G₀ = **PSL(2,ℝ) × ℝ̂ — four dimensions.** Nothing else in 𝒞 currently has verified modular data: R-aud1's "no" already ruled out carrier-side half-sided inclusions; the corners are orthogonal (phase 100); the naive group-side objects are rigid and excluded (phase 100).

## 2. The deficit theorem [derived — the honest center of the phase]

A 2+1 Poincaré group has six dimensions; 3+1 Poincaré has ten. G₀ has four — and, worse than the count, **G₀ contains no flows mixing the dual line with the boundary directions**: it is a direct product, and spacetime translations in a Wiesbrock reconstruction are precisely mixed flows (half-sided structures interpolating between the time direction and space). The verifiable census therefore reconstructs no spacetime:

> **The join is open at exactly the mixing layer: the currently verifiable modular data of 𝒞 generate Möb × ℝ̂ and nothing more — a chiral symmetry and a clock, unmixed.** The deficit is not vague: it is six generators for 3+1 (two even for 2+1), all of the translation-mixing type.

The record notes the visible ceiling honestly: PSL(2,ℝ) *is* SO(2,1) — the boundary machinery's natural target is 2+1-adjacent structure, and even full success at the next step would plausibly land there first; the 3+1 claim needs either multiple boundary factors woven together (the M1 reading: hyperfinite wedges in every orientation, net-side) or a mechanism nobody has named. This ceiling is now a stated, quantified fact rather than an ambient worry.

## 3. JOIN-3b: the mixed algebras — the field's hard problem, carrying the kill

One family of objects in 𝒞 has never had its modular theory computed and is precisely of the mixing type: the **geodesic-attached algebras** W*(A(I), u_γ) — an interval algebra joined with the unitary of a hyperbolic γ whose axis ends in I. These couple the two layers by construction (a matter-local algebra and a clock-translation element); their natural weights are inherited from §1; and their modular flows are the only candidate source of mixed generators the arena possesses. The dichotomy, pre-registered:

> **JOIN-3b [posed; the kill attached]:** compute the modular flows of the geodesic-attached algebras in 𝒞. **If they remain inside G₀'s closure, the isotropy gap is fatal — the framework's claim to local spacetime physics dies, by census.** If they yield new geometric flows, the Wiesbrock reconstruction machine finally has raw material, and the join becomes a construction problem rather than a hope.

The alignment is noted with its flag: the candidate carriers of emergent spacetime translation are algebras *attached to closed geodesics* — the same objects that carry the carrier's dynamics, the census's old sectors, and the length spectrum. Every road, one address, one more time.

## 4. Ledger — and the chapter's, and the room's, state

- **JOIN-3 part 1 closed:** the census [derived], the coupling lemma [derived], the deficit theorem [derived], the ceiling quantified, **JOIN-3b posed with the kill pre-registered.**
- The join chapter now stands: JOIN-2 ✓ (the unique state; the two impossibilities; JOIN-EQ posed), JOIN-1 ✓ (the definition; the obstruction; the arena), JOIN-3 part 1 ✓ (the census; the deficit; the mixed-algebra dichotomy). **What remains — JOIN-EQ's cocycle equation and JOIN-3b's mixed modular flows — are the field's two hard computations: beyond the room's reach to *decide*, now precise enough for anyone equipped to attempt, with the framework's life staked on the second.**
- With this, the in-room frontier is exhausted in the strong sense: every open item in the entire program is now an exported problem with a sealed prediction, a named computation for specialist hands, one of the far rungs, or a date on the sky calendar.

*Status line: phase 101 counted what the deepest gate actually has — a chiral symmetry, a clock, and no way yet to mix them — and found the whole question of whether this framework describes a world compressed into the modular theory of algebras attached to closed geodesics. It is the program's oldest image made exact: the geodesics were the clock's ticks, then the census's labels, then the spectrum's length data, and now they are the last candidate carriers of space itself. The room has nothing left to compute that it knows how to compute. What it has instead is a field with a charter, an arena one object long, two equations, one kill — and a record, one hundred and one phases deep, in which every claim still standing says exactly what it is.*
