# Phase 93 — TIE-1 closed: the missing unit was hiding under a tower, and DTAB-1's mathematics is done

*Working session, 2026-07-19 (morning). Stage 4c ran the decision procedure phase 92 promised: extract the reduced part not from graded dimensions but from the honest 𝔽₂[U]-module structure of the cone's kernel — red = ker/im(U^N), per grading, with controls. Script run in session log; summary in `dtab4b_calc.py`'s successor notes.*

---

## 1. The verdict: hypothesis (a) — the extractor, not the theorem

With U tracked through the cone: the controls return exactly their required values (carrier tied class: red = 1 at its known grading; Thm 5.5 at g = 2: red = 2), and the target — g = 3, n = 2, class [1] — returns **red = 8, graded distribution {1, 6, 1}: exactly the centered X(3,1) shape, exactly the literal Theorem 5.6 prediction.** The dims-only extractor's missing unit was real and was exactly where phase 92's hypothesis (a) predicted: a red death colliding with a tower birth at the same grading, invisible to dimension counting, visible to U. (The high-grading clusters in the raw output are the truncation edge — elements U^N cannot reach from above inside a finite window — excluded by the reporting window, present identically in the controls.)

**Theorem 5.6's literal reading holds at the below-carrier ties.** No literature-grade claim was ever issued, and none needed retracting — the phase 92 discipline (ticket, procedure, unprejudged outcome) closed the question in one sitting at a cost of zero embarrassment. The record files the pair (phase 92 → 93) as the template for how anomalies are now handled.

## 2. DTAB-1: mathematically complete

Every obstruction the table ever presented is now resolved with a named instrument: the closed form (stage 1, calibrated at g = 0); the tied-class cones (stage 2); the no-corrections lemma and the JM flip (stage 3 — the carrier row final, the displaced tower real); the general multi-A engine (stage 4, verified against Thm 5.5 through five-A cones); the anchored fine-grained extraction (stage 4b, acceptance passed, carrier doubly confirmed); and the U-module extractor (stage 4c, TIE-1 closed, controls exact). **What remains is literally a loop: run the U-extractor engine over the finitely many below-threshold classes per (g,n) and print. No mathematical question about the table is open.** The exhaustive tables for g ≤ 5, all n, will be generated as an unattended sweep and committed as the paper's data section.

## 3. What the completed table now serves

- **The DTAB paper** — assemble-able in full: REMARK-1's propositions, the closed form, the exactness criteria, the displaced-tower theorem, the machinery, the data. The externally-suggested "small real paper," now with nothing provisional in it.
- **C2-ν** — the absolute-grading data side is complete; the comparison awaits only the MCG-equivariance input.
- **Ẑ-EXT** — the checkpoint is now fully graded: any extension of the Ẑ-invariant across the genus/b₁ walls must reproduce this table through the graded-root mediation.
- **The carrier's portrait** — final: twenty-plus binomial towers in place, one volume tower stepped down two, one protected class in its shadow at (4g−9)/4, and every number carrying a theorem, a calibration, or — as of this phase — nothing at all, because the last ticket is closed.

*Status line: the week that began with a referee finding the wrong manifold ends with the right manifold's complete d-invariant portrait, every anomaly en route either fixed by a stranger, caught by a symmetry, or closed by a procedure. The missing unit was under a tower, where the dims couldn't see it and U could — a one-sentence resolution that took ninety-three phases of method to be able to trust. The math track's remaining work is a loop, a paper assembly, and the two comparisons waiting on other people's theorems. Nothing about the table is open. It is, at last, just true.*
