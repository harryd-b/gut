# Phase 69 — D-test-0 executed: the constraint system pins b to a finite menu — and unitarity picks Ising at genus two

*Working session, 2026-07-18. Answers Referee W's critical objection (phase 67, item 1): "nothing fixes b; D-test invites a scan." In-room result: **it doesn't — the framework's own census count converts the b-freedom into a finite Diophantine menu per genus, and demanding unitary matter cuts the menu to at most one candidate on a sparse genus sequence whose first entry is genus 2 with Ising matter.** The b-gap is closed at the structural level; what remains for Track W is the rank-level comparison (D-test-1) and the automorphy constraint, now aimed at named targets. The B3c flag flies over the prettiest line below, as it must.*

---

## 1. The constraint flow [in-room; standard minimal-string relations as scaffold]

- **C1 (anomaly)** in its bosonic minimal-string form ties the Liouville parameter to the matter: coupling to (p,p′) minimal matter fixes **b² = p/p′** — a discrete two-integer family, not a continuum. C1 alone does not choose (p,p′): that was the b-gap.
- **C3 at count level (the framework's own datum)**: the boundary structure must carry the census — 2g−1 sectors. The degenerate (Kac-table) content of (p,p′) matter numbers (p−1)(p′−1)/2. Equating:

> **(p−1)(p′−1) = 4g−2.**

One Diophantine equation, sourced entirely from the framework's verified census, with **finitely many coprime solutions per genus** (computed, g ≤ 11): two candidates at g = 2, one at g = 3, never more than four. **b is pinned to a finite menu by the record's own data. There is no scan.**

## 2. The unitarity cut [derived; flagged]

Demanding *unitary* matter (p′ = p+1) makes the equation m(m−1) = 4g−2 — solvable only when the triangular number m(m−1)/2 is odd, i.e., on the sparse sequence

> **g = 2, 8, 11, 23, 28, 46, 53, … (m = 3, 6, 7, 10, 11, …)** — and at **g = 2 the unique unitary candidate is (3,4): the Ising model.**

Stated with the required care: *if* the dictionary's matter is unitary and *if* the count-level identification is the right one, the smallest carrier is genus two with Ising boundary matter — the most-studied CFT in existence sitting at the bottom of the framework's menu. This is either the record's most remarkable convergence or its most seductive numerology; per the standing discipline it is **[conjecture, B3c-flagged]** until D-test-1 — and the honest counterweight is stated now: the k = 0 census sector at g = 2 has rank 6 against naive multiplicity-one Kac expectations, so the rank-level comparison must run through the k = 0 caveats (Jacobian dressing, phase 28) that the record has carried all along. The k ≠ 0 ranks (1, 1) match trivially; the content lives in the hard sector, as it always has.

## 3. What Track W now receives

Not "find b" but: **(i)** check the count-level identification (degenerate primaries ↔ census sectors) against the automorphy constraint C2 — does the Γ-twist select within the menu?; **(ii)** run D-test-1 at the pinned candidates — for g = 2, literally: *does Ising (or (2,7)) boundary matter, Γ-twisted, reproduce the parity table with the k = 0 sector properly dressed?* — a bounded, named computation; **(iii)** the closed-surface gap, unchanged. Referee W's objection is answered in the strongest available currency: the scan he forbade is replaced by a menu his own field's arithmetic can audit in an afternoon.

## 4. Ledger

- The b-gap (phase 67, critical) is **closed at structural level**; D-test restructured as pinned-menu → rank comparison. Packet v2 Track W amended accordingly.
- New registered items: the **genus–matter menu** (§1's table, exact), the **unitarity sequence** (§2), and the g = 2 Ising candidacy [conjecture, flagged].
- Sixth appearance of the small-integer parade (3 sectors ↔ 3 Ising primaries at g = 2) — logged with the same flag as the previous five.

*Status line: the red-team's critical wound is closed with the program's characteristic move — the missing free parameter turned out to be pinned by data the record already owned, the menu is finite, the smallest entry is the most famous CFT there is, and the flag planted on that sentence is as large as the sentence deserves.*

**[ANNOTATION, 2026-07-18, per phase 70 CRITICAL-2 and phase 71]:** this phase's menu was computed in the *bosonic* scaffold under the *total* allocation (all 2g−1 sectors ↔ Kac degenerates). Phase 71 (ST-0) computed the super-scaffold menus side by side — the super total-allocation is empty by parity at every genus [proved], and the fork resolves through phase 53's pairing dichotomy (gate: L1-aud). Phase 71 (D-alloc) registered the refined allocation A, under which the g = 2 unitary candidate is not (3,4) Ising but **(4,5) tricritical Ising**. The Ising line above is therefore the *allocation-total branch*, demoted to one of two mutually exclusive candidates; both remain [conjecture] pending D-test-1.

**[SECOND ANNOTATION, 2026-07-18 (later), phases 74–75]:** superseded more deeply. The census these menus were matched against was computed on the wrong manifold (Σ_g×S¹; the carrier is T¹Σ_g — phase 75); the sector count changes (2g−1 → 2g−2 or 2g−3), **all menus and both candidates are withdrawn as physics**, and this phase's "b is pinned... there is no scan" is retracted per the fresh Track W report: the scan over b had been replaced by a scan over identifications, which is not a pinning. The Diophantine *method* survives; it will be re-run against the carrier census (CEN-2) once OBTAIN-5.6 lands, under the standard the fresh referee set: *count-level matching is not evidence* — only module-level data (weights, fusion, modular matrices) will ever promote a menu entry to a candidate. Separately noted, per the same report: **nothing in the framework yet selects the genus** — at some genera the corrected menus have no unitary entries at all; whether genus is a prediction, an input, or a reductio is now an open question the record states rather than hides (registered as G-SEL).
