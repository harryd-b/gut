# Phase 117 (continuation): ZM-1 round 1 — the construction, conservativity, keystone, and first-pass prizes

**Date:** 2026-07-26
**Status:** DRAFTED — awaiting referee. No verdict enters the ZM-1 registration until the referee pass completes. The consultation below is preserved VERBATIM; nothing edited.

**Editorial header (operator):**
- Headline claims (UNREFEREED): the extension is BUILT with R explicit (nothing pinned; four distinguished values tabulated); **K-ZM-1 does not fire** (the vacuum sector carries the record's representation verbatim — E1, E3, E4/pair-creation, and the BIND-2 rate all reproduced, with the argument that the zero mode cannot enter any k≥1-only functional); **K-ZM-2 does not fire** (JOIN-4a″ survives on the nose; disjointness strengthens the GLOBAL statement — charged folia at norm distance 2 — while the single-algebra Connes–Størmer caveat persists, honestly scoped in GAP-4).
- Prize first-passes: **(i) κ = ½ FORCED by the zero-mode CCR, R-independent — the M-FRAME gate value is now derived, not calibrated**; the pair at common winding gives n(Q₁+Q₂)²/2, i.e. the D-MS cross term, ordering-free; per-particle windings n₁ ≠ n₂ become realizable and give exactly C-S5's value ½(n₁+n₂)Q₁Q₂, with an ordering/braiding ambiguity ∝ (n₁−n₂)Q₁Q₂ (GAP-6). HONESTLY LOCATED RESIDUE: the extension does not abolish the rival crossing-framing definition (C-S3) — the D-MS/framing-deleted binary reduces to WHICH unitary family implements "frame rotation" (deck/zero-mode ⟹ D-MS as theorem; crossing prescription ⟹ framing-deleted); M-FRAME2(b) remains the decider between definitions. **(ii)** The sector energy ⟨T⟩ = q²/2 is an honest expectation at last; the −1/24 remains a declared frame-transfer convention EVEN in the extension; the successor energy column needs its own registration. **(iii)** The twist count is an honest label and the pair phase carries nQ₁Q₂ — but converting phase to ENERGY needs zero-mode evolution: flagged as dynamics, not taken, per the registration's scope guard.
- Forced structural finding [BOX-1]: the record's own conventions force the unit conversion q = Q/√(2π) between record charge and Sugawara charge (anchored to the refereed C-AA2 value), unifying D = q²ℓ = 2h_qℓ — with a reconciliation round owed to phase 113's unit usage and C-BRIDGE/GAP-N7.
- K-ZM-3: no sign of firing; full prize rounds pending.

---

## CONSULTATION (VERBATIM)

# Phase 117 round 1 — ZM-1 construction: the compactified zero-mode extension, with R explicit; K-ZM-1 and K-ZM-2 checked; three prizes at first pass

**Status: [derived by context-free agent; NOT refereed]. Construction round. R carried as a free parameter throughout; nothing pinned.**

## Summary — verdicts

- **Construction delivered (§a):** H_ext = ⊕_{q∈Λ_R} F_q with zero-mode pair [x₀, j̃₀] = i, x₀ compactified; W(constants) become the nontrivial global gauge rotations Z(a) = e^{iaj̃₀}; the C-S1 pairing is repaired (σ_ext(c,η) = +cQ_η, antisymmetric by construction); M-FRAME's multivalued profiles become genuine unitaries for lattice charges. One forced finding: the record's own Weyl conventions fix the charge-unit conversion q = Q/√(2π) between the record charge Q = ∫η′ and the Sugawara charge q (anchored to refereed C-AA2), with consequences flagged for phase 113's unit usage [BOX-1].
- **K-ZM-1 (conservativity): does NOT fire.** The vacuum sector F₀ carries the record's GNS representation verbatim; E1's D, E3's finite parts, E4's eraser/pair-creation license, and the BIND-2 two-arc rate are all functionals of k≥1 oscillator data plus modular data of (A(I), ω) — reproduced exactly (two scope flags, [GAP-3], [GAP-5]).
- **K-ZM-2 (keystone): does NOT fire.** JOIN-4a″ survives verbatim (it quantifies over A(I), to which the extension adds nothing). Disjointness strengthens the *global* statement (charged folia at norm distance 2); it does **not** remove the single-local-algebra Connes–Størmer caveat, which persists [GAP-4]. The winding sublattice Rℤ is locally creatable *in the extended net* — a redraw of superselection, not a contradiction; locus of choice flagged [GAP-5].
- **Prizes (first pass):** (i) κ = ½ is **forced** by the zero-mode CCR (BCH), R-independent — Φ_zm = ½Δa·Q_tot is a theorem in the extension; pair at common n gives n(Q₁+Q₂)²/2 (D-MS cross term, ordering-free); per-particle windings n₁≠n₂ **are** realizable and give mutual term ½(n₁+n₂)Q₁Q₂ — exactly C-S5's value — with an ordering/braiding ambiguity ∝ (n₁−n₂)Q₁Q₂ [GAP-6]. The residual choice (zero-mode vs crossing framing, C-S3) survives and is located exactly. (ii) ⟨T(1)⟩ on the q-sector lowest-weight vector = q²/2, an honest expectation now; the −1/24 remains a declared transfer constant even here. (iii) The pair sector energy carries cross term q₁q₂; converting the deck **phase** nQ₁Q₂ into an **energy** would need zero-mode evolution — flagged, not taken. K-ZM-3 shows no sign of firing at first pass.

## §(a) The construction, R explicit

**Units, forced.** From the record's σ(f,g) = ∫fg′ and ω(W(f)) = e^{−‖f‖²/2}, ‖f‖² = 2πΣ_{k≥1}k|f̂_k|²: writing the current as j(θ) = (1/2π)Σ_k j_k e^{ikθ} and j̃(f) = ∫fj = Σ_k f̂_{−k}j_k, the Weyl relation [j̃(f), j̃(g)] = −iσ(f,g) forces **[j_m, j_n] = 2πm δ_{m+n}**, and the vacuum two-point function reproduces e^{−‖f‖²/2} exactly. Standard oscillators are a_m := j_m/√(2π); Sugawara T(f) = ½∫:j²:f gives T(1) = L₀ = ½a₀² + Σ_{k≥1}a_{−k}a_k. Hence the Sugawara charge is **q = Q/√(2π)** (Q = ∫η′ the record charge): the zero-mode energy of record-charge Q is Q²/4π = q²/2 — precisely refereed C-AA2's conversion. Linear zero-mode pairings (phases) are conversion-free; quadratic ones (energies) carry the factor 1/2π [BOX-1].

**Arena.** Fix R > 0 (Sugawara units; equivalently R_Q := R/√(2π) in record field units). Charge lattice Λ_R := (1/R)ℤ ∋ q; record-charge lattice (1/R_Q)ℤ. **H_ext := ⊕_{q∈Λ_R} F_q**, F_q the Fock space of a_{−k} (k≥1) over |q⟩ with a₀|q⟩ = q|q⟩, a_k|q⟩ = 0 (k>0). Zero-mode pair: x₀ (mean of the boson) lives on ℝ/2πR_Qℤ; conjugate j̃₀ = √(2π)a₀ with **[x₀, j̃₀] = i** in Weyl form — only e^{imx₀/R_Q} (m∈ℤ) exist, so j̃₀'s spectrum is (1/R_Q)ℤ: charge is a genuine discrete quantum number, quantized *by R*.

**Winding.** A profile η with monodromy Q is a genuine map into the target circle iff Q ∈ 2πR_Qℤ; the winding integer is w = Q/(2πR_Q) = q/R ∈ ℤ. Winding sublattice Λ_w = Rℤ ⊂ Λ_R; index R² (when R²∈ℕ); superselected sectors of the winding-extended net = Λ_R/Λ_w ≅ ℤ_{R²}. Smeared vertex operators for q ∈ Λ_w generate the extended local net A_ext(I) ⊃ A(I) — Bose-local iff R² ∈ 2ℤ (even lattice), ℤ₂-graded-local for R² ∈ ℕ, with the standard lattice 2-cocycle [others': Frenkel–Kac; Buchholz–Mack–Todorov; standard — [GAP-1], not re-proved; round 1 below uses only the self-contained zero-mode displacement algebra].

**How the record's operators sit.** For a winding profile η = a + Qθ/2π + p (p single-valued, mean-zero), the geometric generator decomposes j̃(η) = a j̃₀ − Q x₀ + (oscillator part in p) + (branch-cut c-number) — the −Qx₀ coefficient and [x₀,j̃₀] = i are forced jointly by the record's shift formula Ad W(η)(j̃₀-density) = j + η′. Then:

- **W(nonconstant single-valued f):** two embeddings exist — the quotient embedding ⊕_q W_osc(f) (drops f̂₀; matches the record's labels-mod-constants) and the geometric embedding e^{ij̃(f)} (keeps f̂₀·j̃₀). They **coincide on F₀** (j̃₀ = 0 there) and differ on charged sectors by e^{if̂₀Q_sector}: the extension un-quotients exactly what the record quotiented. A_ext uses the geometric one.
- **W(constant a) = Z(a) := e^{iaj̃₀}:** now NONTRIVIAL — the global gauge rotation, eigenvalue e^{iaQ_sector} on F_q; identically 1 on F₀ (conservative). It is global, not local.
- **The extended σ (C-S1 repair):** on constants × winding profiles, define via the operators: Ad Z(a)(W_ext(η)) = e^{iaQ_η}W_ext(η), i.e. **σ_ext(a·1, η) = +aQ_η** (the referee's corrected sign), σ_ext(η, a·1) := −aQ_η. Antisymmetry holds by construction because the constant now pairs with the *monodromy* (a homotopy datum), not a branch representative — the boundary-term failure C-S1 identified is gone. On osc × osc, σ_ext = record σ, unchanged. On winding × oscillator, σ_ext carries an explicit branch-cut convention (the vertex cocycle's cut) — flagged [GAP-2]; it cancels in everything computed below.
- **M-FRAME's multivalued profiles:** become genuine unitaries W_ext(η) = e^{ij̃(η)} iff Q_η ∈ (1/R_Q)ℤ (the charge shift must stay on the lattice); the deck shift η → η + Q now acts nontrivially, via Z(Q) — the C-S2 repair: the deck family IS a family of unitaries here.

**Distinguished R values (enumerated, NOT pinned).**

| R (Sugawara) | Λ_R | structure | which refereed material demands it |
|---|---|---|---|
| √(2π) | (1/√(2π))ℤ = record Q-lattice ℤ | record's abelian unit lattice read in Q-units | C-BRIDGE (Q=±n) under the forced conversion |
| 1 | ℤ | unit lattice read in Sugawara units (R²=1: fermionic extension, h=½ — the complex fermion) | C-BRIDGE under the phase-106 identification q ≡ Q — the two readings are the ν-fork of [BOX-1]; GAP-N7 stands either way |
| √2 | (1/√2)ℤ | self-dual; R²=2 even; extension = su(2)₁ | phase 113's SU(2)₁ material (weights (1/√2)ℤ, roots √2ℤ ✓) |
| √3 | (1/√3)ℤ | R²=3: ℤ₃ sectors, fermionic extension | phase 113 §2's u(3) decomposition (boson on √3ℤ, sector group ℤ₃ ✓) |

E1's D, E3, E4, BIND-2, JOIN-4a″ demand **nothing** of R (all live in F₀; E1 is refereed integrality-free). The fine-structure question (calibration step 3) lives exactly at this choice; untouched here.

## §(b) K-ZM-1 — conservativity: does NOT fire

**Embedding lemma.** F₀ with the quotient(=geometric) embedding is the record's GNS representation *verbatim*: same Weyl relations, same ω (vacuum two-point function reproduced by the units check above), same Möbius unitaries (oscillator implementation, zero mode inert on F₀), hence same modular data for every (A(I), ω) — Tomita–Takesaki is intrinsic to the pair. Every refereed number expressible as a functional of (Weyl calculus over H(I), ω, Möbius flow) is therefore unchanged. Specifically:

- **E1's D = Q²ℓ/2π.** The trivializer machinery uses only ζ^{(n)}_in ∈ H(I): single-valued (zero monodromy ⟹ no x₀-dressing), vanishing at ξ± (kinks only), norm 2πΣ_{k≥1}k|f̂_k|² — *the zero mode cannot enter*: the functional touches only k≥1; the truncation χ_Ī and the flow δ_t preserve single-valuedness; the cocycle c′_t(γ) = e^{iΘ_t}W(ρ_t) has ρ_t ∈ H(I), so its F₀ matrix elements are the record's. D re-derived unchanged.
- **E3's oscillator finite parts.** Φ_osc = 4π²Σ_{k≥1}|η̂′_k|² is k≥1-only; the record's dipole states are single-valued, hence F₀ states; the refereed Φ_ε = πc_ρQ²/ε + (n∓1)Q²/2 is reproduced term-for-term (the n-term now *also* has the zero-mode realization of §(d)(i), which reproduces the same value — consistency, not change).
- **E4 eraser (in A(I) ⟺ Q_tot = 0) and the pair-creation license.** Dipoles are F₀ states; the ⟸ direction is the record's coherent-operator construction, unchanged; the ⟹ direction is the JOIN-4a″-type obstruction on A(I), and the extension adds no unitaries *to A(I)*. Unchanged.
- **BIND-2 rate (Q₁+îQ₂)²ℓ₁/2π.** Verified against phase 112 §(a)–(b): the two-arc computation is entirely the Ḣ^{1/2} (k≥1) norm growth of truncated single-valued trivializers ζ^{(n)}_in ∈ H(I₁) with the record's own χ_Ī₁ — no zero-mode ingredient anywhere in it. Unchanged.

Two honest flags: [GAP-3] E2's phases were not individually re-audited (document outside my reading set; covered by the embedding lemma only). [GAP-5] statements quantifying over "all local operations" change *meaning* if the observable net were re-declared as A_ext(I) (which contains charged vertex operators); no refereed number changes, but the quantifier caveat is the successor-relevant edge (it is also where K-NONAB-2-type erasability questions will live).

**Verdict: K-ZM-1 does not fire.**

## §(c) K-ZM-2 — keystone: does NOT fire

**What disjointness gives, exactly.** The sectors π_q are mutually disjoint representations of the *quasilocal* algebra; folia are norm-closed, and disjoint normal states are at norm distance 2. Hence, globally: no sequence of quasilocal operations on the vacuum converges *in norm* to a state of a different sector class — for the non-winding classes q ∉ Λ_w (the ℤ_{R²} labels), net charge is superselected outright. This is a genuine strengthening over the record's global picture.

**What disjointness does NOT give.** On a single local algebra A(I) (type III₁ factor), the restrictions π_q|_{A(I)} ≅ π₀|_{A(I)} are unitarily equivalent (transportability) — *not* disjoint — and Connes–Størmer approximate transitivity persists: any two normal states of a III₁ factor are approximately unitarily equivalent. The record's caveat survives at the single-algebra level and is untouchable there [GAP-4]. Disjointness lives strictly one level up.

**JOIN-4a″ verbatim.** The theorem quantifies over unitaries/isometries *in A(I)*, with the cocycle (Dω∘α : Dω)_t = e^{iΘ_t}W(k_t) and criterion sup_t‖k_t‖ = ∞ ⟺ ĥ ∉ L². The extension adds nothing to A(I), changes no cocycle (F₀ computation, §b), and the vacuum folium of the record's net is untouched. **The theorem survives on the nose**, and its global companion is now cleaner (above).

**The one caveat.** Charges q ∈ Λ_w = Rℤ *are* locally creatable by the extended net's vertex operators — by construction. This does not contradict the keystone: those operators are not in A(I), and JOIN-4a″ is a statement about the record's net. It is the standard lattice-extension redraw of superselection (only ℤ_{R²} survives as charge *of the extended net*). Whether the extension's winding operators count as "local operations" for the record's erasure questions is a scheme choice the registration's data-scope currently answers (record net primary, extension = data arena); flagged as the exact locus where a future re-declaration would matter [GAP-5]. **K-ZM-2 does not fire.**

## §(d) The three prizes — first pass

**(i) M-FRAME2(a).** The deck family is now a family of unitaries: ψ(t) = W_ext(η_t + a(t))Ω, branch section a(t), Δa = nQ (deck lattice Qℤ, per profile). Zero-mode factor: the displacement e^{i(aj̃₀ − Qx₀)} with [x₀, j̃₀] = i; by BCH with central commutator, e^{i(aj̃₀−Qx₀)}|q_0⟩ = e^{−iaQ/2}(phase-free charge shift), so the zero-mode contribution to the accumulated transport phase is **Φ_zm = ±½Δa·Q_tot** — the ½ is *forced* by the zero-mode CCR (it is the Weyl ½), **R-independent** (R only gates which Q are realizable, Q ∈ (1/R_Q)ℤ); the sign is the orientation/chirality convention (as M-FRAME's [GAP-1] already held). Single defect: nQ²/2 — **the gate value is now derived, not calibrated**; the refereed conditional theorem (A.2) is upgraded: within zero-mode framing, κ = ½ is a theorem.

*Pair, common n:* one profile, one section, Δa = n(Q₁+Q₂): Φ_zm = n(Q₁+Q₂)²/2 ⟹ **M ⊃ nQ₁Q₂ — D-MS forced within zero-mode framing**, now with derived normalization. *Per-particle windings (C-S5):* realizable in the extension — per-defect dressings are separate operator factors with separate sections a₁(t), a₂(t). Weyl-ordered (single exponential, G₁+G₂ = (a₁+a₂)j̃₀ − (Q₁+Q₂)x₀): total ½(n₁Q₁+n₂Q₂)(Q₁+Q₂), mutual term **½(n₁+n₂)Q₁Q₂ — exactly C-S5's value**, collapsing to nQ₁Q₂ at n₁=n₂. Operator-ordered products differ by braiding-type phases ∝ (n₁−n₂)Q₁Q₂ [GAP-6] — vanishing at common n, so the common-n answer is ordering-free. **Not overclaimed:** the extension does not abolish C-S3's rival — per-defect *crossing* framing remains a distinct, finite-ε-realizable definition, and the D-MS/framing-deleted binary reduces to the choice of which unitary family implements "frame rotation": deck/zero-mode (now: D-MS, theorem) vs crossing prescription (framing-deleted). That choice is located exactly there; M-FRAME2(b) remains the decider between definitions.

**(ii) Sector energy.** L₀ = ½a₀² + Σ_{k≥1}a_{−k}a_k; on |q⟩ the oscillators annihilate: **⟨T(1)⟩ = q²/2** — an honest expectation at last (= Q²/4π in record units, [BOX-1]; C-AA2's −q²/2 was its vacuum-folium mirror image). The CAS successor column would read A(sector q) = q²/2 + C_frame, C_frame a *declared* constant: the extension keeps mode normal ordering, so ⟨T⟩ on the extended vacuum is still 0 and **−1/24 remains a frame-transfer convention even here** — the honest disclosure survives compactification. Per K-CAS-2's registered wording, this column needs its own registration on ZM-1 inventory; not entered here. Pure data (eigenvalue); in scope.

**(iii) BIND-2(c).** The twist count is now an honest label: the deck class n of the dressing family. The pair deck *phase* carries nQ₁Q₂ (same computation as (i)); the pair *sector energy* carries cross term q₁q₂ in (q₁+q₂)²/2 — the right shape (note D = Q²ℓ/2π = q²ℓ = 2h_qℓ in intrinsic units, so all three refereed quadratics are one family under [BOX-1]). But identifying the deck phase as a pair *energy* term nQ₁Q₂ requires reading phase-per-transport as energy×time — **that step needs the zero mode to evolve and is out of scope; flagged, not taken.** What stands as data: labels (q₁+q₂, n), the sector-energy cross term q₁q₂, and the computable pair phase.

**K-ZM-3 status:** all three prizes are computable inside the extension at first pass; no sign of firing. Full prize rounds pending.

## Gaps-and-boxes ledger

- **[BOX-1] (forced conversion).** q = Q/√(2π), derived from the record's σ + norm conventions ([j_m,j_n] = 2πmδ) and anchored to refereed C-AA2. Consequence: D = q²ℓ = 2h_qℓ intrinsically. Phase 113's D_λ = 2h_λℓ/2π uses the identification q ≡ Q; its K-NONAB-1 verdict is unit-covariant (both sides rescale together — stable), but any future formula mixing D with h must carry the conversion explicitly. Interacts with GAP-N7/C-BRIDGE; reconciliation is its own (small) round.
- **[GAP-1]** Smeared vertex operators, extended-net locality (R² ∈ 2ℤ), lattice 2-cocycles: [others': Frenkel–Kac; BMT; standard], asserted not re-proved. Round 1's computations use only the self-contained zero-mode displacement algebra.
- **[GAP-2]** σ_ext on winding × oscillator carries a branch-cut convention (vertex cocycle cut) — the operator form of the branch section; cancels in all quantities computed here; full statement deferred.
- **[GAP-3]** E2's phases covered only by the embedding lemma (document not in reading set); BIND-2 and E4 verified against phases 112/106 as written.
- **[GAP-4]** The Connes–Størmer approximate-transitivity caveat persists on each local algebra; the K-ZM-2 "strengthening" is strictly global. Stated so the record cannot later overread it.
- **[GAP-5]** Quantifier caveat: "all local operations" changes meaning if the observable net is ever re-declared as A_ext; the registration's data-scope keeps the record net primary; this is also where winding-charge erasability questions will live.
- **[GAP-6]** Per-particle winding phases carry an ordering/braiding ambiguity ∝ (n₁−n₂)Q₁Q₂; vanishes at common n; needs its own treatment before C-S5 data are registered.
- **[others']** Frenkel–Kac lattice extensions; BMT sectors and h = q²/2; Sugawara; Connes–Størmer; all record imports as cited in-line (phases 104, 105, 108, 109, 112, 113, 116 — refereed status as recorded there).
- **Scope guard:** every result above is data (eigenvalues, phases, labels, lattice structure). The single step that threatened dynamics — phase→energy in (iii) — is flagged and not taken. Standing AI-referee caveat applies; nothing here is refereed.

---

## Amendment section

### A.1 Referee verdict (2026-07-27): both kills CONFIRMED not to fire; κ = ½ refereed as theorem-within-framing

Report preserved verbatim in reviews/REPORTS-phase117-ZM1-referee-2026-07-27.md. All mode-algebra, BCH, and ordering computations re-derived by hand; the units derivation, the ½, the common-n globality step, both kill verdicts, and the R-table each attacked. Verdicts:

- **K-ZM-1: does NOT fire (CONFIRMED).** E1, E3, E4, and the BIND-2 rate independently verified as k≥1/modular functionals reproduced exactly in F₀; E2 covered by the embedding lemma only — entered lemma-conditional per C-AB2.
- **K-ZM-2: does NOT fire (CONFIRMED).** JOIN-4a″ survives verbatim (its statement quantifies over A(I), to which the extension adds nothing); global disjointness is a genuine strengthening; the single-algebra Connes–Størmer caveat correctly persists (GAP-4); the winding-creatability trigger adjudicated in the construction's favor with GAP-5 upgraded to a BINDING CONDITION (C-AB3): re-declaring A_ext as the physical net voids this ruling's premise and mandates a fresh keystone round.
- **κ = ½: refereed as a THEOREM within zero-mode framing** — Φ_zm = ½Δa·Q_tot follows from the record's symmetric Weyl map plus the repaired pairing, R-independent; the previously gate-calibrated normalization of phase 109 A.2 is now derived. The ± sign and the deck-count identification of n remain conventions (C-AB5).
- **D-MS: "forced within zero-mode framing" ruled exactly the right strength.** At common n the value n(Q₁+Q₂)²/2 is choice-free inside the framing — but by the per-particle-coincidence route (C-AB4), not the single-section framing (which would echo M-FRAME's refuted globality step). The crossing-framing rival (C-S3) survives; **M-FRAME2(b) remains the decider between the two definitions of framing.** Per-particle mutual term ½(n₁+n₂)Q₁Q₂ and ordering span (n₁−n₂)Q₁Q₂ verified exact.

### A.2 Corrections ledger

- **C-AB1.** Sign of the mode bracket: the record's conventions force [j̃(f),j̃(g)] = +iσ(f,g), i.e. [j_m,j_n] = −2πmδ — the round's stated sign was the opposite chirality labeling. Only the magnitude 2π is units-load-bearing; every downstream number unaffected; two-point reproduction verified either way.
- **C-AB2.** K-ZM-1 entered as "does not fire; E2 lemma-conditional (GAP-3)."
- **C-AB3.** GAP-5 is a binding condition, not an informational flag.
- **C-AB4.** The common-n D-MS value is derived via the per-particle Weyl-ordered coincidence, not via a single shared branch section; claim strength unchanged.
- **C-AB5.** "κ = ½ derived" scoped: the ½ is derived; sign and deck-count identification remain conventions.
- **C-AB6.** The BOX-1 conversion's effect on phase 113 is substantive relabeling (the j = ½ defect's record charge reads √π, both comparison values become ℓ/2); the K-NONAB-1 verdict is stable (agreement check, both sides move together); the reconciliation round is real work, owed.

### A.3 Post-verdict status

**ZM-1 stands: built, conservative, keystone-compatible.** Refereed into the registration: the construction (with the C-S1/C-S2 repairs verified), the forced unit conversion q = Q/√(2π) with D = q²ℓ = 2h_qℓ, the embedding lemma, both kill survivals, the κ = ½ theorem-within-framing, and the sharpened D-MS status. K-ZM-3 shows no sign of firing. The prize queue: (1) M-FRAME2(b) — now the SOLE remaining decider of the spin-scheme binary (the choice between zero-mode framing and crossing framing); (2) the sector-energy column re-registration on ZM-1 inventory (CAS successor); (3) the BIND-2(c) winding data (with GAP-6's ordering treatment); plus the owed BOX-1/phase-113/C-BRIDGE unit-reconciliation round. GAP-1 (vertex-operator locality, [others']) remains unrefereed; R remains unpinned.

### A.4 Erratum (2026-07-27, post FORK-1 referee — C-AG3)

The consultation's §(a) statement "Winding sublattice Λ_w = Rℤ ⊂ Λ_R; index R² (when R² ∈ ℕ)" is corrected: the containment Rℤ ⊆ Λ_R = (1/R)ℤ holds IF AND ONLY IF R² ∈ ℤ (equivalently ℕ, for R > 0) — the unconditional "⊂" is FALSE off that set; when R² ∉ ℕ the winding sublattice does not exist at all (structural nonexistence, not merely undefined index or failed locality). Exposed by the refereed FORK-1 audit (phases/phase120-FORK1-lattice-audit.md; reviews/REPORTS-phase120-FORK1-referee-2026-07-27.md, C-AG3). Consequence of record: the distinguished value R = √(2π) (the C-BRIDGE reading) has NO winding structure (R² = 2π ∉ ℤ); the R-table's √2 and √3 rows are unaffected (R² = 2, 3). No other refereed ZM-1 content is touched (the round-1 computations used only the self-contained zero-mode displacement algebra; the kill survivals and κ = ½ are winding-sublattice-independent). Verbatim text above unedited per house rules.
