# Phase 121 (continuation): DYN-1 round 1 — the minimal dynamical input

**Date:** 2026-07-27
**Status:** DRAFTED — **REFEREE DEFERRED (budget)**. NOTHING below is refereed; no verdict enters the phase-121 registration until the adversarial referee pass (with rescue mandate on the route rejections and an attack mandate on the (a)≡(b) merge and the C_frame derivation) is run. The consultation is preserved VERBATIM; nothing edited.
**Provenance:** Context-free derivation agent (DYN-1 mandate, phases/phase121-DYN1-registration.md).

**Editorial header (operator):**
- Headline claims (UNREFEREED, all): **DYN-1 CONFIRMED-AMENDED — exactly one canonical dynamics exists.** Routes (a) and (b) MERGE: physical time = arc-length translation along the defect's own carrier = the ℓ-calibrated vacuum modular flow of the carrier holonomy's fixed-point interval (BW/Hislop–Longo + log-uniformization + γ-quotient; calibration s = 2πt). Route (c) vacuous (relabels the killed absolute-energy column). Selected input: **H = (2π/ℓ)(L₀ + C_frame) per carrier.**
- **C_frame = −1/24 conditionally DERIVED** (BOX-7): the refereed weight column's evenness in q forces the Sugawara structure (λ = 0 in the Feigin–Fuchs family), and the cylinder map then computes −c/24 = −1/24 — convention upgraded to consequence, conditional on the single DYN-1 input. GAP-4: assumes the column covers both charge signs.
- **First photon-frequency formula:** neutral mode-k excitation on carrier ℓ has energy gap 2πk/ℓ (C_frame-free) — wavelength λ = ℓ/k; relative scales only, absolute unit untouched per scope guard.
- **K-DYN-1 PASS** (zero refereed numbers changed; [H, Q] = 0; JOIN-4a″ verbatim). **K-DYN-2/COLLIDE-3 = O3** — mass ≠ energy structurally — with the HARD RIDER (BOX-6): mass = E fails MASS-1's monotone registration for EVERY value of C_frame (sectors above threshold always exist and are decreasing in ℓ) — any future identification fires K-MASS-3 unconditionally. MASS-1 untouched. **K-DYN-3 does not fire:** exactly one canonical input, by merger; GAP-5 honestly scopes uniqueness to the registered candidate class.
- **Thesis verdict (unrefereed): survives AMENDED** — "geometry is data, and the data are pregnant with exactly one dynamics." The record fixes WHICH dynamics uniquely (down to its Casimir), never WHETHER.
- Editorial discharge of the round's GAP-6: "E1" in the brief denotes the refereed rate entry D = Q²ℓ/2π (as the brief's own parenthesis stated); the round DID confront it (BOX-1: D unchanged; BOX-2: E·D = 4πh_q(h_q + C_frame), ℓ-free). No open confrontation remains from GAP-6.
- Referee mandates when run (pre-committed): attack the (a)≡(b) merge (the γ-quotient/ZM-1 identification is GAP-2, asserted not constructed); attack BOX-7's import audit (is the Feigin–Fuchs evenness argument sound? is the cylinder map an extra import?); rescue route (c) and the rejected charged-state modular flow (GAP-3's "geometric over state-dependent" criterion); verify all symbolic checks; stress the O3-vs-O2 ruling (is refusing the rest-frame identification legitimate or a dodge?).

---

## CONSULTATION (VERBATIM)

# DYN-1 — The Minimal Dynamical Input: Derivation Record

## Summary and verdicts

**DYN-1 verdict: CONFIRMED, AMENDED.** The record admits exactly one canonical dynamical input: routes (a) and (b) are the *same* declaration in two languages and merge into a single frame — physical time = arc-length translation along the defect's own carrier, equivalently the vacuum modular flow of the carrier-holonomy fixed-point interval calibrated by ℓ. Route (c) is vacuous (it resurrects the killed absolute-energy column). The amendment: the record forces *which* dynamics, not *whether* — adopting dynamics remains an act the data do not compel.

**K-DYN-1: PASS.** No refereed number changes; JOIN-4a″ intact.

**K-DYN-2 (COLLIDE-3): outcome O3** — mass ≠ energy structurally, recorded. Rider: the identification mass = E(Q,ℓ) fails against MASS-1's monotone registration for *every* value of C_frame (verified below), so if anyone later makes that identification, K-MASS-3 fires unconditionally. MASS-1 untouched.

**C_frame: conditionally DERIVED = −1/24.** Conditional on the single DYN-1 input, the refereed weight column itself forces the Sugawara stress tensor, and the standard cylinder map [others'] then computes the shift. Absent the input, C_frame stays declared.

**Thesis verdict: survives AMENDED.**

---

## Route (a): carrier frame

**The declaration.** Each defect's physical frame is the cylinder of circumference ℓ, its own carrier; the Hamiltonian is the generator of arc-length translation. By Schwarzian covariance [others']: E(Q,ℓ) = (2π/ℓ)(h_q + C_frame), h_q = Q²/4π.

**Consequences (all verified symbolically).**

[BOX-1] **D identity.** D = Q²ℓ/2π = 2h_qℓ exactly (checked). The refereed rate invariant is 2 × weight × length; the dynamical frame touches nothing in it.

[BOX-2] **E·D is ℓ-free.** E·D = 4πh_q(h_q + C_frame) = C_frame·Q² + Q⁴/4π (computed and checked). The product of the frame energy and the refereed rate invariant depends only on charge — a frame-independent-looking scalar produced by a frame-dependent energy. With C_frame = −1/24: E·D = Q⁴/4π − Q²/24.

**Neutral oscillator states.** Mode k excited above the sector floor on carrier ℓ: gap = 2πk/ℓ (checked; C_frame cancels). This is ω = 2π/λ with λ = ℓ/k: the carrier supports exactly the wavelengths that integrally subdivide its length. This is the record's first photon-frequency formula — [BOX-3] it is C_frame-independent because it is an energy *difference*, and it fixes only relative scales (frequencies ∝ 1/ℓ across carriers); the absolute unit remains unregistered per the scope guard.

**Canonicality assessment.** Every ingredient is record-internal: the carrier (the record's own refereed assignment to the defect), arc length (the hyperbolic metric is record data — ℓ is already a refereed quantity), orientation (chirality of the current algebra), sign (choose the direction making the spectrum bounded below — [GAP-1] spectral positivity is itself a selection criterion, standard but an input), and the scale (ℓ). What is *not* forced is the decision that translation along the carrier is *physical time* at all. Precisely: intrinsicness of the carrier makes the frame choice canonical **given** that a dynamical input is to be made — there is no rival record-internal frame (see (b), (c)) — but it does not make the input itself mandatory. This is exactly the "canonical frame + scale" the refereed diagnosis said was missing: the carrier supplies the subalgebra/frame, ℓ supplies the scale, and the clock was always free.

## Route (b): modular

The clock is free [others']: the vacuum modular flow of any interval algebra is the Möbius flow preserving it. The decisive question is interval selection.

- **Defect's ramp interval:** the ramp support is modeling data of the state's construction, not a refereed invariant; different equivalent presentations move it. Not forced. Rejected.
- **No interval / global:** the global vacuum has trivial modular flow (refereed diagnosis). Dead end.
- **Carrier fundamental domain:** the carrier geodesic corresponds to a hyperbolic element γ of the Fuchsian group with translation length ℓ and two fixed points on the boundary circle — an interval selected by record-internal data. This works, as follows.

[BOX-4] **The merge (a) ≡ (b).** The vacuum modular flow of the fixed-point interval is the Möbius dilation subgroup fixing γ's endpoints [others': BW/Hislop–Longo]. In half-line coordinates γ: z ↦ e^ℓ z; the dilation flow is z ↦ e^{2πt}z. Uniformizing by u = log z, the modular flow becomes translation u ↦ u + 2πt and γ becomes u ↦ u + ℓ; since translation length equals rapidity equals log-scale, u *is* arc length along the carrier. Quotienting by γ gives the circle of circumference ℓ, on which the pushed-forward modular flow is arc-length translation, with calibration **s = 2πt** (one carrier traversal = modular time ℓ/2π; the 2π is the KMS temperature). The translation generator on the ℓ-circle has mode eigenvalues 2πk/ℓ and equals (2π/ℓ)(L₀ + C_frame) on the sector spaces via standard cylinder quantization [others']. So route (b) with the only forced interval, given its scale by route (a)'s ℓ, **is** route (a)'s Hamiltonian. Two languages, one declaration. Canonicality strengthens: the modular route independently lands on the carrier frame, and modular dimensionlessness is cured by the only length the record attaches to the defect.

[GAP-2] The identification of the γ-quotient of the interval picture with the ZM-1 sector spaces H_ext = ⊕F_q is structural (the record's compactified circle theory is taken to be that quotient), asserted, not constructed here.

[GAP-3] The merge uses the **vacuum** modular flow. The charged state's own modular flow differs by a Connes-cocycle correction [others'] and is state-dependent, non-geometric; it is rejected as a candidate because it is not record-internal-forced — but "geometric over state-dependent" is itself a criterion, flagged.

## Route (c): global rotation

L₀ is canonical as an operator, but its frame is the abstract modular circle: one dimensionless clock for every defect on every carrier, no ℓ anywhere. Its "energies" h_q + Σk·n_k (+C_frame) duplicate the refereed WEIGHT column exactly — this is precisely the killed absolute-energy column relabeled, which the refereed history annihilated: attaching energy semantics to the weight column was ruled to require a dynamical declaration, and route (c)'s declaration adds no structure the weight column lacks (no scale, no carrier-indexing, no ℓ-dependence for the D-invariant to live in). **Route (c) is vacuous**: a convention with zero physical content beyond the surviving dimensionless record. Rejected.

## Kill rulings

### K-DYN-1 (conservativity) — PASS

[BOX-5] The input adds a one-parameter automorphism group per carrier and **no new numbers**: weights are L₀-eigenvalues (untouched — H is a rescaled shifted L₀, same eigenvectors); D = 2h_qℓ unchanged; phases and spin data unchanged; C_frame appears only inside E, which is new, not a revision. Charge creation check (verified, not assumed): H is built from L₀, which is Sugawara-quadratic in the current modes [others'], hence [H, Q] = 0 — the flow acts within each sector F_q and geometrically as rotation, mapping A(I) → A(I_t). A charge-commuting automorphism cannot move a net-charged state into the vacuum's local unitary orbit; JOIN-4a″ ("orbit ⟺ Q = 0") survives verbatim. Energy conservation is indeed orthogonal to superselection, and here reinforces it.

### K-DYN-2 (COLLIDE-3, mass) — outcome O3

With E(Q,ℓ) = (2π/ℓ)(h_q + C_frame):

**C-SEC-R4 handled.** dE/dℓ = −(4πC_frame + Q²)/2ℓ² (computed): decreasing in ℓ iff h_q + C_frame > 0; with C_frame = −1/24 the threshold is Q² = π/6 (verified), below which E is negative and increasing toward 0⁻. A non-monotone-in-ℓ family across charge sectors, exactly as the hazard states.

[BOX-6] **The clash is C_frame-independent.** For *any* C_frame, every sector with h_q > −C_frame (i.e., Q² > −4πC_frame) has E strictly decreasing in ℓ; since h_q is unbounded across sectors, such sectors always exist — for C_frame ≥ 0, *all* charged sectors decrease. MASS-1 registers M(ℓ) monotone-INCREASING. Therefore mass = E is inconsistent with MASS-1 in every sector above threshold, for every declared or derived C_frame, and additionally yields negative mass below threshold when C_frame < 0. No Casimir tuning rescues the identification.

**Ruling.** Outcome **O1 is unavailable** (no derived M(ℓ) = E respects the registration). Between O2 and O3: firing K-MASS-3 requires first *identifying* mass with carrier-frame energy — i.e., declaring the carrier frame the defect's rest frame and importing rest-energy semantics. That is a second declaration; the scope guard permits ONE dynamical input, and MASS-1 was registered through the rate invariant D, not through energy. The record-conservative ruling is therefore **O3: mass ≠ energy, structurally** — E is an energy-in-a-frame; the record has no boost structure comparing carrier frames, hence no theorem that frame energy is mass. Recorded with the rider: the moment a future round declares mass = E, [BOX-6] fires K-MASS-3 unconditionally. MASS-1 remains alive and untouched.

**C_frame status.** C_frame-independent conclusions: the D identity, the excitation gap 2πk/ℓ and the photon-wavelength relation, the merge (a)≡(b), the form E·D = 4πh_q(h_q+C_frame), and the mass-clash rider. C_frame-dependent: the sign/threshold structure of E and its numerical values.

[BOX-7] **C_frame derived (conditionally).** The U(1) current algebra admits a one-parameter family of Virasoro structures T_λ = T_Sug + λ∂J with c = 1 − 12λ² and sector weights shifted by terms linear and odd in q [others': Feigin–Fuchs]. The refereed weight column h_q = q²/2 is even in q; any λ ≠ 0 contradicts it. [GAP-4: this assumes the refereed column covers both signs of q — charge conjugation — taken as given from ZM-1's ⊕_q.] So the record's own refereed data force λ = 0: Sugawara, c = 1. Once the DYN-1 input identifies physical energy as the generator of carrier translation, the standard cylinder map [others'] *computes* the shift: C_frame = −c/24 = −1/24. This upgrades C_frame from convention to consequence — **conditional on the DYN-1 input**. The import audit: the only imported structure is the Sugawara construction and the Schwarzian transformation law [others'], both derivable inside the current algebra the record already owns; what the record "lacked" was the frame declaration, which is DYN-1's single permitted input, not an extra one. Absent that input, C_frame reverts to declared, per the scope guard.

### K-DYN-3 (canonicality) — one route, by merger

Route (c): vacuous. Route (b) standalone: underdetermined (no forced interval, no scale) — its only forced completion is the carrier fixed-point interval scaled by ℓ, which *is* route (a). Routes (a) and (b) merge into exactly one input [BOX-4]. Residual ambiguities audited: interval-vs-complement and flow direction (fixed by spectral positivity, [GAP-1]); additive constant (that is C_frame, [BOX-7]); rival geodesics (none — the record assigns each defect one carrier); absolute unit (out of scope by mandate). [GAP-5] Uniqueness is relative to the registered candidate class and its natural variants; a structurally new fourth route is not excluded by this analysis. **Exactly one canonical dynamical input exists.** DYN-1 does not fail as posed; the data-only thesis is not strengthened — it is amended.

## Thesis verdict

**"Geometry is data, not dynamics" survives AMENDED.** Every refereed number is untouched (K-DYN-1); the record still refuses to *require* evolution. But the stress test shows the refusal is not underdetermination: the record's own structure — carrier, length, chirality, the evenness of its refereed weight column — singles out one and only one dynamics, down to its Casimir, should dynamics ever be attached. The refereed diagnosis is answered point by point: the missing canonical subalgebra/frame is the carrier fixed-point interval; the missing scale is ℓ; the clock was free all along. Amended thesis: *geometry is data, and the data are pregnant with exactly one dynamics.*

[GAP-6] The mandate names a confrontation target "E1" whose content appears nowhere in the supplied inputs; the confrontation executed covers MASS-1 and C-SEC-R4 only. If E1 is a distinct registered item, its confrontation is open.

## VERDICT BOX

1. **DYN-1: CONFIRMED-AMENDED** — exactly one canonical dynamical input; routes (a) and (b) merge (carrier arc-length translation = ℓ-calibrated vacuum modular flow of the carrier's fixed-point interval); route (c) vacuous.
2. **Selected input:** H = (2π/ℓ)(L₀ + C_frame) per carrier, with **C_frame = −1/24 derived** conditional on the input (Sugawara forced by the refereed weight column's evenness; Schwarzian map [others']).
3. **K-DYN-1: PASS** — zero refereed numbers changed; [H,Q] = 0; JOIN-4a″ survives verbatim.
4. **K-DYN-2/COLLIDE-3: O3** — mass ≠ energy structurally; rider: mass = E fails MASS-1's monotone registration for every C_frame (K-MASS-3 would fire); MASS-1 untouched. First photon-frequency formula recorded: gap = 2πk/ℓ, C_frame-free.
5. **Thesis: survives AMENDED** — the record fixes *which* dynamics uniquely, never *whether*; absolute unit remains the final, separate registration.

## Gaps-and-boxes ledger

- **[BOX-1]** D = 2h_qℓ identity — verified symbolically.
- **[BOX-2]** E·D = 4πh_q(h_q + C_frame), ℓ-free — verified symbolically.
- **[BOX-3]** Excitation gap 2πk/ℓ, C_frame-free; wavelength relation λ = ℓ/k — verified.
- **[BOX-4]** The (a)≡(b) merge: fixed-point interval of carrier holonomy, log-uniformization, γ-quotient, calibration s = 2πt; rests on BW/Hislop–Longo [others'] and cylinder quantization [others'].
- **[BOX-5]** Conservativity: H a function of Sugawara L₀ ⇒ charge-commuting, sector-preserving, geometrically a rotation.
- **[BOX-6]** C_frame-independent mass clash: for every C_frame, sectors with h_q > −C_frame have E decreasing in ℓ — verified; kills mass = E against MASS-1 unconditionally.
- **[BOX-7]** C_frame = −1/24 derived conditional on the DYN-1 input, via record-forced Sugawara (λ = 0 from weight-column evenness) + Schwarzian [others'].
- **[GAP-1]** Flow-direction fixed by spectral positivity — a standard but genuine selection criterion.
- **[GAP-2]** Identification of the γ-quotient covering picture with ZM-1's sector spaces: structural, not constructed.
- **[GAP-3]** Merge uses the vacuum (geometric) modular flow; the charged state's Connes-cocycle-corrected flow rejected as state-dependent — rejection criterion flagged.
- **[GAP-4]** BOX-7 assumes the refereed weight column holds for both signs of q.
- **[GAP-5]** Uniqueness (K-DYN-3) is relative to the registered candidate class; unconceived routes not excluded.
- **[GAP-6]** "E1" undefined in the supplied inputs; its confrontation remains open.

Word count: ~1,960.

---

## Amendment section

*(Reserved. REFEREE DEFERRED for budget — to be dispatched next usage cycle with the pre-committed mandates listed in the editorial header. No verdict enters the phase-121 registration until then.)*
