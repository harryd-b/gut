# Phase 99 — JOIN-2 executed: the carrier has a canonical state by theorem, the vacuum provably cannot descend, and the join lands on virgin ground

*Working session, 2026-07-19. The join chapter opens on its most tractable face — the state — with every import source-verified this session (Sullivan 1979 Theorems 8/21 verbatim from Numdam; Renault Prop II.5.4; Neshveyev; Christensen–Vaes Lemma 2.2; the Takesaki/Feldman–Moore dual-weight formulas; Connes' corner-reduction machinery; and a NOT-FOUND sweep whose three negatives shape everything below). Three results and a reformulation.*

---

## 1. The canonical-state theorem [assembly; statement-level new]

Chaining the verified pieces — KMS_β states of a boundary-action algebra correspond to e^{−βc}-conformal quasi-invariant measures (Renault II.5.4; Neshveyev for the non-principal frame; Christensen–Vaes Lemma 2.2 in exactly our formalism), and a cocompact Fuchsian group admits **one and only one** invariant conformal density, of dimension δ = 1, equal to Lebesgue (Sullivan Thm 8, verbatim: *"one and only one Γ-invariant conformal density of any dimension"*) — yields:

> **The boundary algebra of the carrier, with its Radon–Nikodym cocycle flow, has a UNIQUE KMS state, at the critical inverse temperature, and it is the Patterson–Sullivan/Lebesgue state — whose induced geodesic-flow measure (Sullivan's Prop 11/Cor 12 product formula) is Bowen–Margulis = Liouville.** [assembly of established results; the agent confirms the assembled statement exists nowhere in print for Fuchsian boundaries — only its free-group/Cuntz–Krieger avatar]

For the framework this is the largest state-side upgrade since phase 45: **EQ's equilibrium state is not merely a nice postulate — it is the mathematically unique KMS state of the carrier's boundary structure.** The framework's "the fold in equilibrium with its own clock" now reads: *the one state the mathematics permits.* (Registered as a candidate standalone note, **NOTE-2**, in the NOTE-1 mold: pure synthesis, four pages, one new statement, falsifiable bibliographically.)

## 2. The orthogonality theorem and the thermal obstruction [the join's two honest negatives]

**(a) Carrier locality is degenerate.** The carrier's intrinsic local structure — corners χ_I M χ_I = the restricted-groupoid algebras (Connes, Feldman–Moore) — satisfies *orthogonality* for disjoint transversals, not Haag–Kastler commutation: disjoint regions multiply to zero rather than commuting within a common unital frame. [derived, one line; the agent notes even this observation is undiscussed in print] **Consequence, now theorem-shaped: the carrier alone cannot host the local net.** Phase 68's concession ("isotropy is carried by the net, not the carrier") is no longer a retreat — it is forced by the algebra.

**(b) The vacuum cannot descend.** The chiral net's vacuum is the unique Möbius-invariant state; a Γ-invariant *normal* state on the net's global structure would require an invariant density matrix for a group with no finite-dimensional invariant structure — and no such state exists [derived-sketch, flagged for the careful writeup; the agent found the obstruction stated nowhere]. **Consequence: whatever descends along Γ is necessarily a weight/KMS structure, never a vacuum.** The framework's signature ontological claim — *there is no local vacuum; the fundamental state is thermal* — stops being an interpretation here and becomes the only mathematically available option on the quotient. Filed with the flag, but this is the cheapest and cleanest structural convergence the record owns.

## 3. The reformulation: the join is a two-layer correspondence on one circle

The two negatives force the join's true shape. Two Γ-structures live on the same boundary circle: the **measured layer** — (S¹, Lebesgue) ⋊ Γ, the carrier, the clock, the unique KMS state of §1, with modular flow fixing the boundary and twisting u_γ by |γ′|^{it} (the verified dual-weight formula) — and the **net layer** — the Möbius-covariant conformal net A(I), the vacuum, BW, the honest 2π, the QFT-grade locality the measured layer provably lacks. The join is their compatibility:

> **JOIN-EQ [posed]:** *does there exist a Γ-equivariant correspondence between the net layer and the measured layer under which the net's vacuum modular cocycle, restricted along Γ, reproduces the measured layer's Radon–Nikodym cocycle |γ′|^{it} — and with what relative normalization?* If a specific normalization is forced, **T-2π is derived**; if any works, the 2π is a convention forever (the phase 97 dichotomy pattern, now with an equation under it).

And the ground this lands on is empty: the NOT-FOUND sweep certifies that **conformal nets orbifolded by or equivariant under an infinite discrete (Fuchsian) subgroup do not exist in the literature** — finite-group orbifolds only (Xu; Kawahigashi–Longo–Müger); no crossed products of nets by infinite discrete groups; not even the vacuum-obstruction lemma is stated. **The join's mathematical home is a field with no papers in it.** The record therefore does what it has learned to do at such borders: name the field — *Fuchsian-equivariant conformal nets* — and endow it with its founding problem set: the obstruction lemma (§2b, the field's first sentence), JOIN-EQ (its first equation), JOIN-1 (which interval data survive the crossed product — the Takesaki-duality workhorse question), and JOIN-3 (the Wiesbrock census: whether the two layers jointly contain algebras in enough modular positions to generate more than Möbius symmetry — carrying the framework's kill: if provably stuck, the isotropy gap is fatal). This is the program's largest export: not a problem in an existing field, but the charter of a missing one, with the physics as its motivation and the kill condition as its honesty.

## 4. Ledger

- **JOIN-2 closed** at the room's boundary: the canonical-state theorem [assembly]; the orthogonality and vacuum-descent negatives [derived/sketch]; JOIN-EQ posed; NOTE-2 registered as a drafting candidate.
- **EQ upgraded:** its state is now unique-by-assembly, the strongest status any of the five gates' contents has ever held.
- **T-2π** re-founded as JOIN-EQ's normalization question — an equation, no longer a tag.
- **The export:** *Fuchsian-equivariant conformal nets* — charter, founding lemma, three problems, one kill — queued for the packet's next revision as the lead item of a new track.
- Remaining in the join chapter: JOIN-1 (extraction + assembly, feasible), JOIN-3 (consumes 1 and 2; carries the kill). The far rungs unchanged.

*Status line: the ninety-ninth phase asked how to build the bridge and learned three things: the near bank has exactly one place to stand (the unique KMS state), the far bank's crown jewel cannot be carried across (the vacuum does not descend — only warmth does), and the river itself runs through territory no one has mapped. The program has founded smaller things than a field before; this one comes with its own equation, its own first lemma, and — kept where this record keeps such things — its own way to die.*
