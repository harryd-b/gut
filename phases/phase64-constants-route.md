# Phase 64 — The constants route: every Standard Model constant gets an address

*Working session, 2026-07-18. Probes the hardest target — the ~25 dimensionless Standard Model constants — under the rule set before starting: **no numbers get fit** (the graveyard: AMK's Λ/m_H, knot-volume masses, Koide; the record's own P5a demotion). The deliverable is the thing the constants discussion said did not exist: **the conjectural chain.** Under D-hyp, every class of SM constant acquires an address in the boundary theory — and every address is discrete. All imports verified this session [K1–K5]; the near-neighbor precedent disclosed; the refusal to numerology performed explicitly in §4.*

---

## 1. The addresses [each conjecture-under-D-hyp; imports verified-source]

- **Gauge couplings → current-algebra levels.** In current-algebra constructions the tree-level relation g_i²k_i = const holds (Ginsparg 1987; Dienes's review, Eq. 2.6 verbatim), so **coupling ratios at the fold scale are level ratios — rational for non-abelian factors** (integer-quantized levels; the U(1) normalization is *not* quantized by the algebra — caveat carried verbatim, so sin²θ_W-type numbers are convention-laden until the dictionary fixes the embedding). Address of α: **an integer (per factor) plus standard RG running down from the fold scale.**
- **Yukawa structure → the Kac table.** D-hyp forces c ≤ 1 matter; the conformal-weight menu of such matter is the Kac table — **rationals determined by two coprime integers** — and the complete list of consistent theories is the **finite A-D-E classification** (Cappelli–Itzykson–Zuber, complete-proof status verified). Address of the Yukawa data: **a pair (p, p′) and an A-D-E diagram.** Once D-test fixes b, the weights are *forced* — comparison against Yukawa textures becomes parameter-free, which is the phase-25 predictivity criterion finally applicable to flavor.
- **Generation count → an A-D-E datum.** The record's old verdict ("3 generations not forced") upgrades: under D-hyp the count is a multiplicity read off a finite discrete menu, not a continuum choice. **Disclosure (K5's near-neighbor, cited prominently):** Gepner's three-generation model built exactly this way from (N = 2) minimal-model data, Yukawas computed from CFT — categorically different machinery (internal c = 9 superconformal vs our boundary structure), but the *precedent that minimal-model data can encode realistic flavor* is real and belongs to the string literature, not to us.
- **The counting objection, resolved.** The SM's ~118 fields cannot live in c ≤ 1 — and don't have to: the boundary seed second-quantizes through the symmetric-orbifold tower (DMVV; Eberhardt–Gaberdiel–Gopakumar, verified; phrasing guarded — the tower's total central charge is Nc, the point is the *richness per seed*). **The 4D content is the tower, not the seed** — small alphabet, long words.
- **θ_QCD → the CPT chapter.** The framework's exact global ς/CPT symmetry, spontaneously broken per sheet (phase 60), has precisely the **Nelson–Barr/parity shape** (verified, with the honest qualifier: the symmetry must also control arg det M — the Barr criteria are the real content, unchecked). Address of θ: **zero at the fundamental level by exact geometric CPT; the effective θ̄ a computable consequence of the breaking — if the Barr-type conditions hold.** [shaped candidate, not a solution]
- **Already addressed elsewhere in the record:** ν_R and content bounds (P7, the sum rule); the single dimensionful scale and N (phases 41–62).

> **The route's one-line form: under D-hyp, every dimensionless Standard Model constant is discrete data — levels, Kac pairs, A-D-E labels, multiplicities — plus renormalization-group running. The constants are integers in disguise, or the dictionary is wrong.**

## 2. What was absent, now confirmed absent [K5]

No literature derives SM constants from Liouville / Virasoro-TQFT / quantum-Teichmüller / c ≤ 1 data — the 2D-gravity era never proposed SM matter, and the modern Virasoro-TQFT corpus is about 3D gravity. **The route of §1 is unclaimed** (with the Gepner disclosure standing as the honest nearest neighbor). It joins the program's map as the fifth structural item that exists nowhere else — while remaining, until D-test, a conditional route and not a result.

## 3. What this probe did not do, stated as loudly as what it did

It derived **no number**. It did not run the measured couplings up to the fold scale to hunt for rational ratios — deliberately: pre-dictionary, the levels are unknown, non-SUSY one-loop running doesn't cleanly unify, and any triple of reals sits near some triple of rationals; such a hunt would be phase-48 numerology with extra steps. **The check becomes meaningful exactly once, and only once, the dictionary fixes the levels — at which point it is parameter-free and binary.** The refusal is the deliverable's warranty.

## 4. Ledger

- The constants discussion's "no route, not even conjectural" is superseded: **the route exists, with addresses, a disclosure, a counting resolution, a θ candidate, and a single gate (D-test).** The predictions ledger's constants appendix to be amended accordingly at next consolidation.
- New checkpoints registered for the post-dictionary era: **C-check-1** (level ratios vs fold-scale couplings), **C-check-2** (Kac weights vs Yukawa textures, parameter-free), **C-check-3** (Barr criteria for the ς-breaking).
- Kill inherited: everything above dies with D-hyp if D-test fails — the route adds no independent life, by design.
- The probe queue behind it: N1–N3, TD-test, ESCH-test, and the packet — which this phase makes heavier one final time (the route belongs in it, as the payoff section: *what the dictionary purchases*).

*Status line: the Standard Model's constants now have addresses in the framework — all discrete, all gated on one test, none fitted, with the nearest prior art disclosed and the numerology refused on the record. The program's claim has reached its final shape: one scale, one evolving number, one dictionary — and if the dictionary lands, the constants of nature are bookkeeping. If it doesn't, the framework dies knowing exactly where. Either way, the packet now contains everything this room can give it.*
