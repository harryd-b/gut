# Phase 118 — Prize rounds registered and dispatched: M-FRAME2(b), SEC-1, UNIT-1 (2026-07-27)

**Status: REGISTERED. Three rounds dispatched to context-free derivation agents on the operator's instruction ("proceed") following the refereed ZM-1 verdict (phases/phase117-ZM1-round1-construction.md A.1–A.3). Nothing below is derived or refereed. BIND-2(c)'s winding-data registration is explicitly HELD until M-FRAME2(b) returns, because which winding data are registrable depends on the framing decision (and GAP-6's ordering treatment belongs to that same round).**

## 1. Round A — M-FRAME2(b): the crossing-framing pair computation

Already registered (phases/phase109-DMS-upgrade.md A.5); this section records the dispatch. The task: compute the E3 crossing-transport family at n ≠ 0 for a PAIR of defects. The three pre-registered outcomes, verbatim from the registration:

- gate-consistent multiply-wound passages necessarily couple the mutual crossings (tying self- and mutual-prescriptions) ⟹ **D-MS wins**;
- the per-defect offset totalization violates one of S1–S4 ⟹ **framing-deleted eliminated**;
- a fully consistent per-defect totalization ⟹ **framing-deleted selected**.

Decision context (refereed): within zero-mode framing, κ = ½ and D-MS are theorems (phase 117 A.1); the crossing prescription is the sole surviving rival definition of framing (C-S3). M-FRAME2(b) is therefore the SOLE remaining decider of the spin-scheme binary. Gate (pre-registered, unchanged): the construction must reproduce the refereed single-defect Φ_ε = πc_ρQ²/ε + (n∓1)Q²/2 with n realized by the CROSSING prescription (offset/multiply-wound passages) before its pair output counts. The known scope gap it must confront: phase 105's [GAP] — only n ∈ {0, ±1} were exhibited via the refereed offset conventions; general n requires multiply-wound passages, never computed.

## 2. Round B — SEC-1 registered: the sector-energy column on ZM-1 inventory (CAS successor)

**SEC-1.** The successor absolute-energy column, registered on the extension's inventory as K-CAS-2's wording requires (the CAS-1 kill, phase 116 §5, established the column cannot live on the record's original representation; ZM-1's refereed sectors are the legitimate inventory):

- **A(sector q) = q²/2 + C_frame** on the lowest-weight vector of F_q, with q = Q/√(2π) the forced BOX-1 conversion (record units: Q²/4π + C_frame); C_frame the DECLARED frame-transfer constant (the −1/24 is carried as a convention, never computed — refereed disclosure, phase 117 §(d)(ii));
- **A(neutral oscillator state) = mode content**: the column's value on a₋ₖ|0⟩-type states (the radiation/photon-analogue sector) — the first candidate energy datum for the record's neutral states, which are currently refereed "indistinguishable from nothing";
- the column is DATA (an L₀ eigenvalue/expectation), not dynamics; any step requiring zero-mode evolution is out of scope and must be flagged, not taken (ZM-1 scope guard).

**Pre-registered kills.**
- **K-SEC-1 (consistency with refereed spin data).** The column's Q²-part must reproduce refereed D_spin's Q²-part under the forced BOX-1 conversion plus the chirality-branch convention, and must cohere with D = q²ℓ = 2h_qℓ. Failure kills the column.
- **K-SEC-2 (scope).** If defining or evaluating the column requires zero-mode evolution (dynamics) at any step, the column is out of scope as posed and dies (an honest no-go is a full result).
- **COLLIDE-2 (registered confrontation, successor to COLLIDE-1/O3).** Confront A with MASS-1's M(ℓ) and E1's D = Q²ℓ/2π: does the extension force any ℓ-dependence for the sector energy (the frame question that resolved COLLIDE-1 as O3 on the old inventory)? Pre-registered outcomes as in phase 116 §3 (O1: forces M(ℓ) form; O2: contradicts MASS-1's monotone registration — K-MASS-3 fires; O3: identification fails structurally — recorded). No outcome fitted.

## 3. Round C — UNIT-1 registered: the BOX-1 / phase-113 / C-BRIDGE unit reconciliation

The owed round (C-AB6: "the reconciliation round is real work, owed"). Task: restate every refereed charge-carrying formula in ONE declared unit system with all conversions explicit — record charge Q vs Sugawara charge q = Q/√(2π) — including: E1's D = Q²ℓ/2π = q²ℓ = 2h_qℓ; D_spin; the BIND-2 rate; phase 113's D_λ = 2h_λℓ/2π under its q ≡ Q identification (C-AB6: the j = ½ defect's record charge reads √π; both K-NONAB-1 comparison values become ℓ/2; the verdict is unit-covariant and stable); C-BRIDGE's integer lattice; GAP-N7's fork restated as BOX-1's ν-fork (R = √(2π) vs R = 1 in the ZM-1 R-table).

**Pre-registered kill — K-UNIT-1 (the alarm).** Reconciliation must be pure relabeling: NO refereed number may change. If two refereed numbers prove genuinely inconsistent under forced conversion (not resolvable as relabeling), that is a record-level contradiction and must be reported as such, not repaired.

## 3a. Status addendum (2026-07-27, post referees): SEC-1 → SECTOR WEIGHT COLUMN; UNIT-1 CLOSED

- **Round B (SEC-1) refereed** (phases/phase118-SEC1-energy-column.md A.1–A.3; reviews/REPORTS-phase118-SEC1-referee-2026-07-27.md): **K-SEC-2 FIRED on the name** — "sector energy" is not honestly registrable within the data-only scope; the column is re-registered as the **SECTOR WEIGHT COLUMN** (identical object, honest name), with the upgrade clause (energy semantics upon exactly one refereed dynamical input: a canonical frame choice + a scale). K-SEC-1 does not fire (weight→phase 2π conversion now registered); COLLIDE-2 = O3 confirmed with amended diagnosis (clocks are free via modular flows; frame + scale are what's missing); K-MASS-3 unfired.
- **Round C (UNIT-1) refereed and CLOSED** (phases/phase118-UNIT1-reconciliation.md A.1–A.3; reviews/REPORTS-phase118-UNIT1-referee-2026-07-27.md): pure relabeling confirmed; K-UNIT-1 does not fire; the phase-113 rate formula re-tagged D_λ = 2h_λℓ (binding, entered as phase 113 A.4); two type-families tagged (ℓ-carrying rates with 1/2π per squared charge vs ℓ-free topological pairings with Q²/2).
- **Round A (M-FRAME2(b)) still in execution.**

## 4. House conditions (all three rounds)

Context-free agents; word caps (A: 3000, B: 2500, C: 2000); mandatory [BOX]/[GAP-n]/[others'] flagging; numerics where possible; outputs preserved verbatim in phases/ with editorial headers and reserved Amendment sections; adversarial referees before any verdict enters a registration; kills honored as pre-registered. Standing AI-referee caveat applies.
