# Phase 116 (continuation): CAS-1 kill computation and COLLIDE-1 executed — K-CAS-1 FIRES (2026-07-26)

**Status: DRAFTED — awaiting referee. This round claims a KILL of CAS-1; per house rules the kill enters the registration only after the adversarial referee pass. The consultation below is preserved VERBATIM; nothing edited.**

**Editorial header (operator):**
- Headline claims (UNREFEREED): **K-CAS-1 FIRES — CAS-1 dead as registered.** Evaluated in the record's actual states, the Sugawara column registers only the representative-dependent profile energy (Q²c_ρ/ε, divergent), and after the unique shape-independent subtraction the remainder is EXACTLY ZERO for all Q — not Q²/2. The Q²/2 sector value is a zero-mode eigenvalue, and the record's representation makes constants null: **the operator carrying the entire sector energy is identically zero in the record's representation.** The sector rescue (lowest-weight data) imports disjoint representations and the compactified zero mode — tripping K-CAS-2. The pincer is exact and is THE SAME zero-mode mechanism that left M-FRAME undecided: one mechanism, two failures.
- **COLLIDE-1 resolves as O3** (mass ≠ CAS-energy), unconditionally (no derived E(ℓ) exists) and conditionally (the record's only circle is the fixed S¹; the sector value carries no ℓ; mass = E would make mass a function of charge alone, contradicting refereed phase-110 constraints). **K-MASS-3 does NOT fire; MASS-1 untouched** — the registered "opposite ℓ-behaviour" tension in the collision table was between objects of different type.
- Constructive yield ([BOX-C], offered not registered): the exact identity between E3's framing divergence and the moving ramp's Sugawara energy (coefficient match ½Q²c_ρ/ε per unit angle) — the E3 self-energy divergence IS the dynamical phase of the profile's ⟨T⟩, and D-MS subtracts precisely the profile energy.
- Honest disclosure carried: the −1/24 was never obtainable from the record — in the frame where E3's finite parts were computed, ⟨T⟩_ω = 0 identically; the −1/24 is a Schwarzian frame-transfer constant, registrable only as convention.
- Consequence for the calibration plan: step 1 ends with the energy column EMPTY by theorem-shaped no-go, not by neglect; the named reopening route is the same zero-mode construction M-FRAME2(a) already demands. Mass remains a function of (D, Q); calibration must find M elsewhere.

---

## CONSULTATION (VERBATIM)

# Phase 116 execution — CAS-1 kill computation and COLLIDE-1 (context-free derivation agent, 2026-07-26)

## Summary — verdicts

- **K-CAS-2 (gate): does NOT fire against the record-state version of the column** — every ingredient (T affiliated to local algebras via the Virasoro subnet, the rotation generator from the record's own Möbius representation, the vacuum vector) is already in inventory; the −1/24 enters only as a pinned frame-transfer constant, i.e. an adopted convention of D-MS type, disclosable and permitted by K-CAS-1's "one global constant." **But K-CAS-2 DOES fire against the sector-rescoped version** (see below).
- **K-CAS-1: FIRES. CAS-1 as registered is DEAD.** Constructed in the record's Weyl calculus and evaluated on the record's actual states, the column registers the representative-dependent profile energy ½∫(η′)² — exactly Q²c_ρ/ε for the standard dipole (numerically confirmed to machine precision), divergent as ε→0 — and after the only canonical subtraction (minimal subtraction of the per-ramp self-energies, the D-MS precedent) the remainder is **exactly 0 for all Q**, not Q²/2. The Q²/2 sector shape is a zero-mode eigenvalue, and the record's representation trivializes the zero mode ("constants are null," phase 105). The two kills form a pincer: the version that passes K-CAS-2 fails K-CAS-1; the version that would pass K-CAS-1 (sector lowest-weight data) fails K-CAS-2.
- **COLLIDE-1: resolves as O3, conditionally and by default.** With CAS-1 dead, no derived E(ℓ) exists to collide. Under the counterfactual sector reading, the only circle in the record's inventory is the arena's fixed S¹ (size 2π): frame F1 is forced insofar as anything is, E carries no ℓ, and mass = CAS-energy fails structurally. F2 (periodized geodesic) is not record structure — no quotient/periodization is registered anywhere. **K-MASS-3 does not fire. MASS-1 is untouched.**

---

## §(a) K-CAS-2 — well-definedness of the absolute column

**(i) Affiliation and inventory.** For the U(1) current net in its vacuum GNS representation, T = ½:j²: exists as an operator-valued distribution with T(f) essentially self-adjoint and e^{iT(f)} ∈ A(I) for supp f ⊂ I — the c = 1 Virasoro subnet Vir₁(I) ⊂ A(I) [others': standard — Sugawara construction for the circle current algebra per Buchholz–Mack–Todorov; energy representations of Diff(S¹) per Goodman–Wallach; T in chiral nets per Buchholz–Schulz-Mirbach; ⟨T⟩ smearing technology per Fewster–Hollands. I could not web-verify attributions in this environment; flagged, standard]. So locally smeared T is *affiliated* with the record's registered algebras but is **new inventory as a registered observable** — the record touches T exactly once, as the DMS referee's symmetry check (phase 109 A.1). Registering it adds no algebra; it adds a column. That is what a registration is; not a K-CAS-2 violation.

**(ii) Global time?** The frame's global smearing f_frame = 1 gives T(1) = L₀ (mode frame), the rotation generator — an element of the record's own Möbius representation, used throughout E3's transport. Evaluating φ(L₀) is data-like: a number per state, no flow generated, no dynamics claimed. What IS imported is the **choice** of rotation among Möbius flows as "the" energy direction — but that choice is the frame pinning CAS-1 already declares, and the record's E3 already privileges rotation as its transport. No preferred time beyond the declared frame. K-CAS-2 does not fire here.

**(iii) The frame and the constant — the sharp point.** Phase 105's E3 computations live on S¹ = ℝ/2πℤ with the mode norm ‖f‖² = 2πΣ_{k≥1}k|f̂_k|² and ω(W(f)) = e^{−‖f‖²/2} (phases 104 §0, 105 conventions): **circle mode-normal-ordering, in which ⟨T⟩_ω = 0 identically** (L₀Ω = 0; the vacuum is Möbius-invariant). The −c/24 is NOT an expectation value of any record operator in any record state: it is the Schwarzian transfer constant between cylinder and plane normal orderings [others': standard; BCN/Affleck for the Casimir reading, web-verified in the phase-115 referee pass]. So in the frame where E3's finite parts were computed, **the record's vacuum constant is 0**, and A(ω) = −1/24 is obtainable only by *declaring* the plane frame as reference — one global additive constant of pure convention. K-CAS-1's kill explicitly allows one global constant, so this alone does not kill; but it must be entered honestly: **the −1/24 half of CAS-1 is registration, not computation — the record can never output it, only carry it.**

**Gate verdict: K-CAS-2 does not fire** for the column A(φ) = φ(L₀) + const on vacuum-folium states, at the disclosed price that the "absolute" offset is a pinned convention. (It fires against the rescue in §(b) — below.)

## §(b) K-CAS-1 — the computation

**The shift.** From the record's Weyl relations (σ(f,g) = ∫fg′), Ad W(η)(j(f)) = j(f) − σ(η,f)·1 = j(f) + ∫η′f. The quadratic field then shifts by the standard classical + linear terms:

Ad W(η)*[T(f)] = T(f) + j(η′f) + ½∫f(η′)² dθ.

With f = 1 and ω(j(·)) = 0, the linear term dies in vacuum:

A(Q) − A(ω) = (ω∘Ad W(η))(T(1)) − ω(T(1)) = **½∫₀^{2π}(η′)² dθ**.

**Cross-check (exact, then numerical).** In the record's displacement normalization (|α_f|² = ‖f‖² forced by ω(W(f)) = e^{−‖f‖²/2}), ⟨L₀⟩_η = 2πΣ_{k≥1}k²|η̂_k|², and Parseval with η periodic gives 2πΣ_{k≥1}k²|η̂_k|² = ½∫(η′)² exactly. Numerics (raised-cosine dipole, N = 2¹⁸ grid, ε ∈ {0.4, 0.2, 0.1, 0.05}): grid integral, mode sum, and the analytic Q²c_ρ/ε (c_ρ = π²/8 = 1.233701) agree to ~10⁻¹⁰ absolute; the MS remainder is ≤ 10⁻¹⁰ (script deleted per house rules).

**Confronting the registered value.** The record's charged defect state — globally the smoothed dipole W(η)Ω, the only global extension the record supplies (phase 104 §1(a): φ₂ is the restriction of this vector state) — carries:

- **Raw:** A(Q) − A(ω) = ½∫(η′)² = Q²c_ρ/ε → ∞ as ε → 0; shape- and width-dependent. This is the E3 story exactly: the per-unit-angle framing divergence ½Q²c_ρ/ε of phase 105 §(i) **is** the moving ramp's Sugawara energy — the E3 self-energy divergence is the dynamical phase e^{−iE·(angle)} of the profile's ⟨T⟩, and D-MS subtracts precisely the profile energy, leaving the geometric (crossing) phase. This identification is a genuine consistency link between E3 and the Sugawara column, and it is the computation's one constructive yield.
- **MS-subtracted:** removing the two per-ramp self-energies (each ½Q²c_ρ/ε, exactly; disjoint supports make the cross term exactly zero for ε < separation) leaves **0 for every Q, every ℓ, every ε**. No Q²/2. No −1/24. Nothing.
- **Sector-theoretic:** the charge-q BMT sector's lowest L₀-weight is h_q = q²/2 [others': Buchholz–Mack–Todorov; standard], giving q²/2 − 1/24 in the declared cylinder frame. True mathematics — but it attaches to the sector lowest-weight vector, a state in a representation **disjoint** from the vacuum folium. The record's states never inhabit it: phase 105 §5 says so explicitly ("vacuum-folium coherent states, not genuine charged sectors"), and phase 108's Connes–Størmer caveat gives only norm-approximation, with the coherent approximants' energy diverging like 1/ε — no energy-controlled limit.

**Why the sector value is invisible — the mechanism.** h_q = q²/2 is the eigenvalue of ½j₀² on the sector ground state: it is carried entirely by the **zero mode**. The record's representation makes constants null (phase 105 conventions; phase 109 A.5: "in the refereed vacuum representation constants are null and Φ_zm ≡ 0") — the operator whose eigenvalue is the entire Q²-part of CAS-1 is identically zero in the record's representation. This is the **same** zero-mode trivialization that left M-FRAME undecided. One mechanism, two failures: the record cannot see framing's zero-mode cross term, and it cannot see the sector energy, for the same reason.

**Verdict.** K-CAS-1's condition is met on its own terms: constructed in the record's Weyl calculus and evaluated in the record's states, the charged column value (0 after the unique shape-independent subtraction; ε-divergent before it) **fails to reproduce D_spin's Q²-part under one global constant plus branch convention** — a constant cannot absorb a value that is identically zero at all Q, nor one that depends on ε and ρ. **K-CAS-1 FIRES. CAS-1 is dead as registered** ("the registered expectation of T in the record's states" — phase 116 §1 — has no such values). The only rescue — re-scoping the column to sector lowest-weight data — imports disjoint representations, their per-sector rotation covariance, and the compactified zero mode: structure strictly beyond the global vacuum representation, i.e. **the rescue trips K-CAS-2**. The pincer is exact and symmetric with M-FRAME's dichotomy: zero-mode realization ⟹ sector values exist but are new structure; vacuum representation ⟹ no new structure but the values vanish. I register no new postulate; whether a successor "CAS-2 on sector data, with the zero-mode representation constructed first" is worth posing is an operator decision, and it would be gated by the same construction M-FRAME2(a) already demands.

## §(c) COLLIDE-1 — the collision

With (b)'s answer, the collision largely dissolves rather than detonates.

**What circle size enters the sector-energy formula?** The candidates, adjudicated against the record:

- **(F1) The arena's fixed S¹, size 2π.** This is the only compact spatial frame the record possesses: all states, norms, mode expansions, and E3's transport live on it, and sector theory's h_q = q²/2 − c/24 is a statement on this circle. ℓ appears nowhere in it. If any frame is record-forced, it is this one.
- **(F2) The periodized geodesic, circumference ℓ.** E ∝ (2π/ℓ)(q²/2 − 1/24), decreasing in ℓ. This would require quotienting by γ — a periodization in modular time producing a new net. **No such construction is registered anywhere in the record.** E1's D = Q²ℓ/2π lives in γ-iteration, but as a *norm-growth rate per step* (phase 104 §3), not as an energy expectation on a periodized space; the refereed coexistence of E1's modular frame and E3's circle frame is untroubled precisely because they price different objects (a divergence rate of coherent-erasure cost vs a holonomy finite part). Nothing identifies the γ-orbit with a spatial circle. F2 is not record-realizable without new structure.
- **(F3) Something else forced by the computation.** What (b) actually forces: the only absolute energy the record's states carry is the profile energy — which after MS is **zero**, with no frame question left to ask.

**Outcome: O3, in two layers.** Unconditionally: CAS-1 is dead (§b), so no derived E(ℓ) exists and "mass = CAS-1 energy" has no record content at all — the identification fails structurally at the source. Conditionally (had a sector column been admitted despite K-CAS-2): the record-supported frame is F1, E(Q) = q²/2 − 1/24 carries no ℓ, and mass = E fails structurally anyway — it would make mass a function of charge alone, contradicting both MASS-1(a)'s registered charge-independence and phase 110's refereed theorem that M must read the pair (D,Q) through ℓ = 2πD/Q², never charge data alone. Either layer lands on O3.

**Not O4-ambiguous.** The E1/E3 two-frame coexistence is real but is not an ambiguity *about the energy column*: the frames attach to different quantities, and the quantity COLLIDE-1 needed (an energy expectation) turns out to exist only trivially. The one scenario that would reopen the collision — and could then fire K-MASS-3 via F2 — is the same fork as M-FRAME2(a): construct the compactified zero-mode representation, obtain genuine sector energies inside a registered inventory, and *then* ask which circle a defect on a length-ℓ geodesic sees. Until that construction exists, F2 is unposed.

**Consequences for MASS-1.** K-MASS-3 does **not** fire: no refereed or derivable result fills the mass column with anything non-monotone in ℓ — nothing fills it at all. MASS-1 survives this round untouched, in the weak sense: its registered monotone-increasing reading was neither confirmed nor confronted, because the record's only absolute-energy candidate registers zero on every defect. The E1-rate row of the phase-116 collision table (D increasing in ℓ) never was an energy expectation; the apparent "opposite ℓ-behaviour" tension in the table is between objects of different type. Calibration must find M elsewhere — mass remains a function of (D, Q) as refereed in phase 110, and the calibration column remains empty.

---

## Ledger

- **[BOX-A]** The shift formula Ad W(η)*[T] = T + j(η′·) + ½(η′)² and ⟨L₀⟩_coherent = ½∫(η′)²: exact under the record's stated conventions (displacement normalization forced by ω(W(f)) = e^{−‖f‖²/2}); Parseval identity exact; numerically confirmed to ~10⁻¹⁰ (raised-cosine dipole, four ε values; script deleted).
- **[BOX-B]** MS remainder ≡ 0: exact for ε < ramp separation (disjoint supports); the subtraction used is the unique shape-independent one, per the refereed D-MS uniqueness skeleton (phase 109).
- **[BOX-C]** Identification "E3 framing divergence = moving ramp's Sugawara energy (dynamical phase)": exact coefficient match (½Q²c_ρ/ε per unit angle, phase 105 §(i)); offered as a consistency identity, not a registration.
- **[GAP-1]** Attributions asserted from standard knowledge, NOT web-verified in this environment: Sugawara/Virasoro subnet existence and affiliation (Buchholz–Mack–Todorov; Goodman–Wallach; Buchholz–Schulz-Mirbach; Fewster–Hollands); h_q = q²/2 (BMT); −c/24 as Schwarzian transfer constant (standard; BCN/Affleck already web-verified in the phase-115 pass). None is load-bearing for the kill: the kill computation (b) uses only the record's own Weyl calculus.
- **[GAP-2]** The claim "the record's states never reach the sector value" rests on the refereed folium-scope statements (phase 105 §5, phase 108 Connes–Størmer caveat) plus the 1/ε energy divergence of approximants; a controlled statement about *all* possible vacuum-folium state sequences (e.g. non-coherent ones) approximating sector energy data was not attempted.
- **[GAP-3]** The F1-forcing in §(c) is an inventory argument (no registered periodization), not a theorem that no future registration could realize F2; explicitly, M-FRAME2(a)'s zero-mode construction is the named reopening route for both CAS and COLLIDE.
- **[others']** Refereed inputs used as ground truth: phase 104 conventions and D = Q²ℓ/2π; phase 105 E3 (Φ = πc_ρQ²/ε − Q²/2, D-MS); phase 109 (scheme uniqueness, zero-mode nullity, M-FRAME dichotomy); phase 110 (M ≠ f(D), K-MASS-3 terms); phase 115 A.1/C-Y6 (frame-pinning requirement, conversion conventions).
- Standing AI-referee caveat applies; no human has checked any of this.

**One-line close.** CAS-1 dies by its own pre-registered kill K-CAS-1 — the record's states carry only profile energy (MS value: exactly zero; the Q²/2 is a zero-mode eigenvalue the record's representation annihilates), the sector rescue trips K-CAS-2, COLLIDE-1 resolves as O3 with K-MASS-3 unfired and MASS-1 untouched — and the computation's one constructive yield is the exact identity between E3's framing divergence and the defect's Sugawara energy.

---

## Amendment section

### A.1 Referee verdict (2026-07-26): KILL CONFIRMED

Report preserved verbatim in reviews/REPORTS-phase116-CAS1-referee-2026-07-26.md. The referee attempted four rescue routes (alternative subtraction schemes; the zero-mode locus; a frame rescue for the −1/24; a hyper-literal registered-wording ruling) — all failed, and two strengthened the kill:

- **C-AA1 (strengthening).** For the registered dipole states the divergence is the ENTIRE energy — the finite remainder is machine-zero for every shape, so all candidate subtraction schemes coincide, and fusion-naturality alone forces the remainder to zero. The kill is scheme-argument-free.
- **C-AA2 (missed structure, kill-supporting).** The referee exhibited the one vacuum-folium profile with a shape-independent finite Q² remainder — the winding-compensated profile — and found its remainder is EXACTLY the negated zero-mode energy (−q²/2, verified to 8–10 digits), wrong-signed and unregistered (refereed-rejected as fusion-anomalous in phase 109 BOX 6). The zero-mode mechanism is thereby confirmed from the rescue side: the sector energy sits one representation away, precisely where the record cannot reach.
- **C-AA3.** The strict reading kills twice over: the computed vacuum constant in the pinned frame is 0 ≠ −1/24, firing K-CAS-1's first disjunct independently.
- **C-AA4 (precision).** The M-FRAME analogy is asymmetric in the kill's favor: framing had a finite-ε crossing realization keeping its binary open; an energy expectation has no crossing event — the CAS kill is strictly stronger than M-FRAME's undecidedness.
- **C-AA5.** All conventions and quotations verified verbatim; independent numerics on two shapes (cubic and quintic smoothstep — neither used by the submission) agree to 10⁻¹⁰–10⁻¹⁵.
- **Registered-wording ruling (R4):** the submission's hyper-literal reading is correct — "the record's states" are the vacuum-folium states, and the sector rescue trips K-CAS-2 by its registered terms. The pincer is in the registration's own text.

**COLLIDE-1 = O3 CONFIRMED, both layers** (the F1 inventory argument verified by grep — the record affirmatively states no Hausdorff quotient exists; the charge-only contradiction with refereed phase-110 constraints sound). **K-MASS-3 correctly unfired; MASS-1 untouched.** The [BOX-C] identity VERIFIED independently (coefficients exact to 10⁻⁹–10⁻¹²) and ruled registrable as a consistency identity.

### A.2 Post-verdict status

**CAS-1: DEAD (K-CAS-1 fired; the sector rescue trips K-CAS-2; kill referee-confirmed).** Entered in the phase-116 registration. What survives: the exact pincer; the E3-divergence = Sugawara-energy consistency identity (registrable); the honest −1/24 disclosure; and the reopening route — **the compactified zero-mode representation (= M-FRAME2 route (a))**, now the named gate for THREE registered questions: the spin scheme, the winding pair-energy (BIND-2(c)), and any future absolute energy column. Calibration step 1 concludes: the energy column is empty by refereed no-go; mass remains a function of (D, Q); the calibration plan's remaining live routes are step 2 (moduli) and step 3 (the charge-lattice unit), both unblocked by this outcome.
