# Phase 111 (continuation): BIND-1 round 2 — K-BIND-1 executed: NO-GO claimed

**Date:** 2026-07-26
**Status:** DRAFTED — awaiting referee. **This round claims a KILL.** Per house rules a kill verdict enters the registration only after the adversarial referee pass; BIND-1 is not yet declared dead in the record.
**Provenance:** Context-free derivation agent executing the K-BIND-1 mandate (construct-or-break the concatenating fusion rule; both horns attempted). The consultation below is preserved VERBATIM per house rules. Nothing has been edited.

**Editorial header (operator):**
- Headline claim (UNREFEREED): **NO-GO — K-BIND-1 fires.** Two independent closures: (1) implementer-rigidity theorem — with fusion = composition of refereed defect automorphisms (E4's own operation), the composite is Ad W(η₁+η₂) and by C-J3 proper outerness every M_Λ-internal implementer has trivial group support: totals-only is a theorem; (2) the unique concatenating alternative (dressed calculus V = W·λ_γ) is unforced AND provably mis-calibrated — same-axis it doubles the carrier against refereed E4; cross-axis the **anchor-interlacing lemma** (new elementary theorem, 2000/2000 numerics) shows the concatenated carrier's leg sweeps charge î(Q₂−Q₁), never Q₁+Q₂ — like-charge composites come out carrier-neutral, annihilating pairs doubly charged: exact inversion of the physical pattern; and dressing destroys E6's refereed central-extension recovery of D₁₂.
- Load-bearing identification flagged by the round itself: "fusion" = the record's only refereed composition concept. A future "binding map" that is not fusion would be new structure outside this kill's scope. The referee is instructed to rule on whether the kill as registered fires under this identification.
- Claimed survivors (independent of BIND-1's fate): the round-1 defect formula as pure geometry; the anchor-interlacing lemma; and **a latent length-carrying cross-axis coupling already inside the refereed calculus** — the E1 rate of the undressed pair under γ₁-iteration is D_{γ₁}(pair) = (Q₁+îQ₂)²ℓ₁/2π [BOX-5, unrefereed] — the first candidate for exactly the coupling whose absence phase-110's no-go certified; it modifies per-carrier rates, not carriers, so it cannot rescue BIND-1, but it reopens the K-MASS-2 rescue route by another door.

---

## CONSULTATION (VERBATIM)

# BIND-1 round 2 — K-BIND-1: does the arena force, permit, or forbid concatenated-carrier fusion?

**Summary with verdict.** NO-GO — K-BIND-1 FIRES. The dressed-defect construction V(Q,γ) = W(η_{Q,γ})λ_γ in M_Λ is built in full: its group part does concatenate (λ_{γ₁}λ_{gγ₂g⁻¹} = λ_{γ₁gγ₂g⁻¹}, no twist), and its scalar phase is the half-E2 phase e^{−iQ₁Q₂î/2}. But (i) the dressing is not forced — the refereed defect automorphism is implemented in M_Λ by the fiber element alone, and C-J3 proper outerness proves every M_Λ-internal implementer of the refereed composite automorphism has trivial group support (implementer-rigidity: fusion is provably totals-only); (ii) the dressed calculus, the unique concatenating alternative, fails the calibration gates **provably**: same-axis it assigns the doubled carrier γ₁² (length 2ℓ, contradicting refereed E4) and leaves a properly-outer deck residue α_{γ₁²} absent from E4's refereed composite; cross-axis a new elementary theorem (anchor-interlacing lemma, proved and verified on 2000 random configurations) shows the concatenated carrier's leg sweeps charge **î(Q₂ − Q₁), never Q₁ + Q₂** — the refereed charge functional ∫_{I₁₂}η′ anti-calibrates (like-charge composites come out neutral; E4-annihilating pairs come out doubly charged); and dressed implementer lines destroy the refereed E6 central-extension structure (commutators acquire non-central group parts). Both horns close: with the record's fusion concept, totals-only is a theorem; without it, the only concatenating calculus violates refereed E4 and charge additivity. Per the registered kill — "provably totals-only with no carrier merge" — BIND-1 dies. §(d) records the now-moot empirical separation (E1 generically distinguishes the readings, either sign attainable).

## §(a) The dressed-defect construction

**Conventions (from E6, verbatim structure).** M_Λ = (B(H)⊗̄L∞(S¹))⋊Λ, ordinary crossed product, no cocycle twist; group unitaries λ_g, action λ_g(x⊗f)λ_g* = U(g)xU(g)*⊗f∘g⁻¹. Quasi-free Möbius covariance gives U(g)W(f)U(g)* = W(f∘g⁻¹) [BOX-1: standard Weyl covariance of the current net; a possible scalar phase in the implementer choice cancels in every commutator/Ad computation below]. Dressed defect: V(Q,γ) := (W(η_{Q,γ})⊗1)λ_γ, η_{Q,γ} the canonical E2 plateau profile (plateau Q on the leg I_γ = (a,b) ccw, C-D5 ramps). Per C-U2, g ∈ Λ throughout.

**(i) Product — group part.** V(Q₁,γ₁)·V(Q₂,γ₂′) with γ₂′ = gγ₂g⁻¹:

V₁V₂ = W(η₁)·W(η₂′∘γ₁⁻¹)·λ_{γ₁γ₂′} = e^{−iσ(η₁, η₂′∘γ₁⁻¹)/2} W(η₁ + η₂′∘γ₁⁻¹) λ_{γ₁γ₂′}.

Group part: the concatenated class P_g = [γ₁gγ₂g⁻¹], confirmed; the echo is exact and untwisted.

**(ii) Fiber part.** The profile is η₁ + η₂′∘γ₁⁻¹: two plateaus (Q₁ on I₁; Q₂ on γ₁I₂′, the leg of γ₁γ₂′γ₁⁻¹). This is NOT η_{Q₁+Q₂, γ₁₂} (single (Q₁+Q₂)-plateau on I₁₂ = leg of γ₁₂ := γ₁γ₂′): the mismatch Δη = η₁ + η₂′∘γ₁⁻¹ − η_{Q₁+Q₂,γ₁₂} is a fixed nonzero six-ramp profile (ramps +Q₁@a₁, −Q₁@b₁, +Q₂@γ₁a₂′, −Q₂@γ₁b₂′, −(Q₁+Q₂)@a₁₂, +(Q₁+Q₂)@b₁₂). Worse than a shape mismatch, its charge content is wrong — see G2. The phase: with all ramps pairwise disjoint (generic; tag [BOX-2]), σ(η₁, η₂′∘γ₁⁻¹) = Q₂[η₁(γ₁a₂′) − η₁(γ₁b₂′)] = Q₁Q₂·î(γ₁, γ₁γ₂′γ₁⁻¹) = Q₁Q₂·î(γ₁,γ₂′), since γ₁ preserves its own oriented axis. So the dressed product remembers E2 through the scalar e^{−iQ₁Q₂î/2}.

**(iii) Closure.** The product is of dressed FORM (fiber Weyl × group unitary) but is not V(Q,γ₁₂) for any Q: its fiber profile is not in any single-carrier canonical class, and its γ₁₂-leg charge is î(Q₂−Q₁) (G2 theorem below), so it is a charge-î(Q₂−Q₁) non-canonical defect riding P_g. Closure fails in the charged sense; the obstruction is exactly Δη, and no phase or central correction removes it.

**(iv) Is the dressing forced? No — it is excluded for implementers.** The refereed defect is the automorphism/state Ad W(η) (E1–E5; JOIN-4a″ makes it a state-level fact). It extends to M_Λ implemented by the fiber element W(η)⊗1. Nothing refereed ties the defect to λ_γ: covariance is a property of the global crossed-product structure, not a per-defect dressing requirement. Stronger: if V, V′ ∈ M_Λ implement the same automorphism, V*V′ ∈ Z(M_Λ) = L∞(S¹)^Λ⊗1 ⊆ fiber (C-J3, refereed). Since α_γ is properly outer (C-J3), Ad(W λ_γ) = Ad W∘α_γ ≠ Ad W: the dressed object is not a different implementer of the refereed defect — it is a **different automorphism** (defect ⋉ deck translation), i.e. a new object, hence a new postulate.

## §(b) Calibration gates

**G1 (same-axis limit) — FAILS twice.** γ₂ = γ₁, g = e: V(Q₁,γ₁)V(Q₂,γ₁) = phase·W(η₁+η₂)λ_{γ₁²}. Fiber: exactly E4's profile addition ✓. But (α) the group part is the doubled class [γ₁²], translation length 2ℓ; E4's refereed composite stays on the same carrier with the same ℓ ((Q₁+Q₂)²ℓ/2π, refereed). Reading carrier from the group part changes a refereed number by a factor 2. A "primitive-class projection" patch is not multiplication (not a homomorphism) — an added convention, and it concedes the group part is not the carrier. (β) As automorphisms, Ad(V₁V₂) = Ad W(η₁+η₂)∘α_{γ₁²}; E4's refereed composite is Ad W(η₁+η₂) with no deck factor, and α_{γ₁²} is properly outer — the dressed composite is NOT the refereed composite, flatly.

**G2 (charge) — FAILS, by theorem.** The refereed charge functional is Q = ∫_{I}η′ over the carrier's leg. **Anchor-interlacing lemma.** For a crossing pair with î = +1 (ccw order a₁,a₂,b₁,b₂), the product γ₁₂ = γ₁γ₂′ is hyperbolic (refereed C-U1) with repelling point a₁₂ ∈ (a₁,a₂) and attracting point b₁₂ ∈ (b₁,b₂). *Proof sketch:* the closed arc [b₁,b₂] contains no repelling anchor of either factor; γ₂′ moves it toward b₂ within itself, γ₁ moves the result toward b₁ within itself, so γ₁₂ maps [b₁,b₂] strictly into its interior, which must contain the attracting point; dually (γ₁₂)⁻¹ = γ₂′⁻¹γ₁⁻¹ contracts [a₁,a₂] [BOX-3: strict-contraction endpoint details]. Consequently the ccw leg I₁₂ = (a₁₂,b₁₂) contains a₂ and b₁ but not a₁, b₂; and by the same one-step dynamics γ₁(a₂′) ∈ (a₂,b₁) ⊂ I₁₂ while γ₁(b₂′) = γ₁₂(b₂′) ∈ (b₁₂,b₂) ∉ I₁₂. So of the four ramps of the product's fiber profile, exactly {−Q₁ (at b₁), +Q₂ (at γ₁a₂′)} lie in I₁₂:

**∫_{I₁₂}(η₁ + η₂′∘γ₁⁻¹)′ = Q₂ − Q₁  (î = +1);  = Q₁ − Q₂ (î = −1); i.e. î(Q₂−Q₁), never Q₁+Q₂.**

Numerics (script run and deleted): 2000 randomized î = +1 crossing configurations (random anchor 4-tuples, lengths in [0.5,5]) — the inside-leg ramp set was {−Q₁,+Q₂} in 2000/2000, and (a₁₂,b₁₂) landed in ((a₁,a₂),(b₁,b₂)) in 2000/2000; the mirror configuration checked explicitly. The dressed composite therefore **anti-calibrates**: like charges Q₁ = Q₂ give a carrier-neutral composite (E1-invisible on P_g); an E4-annihilating pair Q₂ = −Q₁ gives carrier charge ∓2Q₁ — the exact inversion of the physical pattern. The inverse-orientation channel (concatenation with γ₂′⁻¹, the S⁻ class of round 1) also yields |Q₁−Q₂| [BOX-4: computed by the mirrored lemma; the summed charge appears only for the anti-defect (−Q₂, γ₂′⁻¹) riding the *different* class S⁻ — charge-conjugation-consistent, and no assignment delivers (Q₁+Q₂, P_g)]. Within the constraint that the fiber part implements the refereed defect (its E2 profile class is refereed data), no re-dressing evades this: the leg-charge functional is class-additive, and neutral junk factors contribute zero.

**G3 (which length does D read).** For the undressed (refereed) composite, D is per-constituent-carrier; nothing reads ℓ₁₂. For the dressed product, the honest E1-type rate under γ₁₂-iteration is (Q₂−Q₁)²ℓ₁₂/2π [BOX-5: E1 accumulation argument extended to profiles with ramps off the leg, same shape as E4's refereed split-independence with the referee's O(1) remainder; not itself refereed — but the G2 charge statement above needs no such extension]. Not (Q₁+Q₂)²ℓ₁₂/2π on any reading.

**G4 (covariance; central extension).** Covariance holds for the dressed calculus (λ_hV(Q,γ)λ_h* = V(Q,hγh⁻¹), canonical profiles mapping to canonical profiles) ✓. But E6's refereed Box-2 structure — D₁₂ as the central-extension class of the two implementer lines — is destroyed by dressing: the commutator of dressed lines has group part γ₁γ₂′γ₁⁻¹γ₂′⁻¹ ≠ e (Λ generically free), hence is not scalar, not even central. The refereed internal recovery of D₁₂ lives on the undressed fiber lines only.

## §(c) The no-go horn

**Theorem (fusion is totals-only in M_Λ).** Assume: (A1) the fused object of two charged defects is the composition of their refereed defect automorphisms (equivalently, Ad of the product of their implementers — the identical operation E4 refereed on the same axis; order-independent since Ad-phases cancel, per refereed E2); (A2) carrier data of the composite are read from M_Λ-internal implementers. Then the composite is Ad W(η₁+η₂) — cross-axis profile addition — and **every** implementer of it in M_Λ lies in fiber·Z(M_Λ), with trivial crossed-product group support. *Proof.* Weyl relation gives the composition; if Ad V = Ad W(η₁+η₂) with V ∈ M_Λ then W*V ∈ Z(M_Λ) = L∞(S¹)^Λ⊗1 ⊆ fiber (C-J3). ∎ Carrier data of the pair are preserved (each constituent's profile, hence its carrier's anchors, is recoverable from the summed profile's ramp data); no merge onto any third class occurs or can be read from the algebra. **The blocking axiom is A1 + C-J3 proper outerness.**

**The loophole, and its closure.** The only escape is dropping A1: redefine the defect as the dressed pair (a new postulate — already CONVENTION-at-best under house rules). But §(b) shows the dressed calculus is not merely unforced — it contradicts the refereed record: G1(α) changes E4's refereed number, G1(β) changes E4's refereed composite automorphism, G2 breaks charge additivity by theorem, G4 breaks E6's refereed central-extension recovery. The class selection itself forces the dressing form (crossed-product group parts multiply; obtaining group support in P_g for all pairs forces per-defect dressing by the carrier element, up to inversion — which lands in the inverse class), so no variant survives. Both horns close: **with the record's fusion concept, totals-only is a theorem; without it, no calibrated concatenating rule exists.** This is not UNDERDETERMINED: the kill's own firing condition — "provably totals-only with no carrier merge" — is met.

## §(d) Discrimination (the empirical content, now the epitaph)

Signatures of "pair on c₁⊔c₂" vs "Q₁+Q₂ on P_g" (per crossing coset g):

| Invariant | Pair (totals-only) | Merged (Q₁+Q₂ on P_g) | Verdict |
|---|---|---|---|
| Total charge | Q₁+Q₂ | Q₁+Q₂ | equal |
| Homology | [c₁]+[c₂] | [P_g] = [c₁]+[c₂] (abelianization kills g-conjugation) | equal |
| E7 pairing vs any c₃ | Q₃⟨Q₁[c₁]+Q₂[c₂],[c₃]⟩ | same (homological) | equal |
| E1 | (Q₁²ℓ₁+Q₂²ℓ₂)/2π (no cross-axis cross term in the record) | (Q₁+Q₂)²ℓ₁₂/2π, ℓ₁₂ from the refereed defect formula | **generically differ** |
| E3 spin | two spins ∓Qᵢ²/2; no refereed composite spin | ∓(Q₁+Q₂)²/2 = spin sum ∓Q₁Q₂ (quadratic-refinement shift) | differ by reading |
| E2 unsigned coset counts vs c₃; self-crossings | i(c₁,c₃)+i(c₂,c₃); constituents' own | i(c₁₂,c₃); i(c₁₂,c₁₂) — accessible per C-L4, NOT homological | generically differ [BOX-6: generic, no per-case theorem] |

E1 numbers (defect formula; script run and deleted): (ℓ₁,ℓ₂,θ) = (2,2,π/2): ℓ₁₂ = 3.027; Q = (+1,+1): D_pair = 0.637 < D_merged = 1.927; Q = (+1,−1): 0.637 vs 0 (merged E1-invisible); Q = (+2,−1): 1.592 vs 0.482 (merged smaller — energetically favorable under any monotone reading). Deep crossing (4,4,θ=3.0): ℓ₁₂ = 1.015; even like charges (+1,+1): 1.273 vs 0.646 — merged smaller. Grazing (4,4,θ=0.3), (+1,+1): 1.273 vs 5.065 — merged larger. Difference = [2Q₁Q₂ℓ₁₂ + Q₁²(ℓ₁₂−ℓ₁) + Q₂²(ℓ₁₂−ℓ₂)]/2π: either sign attainable; equality is a codimension-one locus. So the readings were empirically distinct inside the record — the merge was falsifiable, and §(b)–(c) falsified its derivability. One residual observation for the record: the UNDRESSED composite's E1 rate under γ₁-iteration has swept charge Q₁ + îQ₂ (one anchor of the partner always sits in I₁ when crossing), i.e. D_{γ₁}(pair) = (Q₁+îQ₂)²ℓ₁/2π [BOX-5 applies] — a length-carrying cross-axis shift already latent in the refereed calculus; it modifies per-carrier rates, not carriers, so it does not rescue BIND-1, but it is exactly the kind of coupling phase-110's no-go said the refereed record lacked, and deserves its own registered round.

## Gaps-and-boxes ledger

- **[BOX-1]** Weyl/Möbius covariance U(g)W(f)U(g)* = W(f∘g⁻¹): standard quasi-free implementation; implementer phases cancel in all Ad/commutator uses.
- **[BOX-2]** ramp disjointness after γ₁-translation: generic; degenerate coincidences excluded by anchor distinctness, ε-shrinking per C-D5.
- **[BOX-3]** anchor-interlacing lemma: arc-contraction proof sketched; endpoint strictness details unformalized; 2000/2000 numerical confirmation.
- **[BOX-4]** inverse-orientation channel charge |Q₁−Q₂|: computed via mirrored lemma, one configuration numerically checked.
- **[BOX-5]** E1 rate for profiles with off-leg ramps = (∫_{leg}η′)²ℓ/2π: same accumulation argument as refereed E4 split-independence (O(1) remainder); extension not itself refereed. The G2 charge statement is independent of this box.
- **[BOX-6]** genericity of unsigned-count discrimination in (d): no per-case theorem.
- **[others']** all refereed imports as cited: E1/E2/E4 formulas, C-D5, C-J3, C-U1/C-U2, E7 coset bijection, round-1 defect formula, JOIN-4a″.
- No new postulates introduced; the one the loophole needs (dressed defects) is itself gate-broken.

## VERDICT BOX

**NO-GO. K-BIND-1 FIRES; BIND-1 dies as registered.** The arena's fusion operation — composition of refereed defect automorphisms, the identical operation E4 refereed same-axis, extended verbatim to M_Λ — is provably totals-only: the composite is Ad W(η₁+η₂), and by C-J3 proper outerness every M_Λ-internal implementer of it has trivial group support (fatal axiom: fusion-as-composition + implementer rigidity). The unique concatenating alternative, the dressed calculus, is (a) unforced, (b) a different automorphism class (properly-outer deck residue), and (c) provably mis-calibrated: same-axis it doubles the carrier against refereed E4; cross-axis the concatenated carrier's leg carries charge î(Q₂−Q₁) — never Q₁+Q₂ — by the anchor-interlacing lemma (theorem-level, 2000/2000 numerics), inverting the like/unlike-charge pattern; and it destroys E6's refereed central-extension recovery of D₁₂. Load-bearing identification, stated plainly: "fusion" = the record's only refereed composition concept; any future "binding map" that is not fusion would be new structure outside this kill's scope — but under the kill as registered ("provably totals-only with no carrier merge"), the verdict is death. Survives independently of BIND-1 and deserves registration: the round-1 defect formula as pure geometry; the anchor-interlacing lemma; and the latent cross-axis rate shift D_{γ₁}(pair) = (Q₁+îQ₂)²ℓ₁/2π [BOX-5], the record's first candidate length-carrying cross-axis coupling — the K-MASS-2 rescue route, now decoupled from carrier merger.

---

## Amendment section

### A.1 Referee verdict (2026-07-26): KILL CONFIRMED-AS-DERIVABILITY-NO-GO

Kill-claim referee report preserved verbatim in reviews/REPORTS-phase111-KBIND1-referee-2026-07-26.md. The referee's explicit mandate was to RESCUE BIND-1; every rescue attempt failed:

- **Anchor-interlacing lemma: CONFIRMED and UPGRADED to theorem.** The referee supplied its own proof closing BOX-3 (endpoint strictness via one-step dynamics; the argument independently re-proves hyperbolicity of the crossing product). Independent numerics: 3,000/3,000 random configurations (one double-precision artifact resolved pro-lemma at 80 digits) plus 400/400 degenerate stress cases at 80-digit precision. Both channels give leg charge î(Q₂−Q₁); never Q₁+Q₂.
- **Implementer rigidity: CONFIRMED, robust to the weakest reading.** Z(M_Λ) and C-J3 say exactly what the prong needs; even if the defect automorphism is pinned only on the fiber, essential freeness kills all group modes of the relative commutant — trivial group support either way.
- **All four calibration gates: CONFIRMED** (E4 is not carrier-silent — G1α is a genuine contradiction, not an extension; G1β, G2 charge reading, G4 central-extension destruction, and the σ = Q₁Q₂î phase all verified analytically and numerically).
- **Bookkeeping ruling (the decisive part):** the registered kill wording fires in SCOPED form. Proven: no refereed fusion rule derivable from the current record can ever select the concatenated carrier (under A1+A2, totals-only is a theorem; the unique implementer-borne alternative is a different automorphism and provably anti-calibrated). Not proven: that no future non-fusion "binding map" could assign the class P_g — but any such map is a NEW registration, not BIND-1, and is already gate-constrained (it cannot be implementer-borne in M_Λ).

### A.2 Corrections ledger (referee's C-R1..C-R5, recorded as C-V1..C-V5 to avoid collision with the phase-109 C-R labels)

- **C-V1 (verdict scoping).** The consultation's "BIND-1 dies as registered" is replaced by: K-BIND-1 fires as a derivability no-go; **BIND-1 status = DEAD-AS-DERIVABLE**; resurrection requires a new registration, already constrained by this round's gates.
- **C-V2 (BOX-3 closed).** The anchor-interlacing lemma is theorem-grade; numerics corroborative only. No genuine degenerate regime exists (the lemma needs only strict cyclic order).
- **C-V3 (G1α wording).** The same-axis dressed fiber profile is η₁+η₂∘γ₁⁻¹, E2-class-equal to η₁+η₂ (γ₁ fixes its own anchors); the class-level equality is now stated explicitly.
- **C-V4 (BOX-5 caution, important).** The latent cross-axis rate shift's accumulation argument is HARDER than the "same shape as E4 split-independence" analogy: complementary-arc ramps generate O(n) Ḣ^{1/2} cross-pairings under iteration, not O(1); a registration round needs the two-arc line-frame computation, the E1 I-local truncation bookkeeping, and referee numerics of the actual growth rate. The combinatorial core (swept charge Q₁+îQ₂; exactly one partner anchor in the leg) is verified 3,000/3,000 + analytic.
- **C-V5 (discrete formulation).** The registration's third arena ("the discrete formulation") is uninstantiated in the record; the A(I) + M_Λ audit is exhaustive over existing arenas.

### A.3 Post-verdict status

**K-BIND-1: FIRED (scoped). BIND-1: DEAD-AS-DERIVABLE.** Refereed survivors, independent of the kill: (1) the round-1 defect formula (pure hyperbolic geometry, already refereed); (2) the **anchor-interlacing lemma** (theorem-grade, referee-proved); (3) the **latent cross-axis rate shift D_{γ₁}(pair) = (Q₁+îQ₂)²ℓ₁/2π** — combinatorial core refereed, analytic coefficient OPEN per C-V4 — the record's first candidate length-carrying cross-axis coupling, feeding K-MASS-2's future (i) without carrier merger. Candidate next registration; not registered here (operator decision).
