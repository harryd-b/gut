# Phase 119 — Calibration column, steps 2–3 opened: CAL-2 (moduli/extremality) + SU(2)₂ gate registered and dispatched (2026-07-27)

**Status: REGISTERED. Two rounds dispatched to context-free derivation agents on the operator's instruction ("Let's start tackling the calibration column"). Nothing below is derived or refereed. Step 1 of the calibration plan (notes/PLAN-2026-07-26-calibration.md) closed at phase 118: the energy column is refereed as the sector WEIGHT column (data-only; energy semantics blocked pending one dynamical input); mass remains M(D,Q). Step 2 (moduli) is therefore the live front for mass RATIOS; step 3 (dimensionless constants / the charge-lattice unit, α's only candidate home) is gated on the non-abelian program per the plan — its entry computation, SU(2)₂ (phase-113 roadmap item 4), is dispatched in parallel.**

## 1. CAL-2 — the moduli round (calibration step 2, route (a) primary)

**Registered conjecture (route (a), extremality selection).** A record-internal extremality principle selects the moduli point of the hyperbolic surface X = Γ\ℍ, making the primitive closed-geodesic length spectrum — and hence, through MASS-1's registered monotone reading, the STRUCTURE of the mass spectrum — parameter-free at fixed genus. Named candidate: systole-maximality at genus 2 selects the Bolza surface [others']. The genus itself remains a NAMED BLANK: nothing in the record forces g; the round must state this, not fill it.

**What the round computes:** candidate extremality principles ranked by record-internality; the Bolza primitive length spectrum with multiplicities (numerics + literature anchors); the structural confrontation with MASS-1 (generations = distinct carrier geodesics; monotone M ⟹ mass ORDERING = length ordering; spectrum degeneracies ⟹ predicted multiplet structure). Route (b) (environment framing: moduli as 6g−6 measured parameters, every further ratio a parameter-free consistency check) is recorded as the standing fallback, not computed.

**Pre-registered kills.**
- **K-CAL2-1.** If no record-internal extremality principle selects a unique moduli point at the test genus, OR the selected point's length-spectrum structure (ordering + multiplicities) cannot host the refereed generation structure, route (a) is dead as posed and route (b) becomes the registered default.
- **K-CAL2-2 (numerology tripwire, binding on the output).** The no-numerology rule applies with full force: the round must NOT fit the unknown function M, must NOT compare any length or length-ratio numerically against measured particle masses or mass ratios, and must NOT tune the extremality principle to a desired spectrum. Only structural/ordinal claims (orderings, multiplicities, degeneracy patterns, accumulation behaviour) are admissible output. Any fitted comparison auto-rejects that portion of the output as numerology.

## 2. SU(2)₂ — the first genuinely non-abelian computation (gate for calibration step 3)

Registered per phase-113 roadmap item 4: SU(2)₂ (Ising-adjacent; sectors j = 0, ½, 1; first nontrivial channel splitting j=½ ⊗ j=½ = 0 ⊕ 1 with distinct candidate rates) — the first place the channel-dependent binding-spectrum prediction is visible; attackable via the three-Majorana-fermion realization [others']. UNIT-1's refereed re-tag is BINDING on this round: the candidate rate formula is D_λ = 2h_λℓ (invariant form), NOT the superseded 2h_λℓ/2π.

**Pre-registered kills.**
- **K-SU22-1.** The Casimir-transplantation candidate must produce finite, distinct, well-defined channel rates for the 0 ⊕ 1 splitting under the re-tagged unit rules. If the channel rates are forced equal, ill-defined, or require dynamics (channel selection beyond a data label), the channel-spectrum candidate dies (GAP-N5 adjudicated against it).
- **K-SU22-2 (the genuinely-non-abelian gate).** If the SU(2)₂ candidate entries secretly reduce to an abelian embedding (all sector weights of the form q²/2 on a rank-1 lattice, as happened benignly at SU(2)₁), the "first genuinely non-abelian computation" claim dies and the round is re-scoped as another abelian consistency check. The round must check h_{1/2} = 3/16 against lattice realizability explicitly.
- **Step-3 mandate (data gathering, not resolution):** the round must record what SU(2)₂ demands of the compactification radius / charge-lattice unit (GAP-N7's fork in R-coordinates, per the refereed UNIT-1 table) — WITHOUT resolving the fork, which remains its own registration.

## 3. House conditions (both rounds)

Context-free agents; word caps 2500 each; mandatory [BOX]/[GAP-n]/[others'] flagging; numerics where possible; outputs preserved verbatim in phases/ with editorial headers and reserved Amendment sections; adversarial referees before any verdict enters a registration; kills honored as pre-registered; standing AI-referee caveat. The M-FRAME2(b) referee (phase 118, in flight) is independent of both rounds.
