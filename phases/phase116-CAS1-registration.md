# Phase 116 — CAS-1 registered: the absolute energy column; COLLIDE-1: the mass-candidate collision test (2026-07-26)

**Status: REGISTERED. CAS-1 is a candidate dictionary column with pre-registered kills; COLLIDE-1 is its confrontation with MASS-1 and E1. Nothing here is derived or refereed. Registered on the operator's instruction as calibration step 1 (plan: notes/PLAN-2026-07-26-calibration.md).**

## 1. CAS-1 — statement (incorporating the phase-115 referee's C-Y6 corrections)

The **absolute column** A is the registered expectation of the Sugawara stress tensor T = ½:j²: in the record's states, evaluated in a PINNED frame (the standard circle frame of the E3 finite-part computations — the frame must be named because ⟨T⟩ is Schwarzian-covariant and the −c/24 is frame-dependent):

- **A(ω) = −c/24 = −1/24** (vacuum; c = 1 for the U(1) current) [others': standard CFT — Blöte–Cardy–Nightingale PRL 56 (1986) 742; Affleck PRL 56 (1986) 746; both web-verified in the phase-115 referee pass].
- **A(charged sector Q) = Q²/2 − 1/24** in the Sugawara normalization, where the conversion to the record's refereed data is the charge-lattice normalization Q = q√(2π) plus the modular-time 2π factor AND the chirality-branch convention — NOT phase-104's κ, which acts on the norm-based D, not the scheme-fixed D_spin (C-Y6, binding).

Why this is a candidate and not numerology: the record's E3 already contains the sector shape (D_spin's Q²-part = the U(1) conformal weight up to the stated conversion); the −1/24 completes L₀ − c/24. The column pairs with the spin/weight data, NOT with ℓ (ℓ-linear c-terms are scheme artifacts killed by fusion naturality — the refereed F = λQ²ℓ episode).

What CAS-1 buys if it survives: (i) the first ABSOLUTE (non-relative) entry — the record's answer to SPACE-1's Q1 stops being "exactly zero by construction"; (ii) content for the radiation/neutral sector (a photon-analogue with an energy datum — currently indistinguishable from nothing, refereed); (iii) the anchor for the calibration column.

## 2. CAS-1 — pre-registered kills

- **K-CAS-1 (the computation).** Construct ⟨T⟩ in the record's Weyl calculus and evaluate: if the vacuum constant is not −1/24 in the pinned frame, OR the charged-sector Q²-part fails to reproduce refereed D_spin's Q²-part under ONE global constant plus the branch convention, CAS-1 is dead.
- **K-CAS-2 (well-definedness — GAP-b1 promoted to a kill).** If an absolute ⟨T⟩ cannot be well-defined on the record's local algebras without importing structure beyond the global vacuum vector — in particular if it smuggles in dynamics-adjacent structure the successor thesis excludes — CAS-1 is unregistrable as a dictionary column and dies as posed. An honest no-go here is a full result.

## 3. COLLIDE-1 — the mass-candidate collision (registered test)

The record holds two energy-adjacent quantities with OPPOSITE length-dependence, and one registered mass conjecture:

| Candidate | Form | ℓ-behaviour |
|---|---|---|
| E1 rate | D = Q²ℓ/2π | increasing |
| CAS-1 sector energy (candidate) | E ~ (2π/ℓ_frame)(Q²/2 − 1/24)-type | decreasing in the frame size |
| MASS-1 | M(ℓ) monotone (registered increasing reading) | increasing |

**COLLIDE-1:** once CAS-1's computation exists, confront the three. The first task is the frame question: WHAT plays the role of the circle size in the sector-energy formula for a defect on a geodesic of length ℓ — is it ℓ itself (then E ∝ (Q²/2 − 1/24)/ℓ and mass would DECREASE with carrier length), the modular-time 2π (then E carries no ℓ at all and mass ≠ E trivially), or something else the computation forces? Pre-registered outcomes, all acceptable:
- **(O1)** CAS-1 forces a functional form for M(ℓ) — calibration's biggest blank filled by derivation; MASS-1's registration is amended to the derived form.
- **(O2)** The derived form contradicts MASS-1's monotone-increasing registration — **K-MASS-3 fires**; MASS-1 dies or is re-registered with the derived monotonicity, per its own kill terms.
- **(O3)** The identification mass = CAS-1 energy fails structurally (e.g. the frame answer is 2π, no ℓ-dependence) — recorded; mass remains a function of (D, Q) as refereed in phase 110, and calibration must find M elsewhere.

No outcome is fitted; whichever the computation forces is entered.

## 4. Relation to the calibration plan

CAS-1 + COLLIDE-1 are step 1 of the four-step calibration plan (notes/PLAN-2026-07-26-calibration.md): (0) the calibration-free predictions ledger (dispatched in parallel); (1) this; (2) moduli — extremality-selection conjecture vs environment framing; (3) the dimensionless constants via the charge-lattice unit (GAP-N7's fork); (4) the single unit anchor, last and definitional.
