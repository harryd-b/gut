# Phase 121 — DYN-1 registered: the minimal dynamical input (2026-07-27)

**Status: REGISTERED CONJECTURE with pre-registered kills. Nothing here is derived or refereed. Registered on the operator's instruction ("proceed") as the program's highest-impact open registration — the single bottleneck behind the mass formula, the energy of space (SPACE-1 Q1), the binding energy of BIND-2(c), and photon frequency. EXPLICITLY A TEST of the successor thesis: "geometry is data, not dynamics" has been protected through phases 104–120; DYN-1 stresses it. If DYN-1 survives, the thesis is amended to "data + one declared dynamical input." If K-DYN-3 fires, the thesis survives STRENGTHENED by an honest no-go. Either outcome is a full result.**

## 1. Statement

**DYN-1.** The record admits exactly ONE canonical dynamical input — a physical time/Hamiltonian and its frame — selected by record-internal structure, with no further arbitrary choice. The refereed diagnosis this builds on (SEC-1 referee, C-SEC-R6): clocks are already free in the record (interval modular flows ARE Möbius flows — zero new input); what is missing is a canonical frame/subalgebra choice PLUS a scale. Candidate routes the execution round must evaluate for canonicality:

- **(a) Carrier-frame route:** a defect's physical frame is its own carrier geodesic — the cylinder of circumference ℓ. Record-internal candidate-canonical (the carrier is the defect's intrinsic object; no external choice). Consequence: E = (2π/ℓ)(h + C_frame) per refereed Schwarzian covariance.
- **(b) Modular route:** the modular Hamiltonian of a canonically-selected local algebra. Clocks free; the selection of the interval/subalgebra is the open canonicality question; modular time dimensionless (the scale question persists).
- **(c) Global rotation route:** the record's own circle rotation generator L₀ declared as the Hamiltonian. Canonical as an operator; its frame is the modular circle, not the carrier — the ℓ-dependence question must be confronted, not assumed.

## 2. Pre-registered kills

- **K-DYN-1 (keystone/conservativity).** The dynamical input must change NO refereed number and must not break JOIN-4a″ (charged states not in the exact local unitary orbit of the vacuum). Any violation kills DYN-1 outright.
- **K-DYN-2 (the mass confrontation — COLLIDE-3, outcomes pre-registered).** The selected dynamics yields an energy formula; confront it with MASS-1 (mass = M(ℓ), registered monotone-increasing) and E1 (D = 2hℓ). Known hazard to face honestly (refereed C-SEC-R4): the carrier-frame energy is DECREASING in ℓ iff h + C_frame > 0 (with the standard −1/24: iff Q² > π/6), increasing below. Outcomes: (O1) a derived M(ℓ) form — calibration's biggest blank filled; (O2) contradiction with MASS-1's registration — K-MASS-3 fires, MASS-1 dies or re-registers per its own terms; (O3) mass ≠ energy structurally — recorded. No outcome fitted.
- **K-DYN-3 (canonicality — the thesis test).** If NO route is canonical — every candidate imports an arbitrary choice with no record-internal selection principle — DYN-1 fails as posed; the data-only thesis survives strengthened. An honest no-go is a full result.
- **Scope guards:** ONE dynamical input maximum; DYN-1 may fix the FRAME (relative, in ℓ) but NOT the absolute unit (calibration step 4 stays separate and last); no numerology; C_frame remains declared-not-computed unless the dynamics derives it.

## 3. House conditions

Context-free derivation agent (word cap 2500; mandatory [BOX]/[GAP-n]/[others'] flagging); output preserved verbatim with editorial header and reserved Amendment section; adversarial referee (with RESCUE mandate on any no-go) BEFORE any verdict enters this registration — **the referee pass may be deferred for budget; nothing becomes refereed until it runs.** Kills honored as pre-registered. Standing AI-referee caveat.
