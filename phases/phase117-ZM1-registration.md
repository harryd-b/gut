# Phase 117 — ZM-1 registered: the compactified zero-mode representation (2026-07-26)

**Status: REGISTERED CONSTRUCTION with pre-registered kills. Nothing here is built, derived, or refereed. Registered on the operator's instruction ("do it") as the program's single highest-leverage open computation — the named gate for three registered questions.**

## 1. Statement

**ZM-1.** Extend the record's arena by the compactified zero mode: the lattice / vertex-operator extension of the U(1) current net [others': Frenkel–Kac; standard CFT — the record already touched it at the SU(2)₁ check, phase 113], in which:

- the boson's constant mode φ₀ lives on a circle of radius R (compactification), making its conjugate momentum j₀ (charge) a genuine discrete quantum number and the wrap count (winding) a genuine integer-valued operator;
- the extended Hilbert space is the direct sum over charge/winding sectors of oscillator Fock spaces tensored with the zero-mode lattice states;
- charged sectors become honest disjoint representations with lowest L₀-weight q²/2 (in the appropriate normalization) — the sector energy the CAS-1 kill proved is "one representation away";
- the deck/branch-section winding of the M-FRAME material becomes an honest family of unitaries instead of (per the refereed M-FRAME verdict) "a metaphor corresponding to no family of unitaries."

**The R-disclosure (binding).** The radius/lattice unit R is a genuinely new choice, and it is loaded: it is the same charge-lattice-unit fork exposed as GAP-N7 (the record's integer lattice vs the self-dual point 1/√2 vs the √3-lattice of the u(3) decomposition), and it is where any future fine-structure-constant question lives (calibration plan step 3). Round 1 must NOT silently choose R: it must carry R as an explicit parameter, enumerate the distinguished values, and record what each refereed structure demands of it. Any eventual pinning of R is a separate registration.

## 2. What ZM-1 is for — the three prizes (registered as its round-2+ mandates)

1. **M-FRAME2 route (a):** with the zero mode dynamical, the framing phase Φ_zm is computable — the D-MS vs framing-deleted binary becomes a calculation. (Also decides BIND-2's K-BIND2-3.)
2. **BIND-2(c):** the winding pair-energy term (the operator's twist count) becomes a first-class quantum number with a computable pair term.
3. **The absolute energy column (CAS successor):** ⟨T⟩ on sector states = q²/2 (+ the disclosed frame constant) as an actual expectation — reopening, on registered inventory, what K-CAS-1/K-CAS-2's pincer closed.

## 3. Pre-registered kills

- **K-ZM-1 (conservativity — the embedding kill).** The ENTIRE existing refereed dictionary must embed unchanged in the oscillator/vacuum sector of the extension: every refereed number (E1's D, E2's phases, E3's finite parts, the BIND-2 rate, the pair-creation license) must be reproduced exactly. If compactification changes any refereed number, ZM-1 is dead as an extension of THIS record (whatever its independent mathematical merits).
- **K-ZM-2 (keystone compatibility).** JOIN-4a″ must survive in the extended arena in an appropriate (stated) form — plausibly strengthened (charged sectors disjoint ⟹ local unreachability cleaner), but this must be shown. If the extension makes net-charged states locally reachable from the vacuum, ZM-1 contradicts the refereed keystone and dies.
- **K-ZM-3 (the point of it).** If, once built, the construction still cannot decide the M-FRAME2 binary or render the sector energy computable — i.e. if the three prizes remain out of reach INSIDE the extension — ZM-1 fails its purpose and is entered as a null result (not a contradiction, but a registered failure of the gate hypothesis).
- **Scope note (not a kill):** ZM-1 is new registered structure, permitted by the successor thesis only as DATA (representations, sectors, quantum numbers — no dynamics). Any step that requires the zero mode to EVOLVE (rather than label) is out of scope and must be flagged, not taken.

## 4. Relation to the standing program

ZM-1 is the confluence of: the M-FRAME referee's C-S2 (the mechanism lives in "the compactified/dynamical zero mode... flagged, not analyzed"); the CAS-1 kill's pincer (sector energy zero-mode-carried, annihilated in the vacuum representation) and the C-AA2 finding (the winding-compensated profile's remainder = minus the zero-mode energy — the energy is exactly one representation away); GAP-N7 (the lattice-unit fork); and the operator's stiffness/twist intuition (BIND-2(c)-(d)), whose twist count becomes an honest operator here. Phase-113's SU(2)₁ material is the construction's nearest existing calibration point (the self-dual radius, kink ≡ plateau).
