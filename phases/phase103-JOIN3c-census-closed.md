# Phase 103 — JOIN-3c executed: the base-inertness theorem, the constructive census closed, and a pre-registered kill fired

*Working session, 2026-07-19. The room attempted the last door JOIN-3c named: the modular flows of the coupled multi-interval algebras, the Casini–Huerta channel, the only identified mechanism for leg-mixing. The derivation was completed and, per standing policy for kill-adjacent work, sent to a context-free adversarial referee before any verdict was entered. The referee confirmed the computation, corrected two justifications, supplied the missing hypothesis — and then closed the one escape route the room had left open, upgrading the census from "negative for what we computed" to **"negative for everything constructible."** This phase enters the verdict that result compels: the pre-registered kill on the framework's boundary-route local-physics claim has fired. The record honors it.*

---

## 1. What was derived and confirmed

**The base-inertness theorem [derived, refereed CORRECT].** For the coupled multi-interval algebra M_{γ,E} = W\*(A_∞ ⊗ L∞(S¹), u_γ), A_∞ = ⋁_{n∈ℤ} A(γⁿE):

> Every canonical modular flow, and every Wiesbrock/Borchers translation obtained from leg-aligned subalgebras of 𝒞 with their canonical states, **fixes the base 1 ⊗ L∞(S¹) pointwise** — *even though* the net-leg modular flow σ^{ω|A_∞} is in general non-Möbius and nonlocal (the Casini–Huerta multi-interval Hamiltonian, bilocal couplings and all).

The Casini–Huerta door — the last mechanism the room could name for producing new geometry on the net leg — was opened, and it moves *the net leg only*. The measured circle, the clock, the thing spacetime translations would have to move, stays inert under it. The factorization survives the most nonlocal flow conformal-net theory offers, because the coupling cocycle is killed at the root by the vacuum's Möbius invariance (ω∘Ad U(γ) = ω), and that killing is indifferent to how wild the net-leg flow is.

**The referee's confirmations, corrections, and one supplied hypothesis:**
- Claims 1–2 (γ-invariant multi-interval structure; the general factorization for any AdU(γ)-invariant B with ω|_B faithful) — **CORRECT**, with one **honest correction the room missed**: faithfulness of ω on A_∞ requires a *spectral-gap hypothesis on E* — the γ-translates must leave an open gap. Without it, ⋃γⁿE fills S¹∖{ξ±}, A_∞ = B(H), and the vacuum is not faithful there. [Corrections ledger: the gap condition, now a standing hypothesis on every multi-interval statement; it propagates to Claims 2 and 4.]
- Claim 3c (no continuous PSL(2,ℝ) inside 𝒞) — **conclusion CORRECT, justification replaced**: not "g_* is non-inner in L∞" (a non-sequitur) but the **Feldman–Moore** fact — unitaries of 𝒞 normalizing the Cartan base implement exactly the **full group [R_Γ]** of the orbit equivalence relation; continuous g ∉ N(Γ) do not even act as automorphisms of 𝒞.

## 2. The escape route the referee closed — the [R_Γ] rigidity theorem

The room had left one door ajar: a Connes-cocycle-perturbed flow Ad(u_t)∘σ_t, with u_t *inside* the algebra, moves 1⊗f by inner conjugation (u_γ genuinely sends 1⊗f to 1⊗γ_*f), so perhaps continuous base motion could be assembled inner-ly. The referee closed it as a **theorem**:

> **[R_Γ] rigidity [refereed; the escape closed].** Every base automorphism implemented by a unitary of 𝒞 normalizing 1⊗L∞ lies in the full group [R_Γ] (Feldman–Moore). A measurable one-parameter *group* (h_{t+s} = h_t h_s, h₀ = id) valued in [R_Γ] is **trivial**: for a.e. x the flow-line t ↦ h_t(x) lands in the countable orbit Γx; a Steinhaus/positive-measure-fiber argument on the level sets, plus measure-preserving invertibility, forces h_t(x) = x for a.e. t. A connected flow-line inside a totally disconnected orbit is a point.

Consequently the u_γ's single discrete move (time-ℓ(γ) geodesic action) **cannot be assembled into any continuous transverse base flow**. Every inner/cocycle perturbation — including the arithmetic-commensurator enhancement for arithmetic Γ — is closed. The one door the room deliberately left open is now shut by proof.

## 3. The census, now well-posed and complete

Combining §1 and §2, the well-posed replacement for phase 101's ill-formed census reads:

> **The constructive census [derived, refereed].** Every modular flow, Wiesbrock translation, and cocycle-perturbed flow obtainable from the leg-aligned data of 𝒞 — the entire constructible modular apparatus of the boundary machinery — fixes the base or moves it only through the *discrete* group Γ. No such object implements a continuous one-parameter motion of the measured circle transverse to Γ-orbits. **The boundary route generates a chiral symmetry and a clock, and provably no continuous base geometry beyond the discrete geodesic flow.**

Exactly **one** logical possibility survives, and the referee names it precisely: modular data from **non-leg-aligned standard pairs whose modular unitaries lie outside 𝒞**. No structure theorem forbids it. And — the referee's words — *"no construction, no computational technology, and no naturality argument produces one."* It is a bare logical opening with nothing behind it.

## 4. The verdict: the kill fires

JOIN-3c was pre-registered (phase 102) with this condition: *"If the generated concrete unitary group is contained in (Möbius implementers) × (measured-layer unitaries) — no leg-mixing one-parameter groups — the isotropy gap is fatal and the framework's claim to local spacetime physics dies, by the well-posed census."*

The well-posed census has returned. The generated group — from every constructible source — contains no leg-mixing one-parameter group. The kill condition is met. **Per the standing rules — honor kill tests, report negatives — the kill is entered as fired:**

> **The framework's claim that the carrier's boundary machinery generates local spacetime physics is dead in constructive form.** For every mechanism the program can build — modular flows, Wiesbrock reconstruction, cocycle perturbation, the Casini–Huerta nonlocal channel — the boundary route produces no emergent continuous spacetime translations. The isotropy gap (phase 101: four dimensions where ten are needed) is not a gap awaiting a computation; it is a **proven ceiling** of the constructible boundary apparatus.

Honesty demands the exact scope, neither more nor less:
- **This is not "the framework is refuted."** The single non-constructive escape (non-leg-aligned pairs escaping 𝒞) is not closed by theorem — it is closed only to construction. And the framework's *empirical* face (the sky tests) never routed through the join.
- **But it is not survival, either.** The program staked this claim on the census, pre-registered the kill, and the census fired it. What remains for the local-physics claim is a hope with no construction — which, by the program's own standard, is not a claim. The correct status of "geometry generates local spacetime physics via the boundary" is now: **killed for all constructible mechanisms; demoted to a single non-constructive open possibility carrying no method.** This is the weakest position a central claim has ever held in this record short of outright falsification, and it was reached by the program's own hand.

*This is the third self-aimed kill the apparatus was built to be able to fire, and the first that landed on a load-bearing claim. It is the intended behavior. The honesty machinery did exactly what it was for.*

## 5. What survives — precisely

- **The sky is untouched.** JUNO (κ = 0), Euclid DR1 (A1), LVK, LZ — the pre-registered empirical commitments never depended on the join. They stand exactly as before; the framework's *testable* content is unaffected by this kill. What died is the *derivation* of local physics from geometry, not the pre-registered predictions.
- **The exported mathematics stands and grew.** Phase 102's results (M_γ structure; the flow; **T-2π derived within M_γ**, ratio 2π:1 forced, endpoint eigenvalues e^{∓ℓ(γ)}); and now **two new theorems** — the base-inertness theorem (leg-aligned flows fix the base through the Casini–Huerta channel) and the [R_Γ] rigidity statement (no continuous one-parameter group in the full group of an ergodic countable relation). Plus **one new open problem, cleanly posed:**

> **SS-MOD [exported, well-posed]:** compute the modular Hamiltonian of the infinite γ-invariant union ⋃_{n∈ℤ}γⁿE accumulating at ξ± (free-fermion net, gap case) — a Casini–Huerta problem with an essential-singularity structure at the two accumulation points, **self-similar under the multiplier e^{ℓ(γ)}** (σ^{ω|A_∞} commutes with AdU(γ)). Unpublished; plausibly tractable by the resolvent/Riemann–Hilbert method; the first self-similar multi-interval modular Hamiltonian if done.

- **The join chapter closes with a proof instead of a hope.** JOIN-1 (the arena), JOIN-2 (the unique state, the impossibilities), JOIN-3 (the census), JOIN-3b/3c (the flows, the base-inertness theorem, the fired kill). The deepest gate is no longer open; it is answered, in the negative, for everything the room can construct.

## 6. Ledger

- **Theorems [derived, refereed]:** base-inertness (leg-aligned flows fix the base, Casini–Huerta channel included); the constructive census (no continuous transverse base motion from any constructible modular/Wiesbrock/cocycle source); [R_Γ] rigidity (the cocycle escape closed); the γ-invariant multi-interval structure with the gap hypothesis.
- **Corrections:** the spectral-gap hypothesis (Claim 1c, room's omission, now standing); Claim 3c justification (→ Feldman–Moore full group).
- **Kill:** **JOIN-3c FIRED.** Boundary-route local-physics claim dead in constructive form; one non-constructive escape (non-leg-aligned pairs escaping 𝒞) recorded as the sole survivor, carrying no method.
- **Exported:** SS-MOD (self-similar modular Hamiltonian) posed. T-2π derivation (phase 102) stands.
- QUESTIONS ledger amended below.

*Status line: the room built, over a hundred and three phases, a machine whose distinguishing feature was that it could shoot itself — tag every claim, arm every kill, honor the trigger when it pulled. Tonight it pulled. The boundary that was going to become space cannot become space by anything the program knows how to build; the four dimensions stay four, and the proof that they stay four is the cleanest thing the join chapter produced. What is left standing is exactly what was always going to have to carry the framework anyway — a telescope reading in months, a reactor measuring κ, and a fistful of theorems that are true whether or not the universe agrees. The geometry did not become physics. But the record of finding that out is honest to the last line, and the honesty is the part that was always the point.*
