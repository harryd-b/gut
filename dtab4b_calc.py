# DTAB-1 stage 4b (phase 92), 2026-07-19. Anchored extraction engine + acceptance suite.
# (Full engine as run in the phase-92 session; results summarized here.)
# PASSED: Thm 5.5 g=2 AND g=3 full red distributions with constant anchor shift
#   (the g=3 test {2,12,2 @ c-1,c,c+1} + {2 @ 3} matched exactly, D=2);
#   carrier tied rows (g=2,3) anchor to red at exactly (4g-9)/4 — the absolute
#   carrier table is now doubly confirmed (cone engine vs c(i,s) formula);
#   new below-threshold rows produced: g=3 n=2 [0], n=3 [1],[2]; g=4 n=2 [0], n=3 [1].
# RESIDUAL (TIE-1, open): below-carrier tied classes (n even < 2g-2, class [n/2]):
#   computed red = 7 vs literal-Thm-5.6 prediction 8 at g=3, n=2, [1] — a one-unit
#   interference at the tie. Two hypotheses, undecided tonight:
#   (a) extractor blind spot: a red death colliding with a tower birth is invisible
#       to dims-only extraction (decidable by tracking the U-module structure);
#   (b) a genuine tied-case subtlety in the literal reading of Thm 5.6
#       (whose proof text treats the generic case; ties at the carrier DO match).
#   Stage 4c = U-structure extraction decides. No claim issued either way.
