# REPORT — phase 105 Dconj (conjugation/antimatter) referee pass (2026-07-26, verbatim)

*Context-free adversarial referee on the conjugation-entry derivation (phases/phase105-Dconj-antimatter.md), run per standing pre-verdict policy. Note on provenance: a first referee run was dispatched 2026-07-24 and stopped mid-flight (session compaction); this is the re-dispatched run, same brief, completed 2026-07-26. The referee ran numerics (2^18-point grid, Gaussian-smoothed ramps, exact reflection by index map; the anti-symplecticity identity σ(η₁∘r, η₂∘r) = −σ(η₁,η₂) confirmed to machine precision in every configuration as a code check).*

*Verdict summary: core of the entry CONFIRMED. GAP-1 (the sign of f^J) CLOSED — f^J = +f∘r via Guido–Longo sector conjugation + BMT sector composition, so the modular mirror flips charge and the "antimatter = charge flip" reading is earned, convention-free. Box 4b (full conjugation flips D₁₂) is an exact theorem, numerically confirmed. Box 4a (ONE-SIDED mirroring flips D₁₂) is UNPROVEN — the pre-flagged double-flip risk materializes: in explicit branch conventions the raw commutator phase under mirror-one is unchanged (0 → 0), or worse, not even ±σ (−1/4 → +3/4 in generic position); the law stands or falls with an unproven extension of E2's axes-and-charges-only invariance to reflected profiles (GAP-2 is a real gap, exported as an open lemma to the entry-6 arena build). Box 5 CORRECT WITH REPAIR — for Q ≠ 0 the defect automorphism is OUTER on A(I) (divergent H^{1/2} norm of the winding step), so W(η)* ∉ A(I) and the explicit A·JAJ·Ω formula is literally valid only at Q = 0; the repair (ξ_η := the unique natural-cone representative of ω∘α_η|_{A(I)}) preserves every claimed property, verified by the referee. §2(b) spin/statistics consistency check passes exactly. Verdicts entered in the derivation document's amendment. Report verbatim below; standing AI-referee caveat applies.*

---

The numerics are converged (clean rationals) and the built-in sanity checks pass: the both-mirrored anti-symplecticity identity σ(η₁∘r, η₂∘r) = −σ(η₁,η₂) holds to machine precision in every row, validating the code. I have what I need for the report.

---

**Summary verdict.** The modular-theoretic core of this entry is sound: the algebra of §1 is correct step by step, Box 2 follows, Box 3 and §2(b) are internally consistent, and the standard-form purification of §3 is correct in content though its central formula is literally valid only for Q = 0 (for Q ≠ 0 the defect automorphism is *outer* on A(I), so W(η)* ∉ A(I); the repair via the unique positive-cone representative preserves every claimed property, which I verified). GAP-1 (the sign of f^J) can be closed by the sector-conjugation argument: I adjudicate f^J = +f∘r, so "antimatter = charge flip" stands. The weak point is Box 4a: my explicit recomputation of σ(η₁∘r₁, η₂) with smooth ramp profiles shows the raw commutator phase does **not** robustly flip sign under mirroring one defect — the double-flip cancellation the mandate warned about materializes in explicit branch conventions, and in generic crossing configurations the mirrored phase is not even ±σ. Box 4a therefore rests entirely on the unrecomputed extension of E2's axes-and-charges-only law to reflected profiles (exactly GAP-2), which I could not verify from the data in this brief: UNPROVEN.

**1. The action of J — Boxes 1, 2; GAP-1 (mandate A).** Verdict: **CORRECT** (Box 1 and Box 2), with GAP-1 **CLOSED** conditional on two standard literature theorems.

I checked every algebraic step of §1: J e^{iφ(f)}J = e^{−iJφ(f)J} (antilinearity); conjugation of the Weyl relation forcing σ(f^J,g^J) = −σ(f,g); the substitution computation σ(f∘r, g∘r) = −σ(f,g); ‖f∘r‖ = ‖f‖ in the homogeneous H^{1/2} norm (|f̂_k| ↔ |f̂_{−k}|); and the two-minus-sign cancellation Jφ(f)J = φ(−f∘r) ⟹ JW(f)J = W(f∘r). All correct.

Sign adjudication (mandate A). The proposed sector chain is valid: (a) Guido–Longo (from modular covariance, which is given here via Bisognano–Wichmann) establishes that the conjugate sector of a DHR sector ρ localized in I is the J-reflected sector j∘ρ∘j, j = Ad J. (b) For the U(1) current algebra the BMT sectors ρ_q compose as ρ_q∘ρ_{q'} ≃ ρ_{q+q'} and are pairwise inequivalent, so the conjugate (inverse) of charge q is charge −q, with ρ_q ≄ ρ_{−q} for q ≠ 0. (c) If f^J = −f∘r, then η^J = −η∘r has density (−η∘r)′(x) = +η′(−x), of charge +Q, so j∘ρ_Q∘j ≃ ρ_{+Q} — contradicting (a)+(b) for Q ≠ 0. Hence f^J = +f∘r. This is a genuine, convention-free closure: the only imports (GL, BMT) are established theorems of the same standing as the BW input the whole entry assumes. One residual caveat: §1 tacitly assumes f^J ∈ {±f∘r} rather than deriving that these are the only anti-symplectic isometric candidates; but since the sector argument fixes the action on charge classes and the modular J is unique, this does not affect the conclusion. The weight-tracking heuristic is internally consistent as stated (continuing the dilation e^{−2πt}x to t = −i/2 gives e^{iπ}x = −x; the weight-1 density picks up the Jacobian factor e^{iπ} = −1; combined with antilinearity the signs cancel) — but it is correctly labeled a heuristic; the sector argument is what closes the gap.

Box 2's charge computation Q[η^J] = η(0) − η(∞) = −Q is correct, as is the automorphism identity (J·AdW(η)·J) = AdW(η∘r) (I checked it on Weyl generators using anti-symplecticity and antilinearity of scalar conjugation). The trichotomy adjudication (admissible-but-invisible) applies the given locality theorem correctly.

**2. Transformation laws — Boxes 3, 4a, 4b; §2(b) (mandates B, D).**

(a) Box 3: **CORRECT** given E1. D depends only on Q² and ℓ; the antiunitary J carries the modular data of (A(I),Ω) to (A(I′),Ω), preserves cocycle-difference norms, and r maps the axis to itself with the same translation length. Nothing to fault.

(b) §2(b), mandate D: **CORRECT.** From D_spin = ∓Q²/2 + nQ²/2, applying n → −n and ∓ → ± gives ±Q²/2 − nQ²/2 = −(∓Q²/2 + nQ²/2) = −D_spin, which is exactly what scalar conjugation e^{iD_spin} → e^{−iD_spin} requires. The consistency check is real. Minor quibble: "(−1)^{−q} = (−1)^q" is literally true only for integer q; for anyonic q the correct statement is that the statistics phase e^{iπq} is conjugated to e^{−iπq}, which *is* the phase of charge −q — so the conclusion survives, but the equation as written is loose.

(c) Box 4b: **CORRECT.** σ(η₁∘r, η₂∘r) = −σ(η₁,η₂) is a theorem (anti-symplecticity, independent of the GAP-1 sign), and my numerics confirm it to machine precision in every configuration and convention tested.

(d) Box 4a, mandate B: **UNPROVEN — the double-flip risk is real.** I computed σ(η₁, η₂) = ∫η₁ η₂′ dθ and σ(η₁∘r₁, η₂) numerically (N = 2^18 grid, Gaussian-smoothed ramps of width 0.02, exact reflection by index map so the mirrored primitive is η₁∘r₁ by construction), with I₁ = (0,π), r₁(θ) = −θ, ramp charges Q₁ = Q₂ = 1. Because η₁ winds for Q₁ ≠ 0, a single-valued primitive requires placing the compensating (branch) charge; I tested the three natural conventions: (i) full −Q₁ at anchor ξ₋; (ii) full −Q₁ at anchor ξ₊; (iii) symmetric −Q₁/2 at each anchor — the reading most consistent with C-D5's "symmetric/midpoint regularization." Configurations: two crossing interleavings with axis 2 = (π/2, 3π/2) (r₁-symmetric), a generic crossing axis (3π/4, 7π/4), the opposite-orientation arc, nested axes, and disjoint axes. Results (σ₁₂ → σ(mirror-1, 2)):

- Crossing, r₁-symmetric axis 2, conv. (iii): +1/4 → −1/4 and −1/4 → +1/4 — clean flip, supporting 4a.
- Same configuration, conv. (i): 0 → 0; conv. (ii): +1 → −1. Convention-dependent.
- **Generic crossing axis, conv. (iii): −1/4 → +3/4.** Not −σ, not +σ. Conv. (ii): 0 → +1.
- Opposite-orientation crossing, conv. (iii): −3/4 → −1/4; conv. (i): −1 → 0.
- Nested (î = 0): conv. (iii) gives σ₁₂ = ±1/2 ≠ 0 (sign set by ramp order), mirror → 0.
- Disjoint (î = 0): conv. (iii): 0 → +1/2.

Two conclusions. First, the raw commutator phase of concrete regularized profiles is **not** a function of axes and charges alone under any tested convention — it depends on the ramp positions c₁, c₂ and on branch placement, and it does not reproduce E2's claimed values (nested axes give nonzero σ; magnitudes are O(Q₁Q₂), not 2πQ₁Q₂ under the stated normalization Q = ∫η′). E2 is declared refereed, so it must contain canonicalization/bookkeeping conventions not transmitted in this brief; I cannot re-derive its law from the data given. Second, precisely because of this, Box 4a's claim cannot be verified: mirroring defect 1 changes both its charge sign *and* its plateau geometry, and the exact identity my numerics confirm is σ(η₁∘r₁, η₂) = −σ(η₁, η₂∘r₁), which reduces to −σ(η₁,η₂) only when r₁ carries the (axis-2, ramp-2) data to an equivalent configuration (as in my symmetric test case). In generic position the mirrored phase differs from −σ₁₂ by ramp-position-dependent terms of size Q₁Q₂/2. So GAP-2 is not a formality: the "axes-and-charges-only dependence extended to I₁′-supported profiles" is exactly what fails at the level of raw σ. If E2's invariant D₁₂ is well-defined by some canonicalization, 4a is *plausible* (and is consistent with applying the E2 law formally: (−Q₁)Q₂î; also consistent with 4b applied twice), but it is not proven here, and in explicit conventions the double-flip cancellation (e.g. conv. (i): σ unchanged) or worse (−1/4 → +3/4) occurs. Verdict: UNPROVEN; the derivation's own flag understates the problem. The repaired statement I can certify: mirroring *both* defects flips D₁₂ (Box 4b, theorem); mirroring one defect flips D₁₂ **iff** the E2 invariant extends to reflected profiles with unchanged î — an open lemma that needs E2's exact C-D5 bookkeeping to settle.

**3. The mirror pair — Box 5 (mandate C).** Verdict: **CORRECT WITH REPAIR.**

The standard-form facts invoked are right (Haagerup): P^♮ = closure{A JAJ Ω : A ∈ A(I)} is the natural cone at Ω; every normal state of A(I) has a unique vector representative in P^♮; the map is a homeomorphism onto the normal state space. The ordering check requested: with W := W(η), ξ = W*JW*JΩ, and A ∈ A(I): using [JW*J, A] = [JW*J, W*] = 0 and (JW*J)*(JW*J) = 1, ⟨ξ, Aξ⟩ = ⟨Ω, JWJ·WAW*·JW*JΩ⟩ = ⟨Ω, WAW*Ω⟩ = (ω∘AdW(η))(A). The claimed ordering (AdW(η), not AdW(η)*) is correct. J-invariance: Jξ = JW*J·W*·JΩ = ξ, correct. The A(I′) restriction: for J-invariant ξ, ⟨ξ, JAJξ⟩ = φ_ξ(A*) with φ_ξ = ⟨ξ,·ξ⟩|_{A(I)}; on Weyl generators B = W(g), g in I′, this equals e^{−iσ(η∘r,g)}ω(W(g)) = (ω∘AdW(η^J))(B), using σ(η∘r,g) = −σ(η,g∘r). So the mirror-state claim (c) is correct given Box 1.

The genuine defect: for Q ≠ 0, **W(η)* is not in A(I)** — indeed no implementer of α_η exists in A(I): the winding step-profile has divergent homogeneous H^{1/2} norm (the classic log divergence), so α_η|_{A(I)} is an *outer* automorphism of the type III₁ factor — which is precisely why E1's erasure rate D > 0 is nontrivial. Hence ξ_η = "A JAJ Ω with A = W(η)* ∈ A(I)" is literally false for Q ≠ 0, and GAP-3's "neutral-pair implementer" gloss does not rescue clause (e) as written (a neutral-pair implementer exists and is unique up to phase in the irreducible vacuum representation, but it does not factor through A(I)). Repair: define ξ_η as the unique P^♮-representative of the normal state φ = ω∘α_η|_{A(I)} (equivalently U_α*Ω with U_α the standard implementation of the automorphism). Then: existence and uniqueness are automatic; cone vectors are J-fixed, giving (b); the restriction identities (c) hold by the computation above, which uses only the automorphism (i.e., η′ on I) and Box 1; and (d)'s global-neutrality/local-non-erasability discussion is correct and consistent with E4. For Q = 0 (single-valued η with compactly supported primitive in I), the explicit formula is exactly right and ξ_η ∈ {AJAJΩ} without closure. So every physical claim of Box 5 survives; the *formula* is Q = 0 only, and the entry's uniqueness language is then correct. The "thermofield-double" reading is fair.

**4. CPT reading.** **CORRECT** as tagged: attribution to BW/Borchers/Guido–Longo is appropriate, nothing is over-claimed, and with GAP-1 closed (§1 above) the "J = reflection × charge conjugation × antilinearity" summary is accurate.

**5. Honest limits.** **CORRECT WITH REPAIR.** The convention-dependence discussion of f^J is honest (and now resolvable per §1). But §5 misclassifies Box 4a as a "theorem in the toy modulo the flags": my computations show it is not derivable from the brief's data even modulo the GAP-1 sign — it needs the unproven E2-extension lemma. And GAP-3's framing understates that the Q ≠ 0 outerness breaks clause (e)'s identification, not merely the "formal circle-Weyl manipulation."

**Summary**

1. Box 1: CORRECT; GAP-1 CLOSED via Guido–Longo + BMT sector conjugation: f^J = +f∘r, J flips charge. Weight-tracking heuristic internally consistent but only heuristic.
2. Box 2 and trichotomy adjudication: CORRECT.
3. Box 3: CORRECT (given E1).
4. §2(b) spin/statistics: CORRECT; mandate D check passes (n→−n, ∓→± gives exactly −D_spin, matching antiunitary conjugation); "(−1)^{−q} = (−1)^q" literal only for integer q.
5. Box 4b: CORRECT — exact theorem, numerically confirmed to machine precision in all configurations.
6. Box 4a: UNPROVEN — direct numerics (smooth ramps, three branch conventions, four crossing + nested + disjoint configurations) show σ(η₁∘r₁, η₂) = −σ(η₁,η₂) only in reflection-symmetric configurations; generically the double-flip cancellation or worse occurs (e.g. 0 → 0; −1/4 → +3/4). The claim stands or falls with an unproven extension of E2's axes-and-charges-only law to I′-supported profiles (GAP-2 is a real gap), and the raw σ of concrete profiles does not reproduce E2's law under any convention reconstructible from this brief.
7. Box 5: CORRECT WITH REPAIR — for Q ≠ 0 the automorphism is outer on A(I) (divergent H^{1/2} norm of the winding step), so W(η)* ∉ A(I) and the explicit A JAJ Ω formula is Q = 0 only; defining ξ_η as the unique natural-cone representative of ω∘α_η|_{A(I)} preserves all claims. Ordering (AdW(η)), J-invariance, cone uniqueness, and the mirror-state restriction all verified.
8. §4, §5: CORRECT as tagged, except §5 understates the status of Box 4a and of GAP-3.
