# Referee report — ZM-1 round 1 (phase117-ZM1-round1-construction.md)

**Date:** 2026-07-27
**Referee:** context-free adversarial agent (all mode-algebra, BCH, and ordering computations re-derived by hand; all eight reading-set documents audited; the units derivation, the ½, the common-n globality step, both kill verdicts, and the R-table each attacked).
**Editorial note (operator):** Preserved VERBATIM below. Verdicts: **K-ZM-1 does not fire (CONFIRMED, E2 lemma-conditional per C-AB2); K-ZM-2 does not fire (CONFIRMED, GAP-5 upgraded to a binding condition per C-AB3); κ = ½ refereed as a THEOREM within zero-mode framing (the previously gate-calibrated normalization now derived); the D-MS statement ruled at exactly the right strength — M-FRAME2(b) remains the decider between framing definitions.** Corrections C-AB1..C-AB6 entered in the phase file's Amendment.

---

## REPORT (VERBATIM)

# ADVERSARIAL REFEREE REPORT — Phase 117 ZM-1 round 1 (context-free pass, 2026-07-27)

Independent derivations performed for mandates (A) and (B) (mode algebra, two-point function, BCH/displacement phases, pair orderings — all by hand from the record's stated conventions; no numerics needed, all quantities exact). All eight reading-set documents audited. I attempted to break the units derivation, the ½, the common-n globality step, both kill verdicts, and the R-table. Findings below.

## (A) BOX-1 units derivation — CONFIRMED, one sign correction (C-AB1)

**Bracket.** With f̂_k = (1/2π)∫fe^{−ikθ}dθ: σ(f,g) = ∫fg′ = 2πiΣ_k k f̂_{−k}ĝ_k. With j(θ) = (1/2π)Σ_k j_k e^{ikθ}, j̃(f) = Σ_k f̂_{−k}j_k (checked). Imposing [j̃(f),j̃(g)] = ∓iσ(f,g) with [j_m,j_n] = Cmδ_{m+n} forces C = ±2π. **The magnitude |C| = 2π is forced; the submission's stated sign is wrong against the record.** The record's own conventions (phase 116 referee §A: Ad W(η)(W(f)) = e^{−iσ(η,f)}W(f); phase 116 body: Ad W(η)(j(f)) = j(f) − σ(η,f)) with W(f) = e^{ij̃(f)} force [j̃(f),j̃(g)] = **+iσ(f,g)**, hence [j_m,j_n] = −2πmδ_{m+n}; the submission's "[j̃(f), j̃(g)] = −iσ(f,g) forces [j_m,j_n] = 2πmδ" is the opposite chirality labeling (j_k ↔ j_{−k}). This is harmless to every downstream number: only |C| enters units, positivity fixes which modes annihilate, and the vacuum functional is sign-insensitive — I verified ⟨j̃(f)²⟩_Ω = 2πΣ_{k≥1}k|f̂_k|² = ‖f‖², reproducing ω(W(f)) = e^{−‖f‖²/2} **exactly**, either labeling. Entered as C-AB1 (correction of record, not substance; cognate to refereed C-S4).

**Conversion.** a_m := j_m/√(2π) gives [a_m,a_n] = mδ; the current shift j → j + η′ shifts j₀ = ∫j dθ by Q = ∫η′, so a₀-eigenvalue q = Q/√(2π) — **forced**. L₀ = ½a₀² + Σa_{−k}a_k gives zero-mode energy q²/2 = Q²/4π. The anchor is genuine: the phase-116 referee's C-AA2 states verbatim "½∫(η′)² − ½Q²c_ρ/ε = −Q²/4π … Under Q = q√(2π): −q²/2" — the same conversion, refereed, from the rescue side. D = Q²ℓ/2π = (2πq²)ℓ/2π = q²ℓ = 2h_qℓ — exact.

**Phase-113 covariance.** Sharpened, and confirmed. h_λ is dimensionless (¼ is ¼ in any units), so what the conversion moves is the *identification*: phase 113's "abelian shadow of j=½ is a plateau defect of charge Q = ±1/√2" reads the sector charge as record charge (q ≡ Q, the R = 1/ν-fork row); under BOX-1 the honest record charge is Q = √π and both sides of the K-NONAB-1 comparison become ℓ/2 (= 2h_λℓ = q²ℓ) instead of ℓ/4π. Since K-NONAB-1 is an *agreement* check and both sides carry the same charge reading, **the verdict is stable under the conversion** — but the relabeling of which record defect realizes the sector is substantive, so the owed reconciliation round is real work, not cosmetics (C-AB6). The √2 row ("weights (1/√2)ℤ, roots √2ℤ") matches phase 113 §1(a) verbatim; the √3 row matches §2 exactly ((1/√3)ℤ/√3ℤ ≅ ℤ₃); index [Λ_R : Rℤ] = R² checked; Bose-locality iff R² even is correctly left as [others']/GAP-1.

## (B) BCH/κ = ½ — CONFIRMED as scoped; the sharp question answered

**Single defect.** [x₀,j̃₀] = i, A = iaj̃₀, B = −iQx₀, [A,B] = −iaQ: e^{A+B} = e^{B}e^{A}e^{−iaQ/2}, so on the charge-0 vacuum e^{i(aj̃₀−Qx₀)}|0⟩ = e^{−iaQ/2}·(charge shift)|0⟩ — the submission's identity, re-derived exactly. The pairwise overlap ⟨ψ(a₁),ψ(a₂)⟩ of two such dressings carries phase e^{−iQ(a₂−a₁)/2}; telescoping over a deck loop Δa = nQ gives |Φ_zm| = ½ΔaQ = nQ²/2. **R appears nowhere** — R gates only which Q lie on the lattice. The ½ is the BCH central half. Confirmed.

**What "derived" means (C-AB5).** The ½ follows from exactly two inputs: (i) the record's Weyl map W(f) = e^{ij̃(f)} — the single (symmetric) exponential, so W(η + a·1) is the Weyl-ordered displacement — and (ii) [x₀,j̃₀] = i with the −Qx₀ coefficient, which I verified is jointly equivalent to the C-S1-repaired pairing: Ad e^{iaj̃₀}(W_ext(η)) = e^{−iσ_ext(a,η)}W_ext(η) with σ_ext(a·1,η) = +aQ (BCH check performed; matches C-S1's corrected sign; the shift Ad(zero-mode factor)(j̃₀) = j̃₀ + Q checks). Both inputs are record conventions, not new calibrations — so "κ = ½ derived, not calibrated" is fair, with the surviving conventions exactly as flagged: the ± sign (orientation/chirality, per C-AB1/C-S4) and the identification of E3's crossing-n with deck count (that identification IS the zero-mode-framing definition; the C-S3 rival defines n otherwise).

**The pair at common n — the mandated sharp question.** Does "one profile, one section, Δa = n(Q₁+Q₂)" derive a single branch section or choose it, re-importing M-FRAME's presupposed step 1? Ruling: **the single-section framing is a choice — but at common n the value is choice-free within zero-mode framing**, and the submission's own per-particle computation proves it: separate sections a₁(t), a₂(t) with Δa_i = n_iQ_i, Weyl-ordered, give ½(n₁Q₁+n₂Q₂)(Q₁+Q₂) (re-derived: overlap phase ½Δ(a₁+a₂)Q_tot), which at n₁ = n₂ = n equals n(Q₁+Q₂)²/2 — identical to single-section. The extension explicitly does NOT force one section (it realizes per-particle sections), so the correct derivation of the common-n value cites this coincidence, not the single-profile framing (C-AB4). With that route the value stands **unconditionally within zero-mode framing** — which is precisely the refereed A.2 conditional theorem, now with derived normalization. "D-MS forced within zero-mode framing" is therefore the **right strength**: not weaker (no residual choice at common n inside the framing), not stronger (C-S3's crossing framing survives untouched; M-FRAME2(b) remains the decider). No overclaim found — the submission's own residue paragraph states exactly this.

**Per-particle orderings.** [G₁,G₂] = i(a₁Q₂ − a₂Q₁); e^{iG₁}e^{iG₂} vs e^{iG₂}e^{iG₁} vs Weyl-ordered give mutual terms n₂Q₁Q₂, n₁Q₁Q₂, ½(n₁+n₂)Q₁Q₂ respectively (re-derived): ordering span exactly (n₁−n₂)Q₁Q₂, vanishing at common n. GAP-6's "∝(n₁−n₂)Q₁Q₂" is exact, and the Weyl-ordered value is the mean of the two orderings — C-S5's ½(n₁+n₂)Q₁Q₂ confirmed as quoted from phase 109 A.3.

## (C) K-ZM-1 — CONFIRMED, verdict conditional as flagged (C-AB2)

Embedding lemma sound: F₀ carries the same Weyl relations (bracket check above), same ω (two-point reproduction verified), Möbius generators' zero-mode terms (a₀a_{±1} etc.) annihilate F₀ so the restriction is the record's oscillator implementation, and modular data are intrinsic to (A(I), ω). Quotient and geometric embeddings coincide on F₀ (j̃₀ = 0) — checked. E1 spot-check against phase 104: ζ^{(n)}_in built from η with ∫_{S¹}η′ = 0 (single-valued, phase 104 §1(a)), vanishing at ξ± with kinks only, ‖·‖² = 2πΣ_{k≥1}k|·|² — strictly k≥1; χ_Ī and δ_t preserve single-valuedness; the zero mode cannot enter. Correct. BIND-2 spot-check against phase 112 §(a)–(b): the pair profile is globally compensated (single-valued), the functional is the χ_Ī₁-truncated Ḣ^{1/2} growth — no zero-mode ingredient; correct. E3: Φ_osc is k≥1-only; dipoles are F₀ states; and the zero-mode realization of the n-term reproduces nQ²/2 (my (B) derivation) — consistency, not change; correct. E4: the ⟸ direction is F₀; the ⟹ direction quantifies over A(I), to which the extension adds nothing.

The one soft spot the submission itself flags: **K-ZM-1's registered wording lists "E2's phases" explicitly**, and E2 is covered only by the blanket lemma (GAP-3). Since every record state is vacuum-folium and E2's phases are functionals of (Weyl calculus over H(I), ω, Möbius flow), the lemma genuinely covers them — but the verdict should be entered as *lemma-conditional for E2* (C-AB2), not as an unqualified per-item reproduction. GAP-5 honestly scoped. **K-ZM-1 does not fire.**

## (D) K-ZM-2 — CONFIRMED, with the trigger clause adjudicated (C-AB3)

JOIN-4a″'s refereed statement (phase 108 A.1, quoted): "Let A = A(I)… Then ω∘α is not in the exact unitary orbit of ω — indeed no isometry w ∈ A gives ω(w·w*) = ω∘α." It quantifies over A(I) only; the extension enlarges A_ext(I) ⊃ A(I) but changes neither A(I), nor ω on it, nor the cocycle (intrinsic to (A(I), ω, α); F₀-computed). **Survives verbatim.** Disjointness scoping correct: mutually disjoint reps of the quasilocal algebra ⟹ folia at norm distance 2 (standard) — a strictly global strengthening; on a single type-III₁ factor A(I), sector restrictions are unitarily equivalent (transportability — correct as claimed) and Connes–Størmer approximate transitivity persists — GAP-4 stated exactly right, blocking future overreading.

The dangerous clause is the kill's own trigger: "**If the extension makes net-charged states locally reachable from the vacuum, ZM-1 contradicts the refereed keystone and dies.**" The extension DOES make winding charges q ∈ Rℤ reachable by A_ext(I) operators — by construction. Ruling: the trigger must be read against the theorem it protects; "locally reachable" in the registration's sense is A(I)-reachable (the registration's own gloss — "charged sectors disjoint ⟹ local unreachability cleaner" — concerns the extended net's *superselected* classes ℤ_{R²}, which are correctly unreachable), and no refereed statement quantifies over A_ext(I). The winding-sublattice creatability is the standard lattice-extension superselection redraw, contradicting nothing as scoped. **K-ZM-2 does not fire** — but GAP-5 must be entered as a **binding condition**, not an informational flag: any future declaration of A_ext as the physical/observable net voids this ruling's premise and mandates a fresh keystone round before any erasability claim is reused (C-AB3). The submission says nearly this ("the exact locus where a future re-declaration would matter"); the registration should carry it with teeth.

## (E) Scope and registration — CONFIRMED with notes

Data-not-dynamics: respected. Transport phases are Pancharatnam data of parametrized families (no generator, no evolution), and the registration itself lists Φ_zm as prize 1; the single dynamics-shaped step (phase→energy in (iii)) is flagged and not taken. ⟨T⟩ = q²/2 is an eigenvalue; the successor energy column is correctly NOT entered (K-CAS-2's registered wording honored). R-disclosure: nothing pinned; R free throughout; the four rows verified above (√(2π): record Q-lattice ℤ under the forced conversion ✓; 1: the ν-fork, correctly labeled ✓; √2, √3: match phase 113 ✓ — noting phase 113's lattices are written in the q≡Q reading, which is exactly the fork the table's first two rows expose). Verdict wording matches the registered kills, with C-AB2's qualifier for E2. The headline "(i) κ = ½ FORCED… gate value derived, not calibrated" is acceptable only with its two in-text qualifiers (within zero-mode framing; sign conventional) — both present in §(d)(i); the D-MS claim is at the correct strength relative to C-S3 (see (B)).

---

## VERDICT BOX

**K-ZM-1: does NOT fire — CONFIRMED**, conditional as flagged: E1, E3, E4, BIND-2 independently verified as k≥1/modular functionals reproduced exactly; E2 covered by the embedding lemma only (GAP-3; C-AB2 qualifier mandatory in the registration entry).

**K-ZM-2: does NOT fire — CONFIRMED.** JOIN-4a″ survives verbatim (quantifies over A(I); nothing added there); global disjointness is a genuine strengthening; GAP-4 (single-algebra Connes–Størmer) correctly persists; the winding-creatability trigger adjudicated in the submission's favor with GAP-5 entered as binding (C-AB3).

**May enter the ZM-1 registration as refereed:** (1) BOX-1 — [j_m,j_n] = ±2πmδ_{m+n} (magnitude forced; sign per C-AB1), q = Q/√(2π), zero-mode energy Q²/4π = q²/2 anchored to refereed C-AA2, D = q²ℓ = 2h_qℓ; phase-113's K-NONAB-1 verdict stable under conversion (agreement check; both sides move together), reconciliation round owed and substantive (C-AB6). (2) The §(a) zero-mode construction as checked: [x₀,j̃₀] = i ⟺ σ_ext(a·1,η) = +aQ_η (C-S1 repair verified by BCH), Z(a) nontrivial and trivial on F₀ (C-S2 repair), Λ_w = Rℤ, sector group ℤ_{R²} — vertex-operator locality remains [others']/GAP-1, unrefereed. (3) The embedding lemma and both kill survivals as above.

**Refereed status of κ = ½:** *Theorem within zero-mode framing* — Φ_zm = ½Δa·Q_tot follows from the record's Weyl map (symmetric exponential) plus the repaired pairing, R-independent, sign conventional (C-AB5). This upgrades phase 109 A.2's refereed conditional theorem by deriving the previously gate-calibrated normalization. It does **not** close the D-MS/framing-deleted binary.

**Refereed status of D-MS:** "Forced within zero-mode framing" is exactly right — at common n the value n(Q₁+Q₂)²/2 is choice-free inside the framing (per-particle Weyl-ordered coincidence, C-AB4: cite that route, not the single-section framing), the per-particle mutual term ½(n₁+n₂)Q₁Q₂ and ordering span (n₁−n₂)Q₁Q₂ are exact (GAP-6 confirmed); C-S3's crossing framing survives; **M-FRAME2(b) remains the decider between definitions.**

**Corrections:**
- **C-AB1 (sign of record).** The record's Ad convention with W(f) = e^{ij̃(f)} forces [j̃(f),j̃(g)] = +iσ(f,g), hence [j_m,j_n] = −2πmδ; the submission's stated sign is the opposite chirality labeling. |2π| — the only units-load-bearing content — unaffected; two-point reproduction holds either way.
- **C-AB2.** K-ZM-1's entry must read "does not fire; E2 lemma-conditional (GAP-3)" — the registered kill names E2's phases explicitly.
- **C-AB3.** GAP-5 upgraded to binding condition: re-declaring A_ext as the physical net voids the K-ZM-2 ruling's premise and requires a fresh keystone round.
- **C-AB4.** Common-n derivation route: replace "one profile, one section" (a choice echoing M-FRAME's refuted step 1) by the per-particle-coincidence argument the submission itself contains; claim strength unchanged.
- **C-AB5.** "κ = ½ derived" scoped: the ½ is derived; the ± sign and the deck-count identification of n remain conventions (as flagged).
- **C-AB6.** Phase-113 stability sharpened: h_λ is dimensionless; under BOX-1 the identified record charge of the j = ½ defect relabels 1/√2 → √π and both comparison values become ℓ/2; verdict stable, reconciliation round substantive.

*(≈2450 words.)*
