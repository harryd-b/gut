# Referee report — K-BIND-1 kill claim (phase111-BIND1-round2-KBIND1.md)

**Date:** 2026-07-26
**Referee:** context-free adversarial agent with explicit RESCUE mandates (independent proof of the anchor-interlacing lemma; independent numerics on 3,400 configurations including 80-digit degenerate stress; independent audit of the rigidity theorem including the weakest fiber-only reading; hyper-literal ruling on the registered kill wording).
**Editorial note (operator):** Preserved VERBATIM below. Verdict: **KILL CONFIRMED-AS-DERIVABILITY-NO-GO** — the mathematics survives all rescue attempts; the registered kill fires in scoped form; BIND-1 status = DEAD-AS-DERIVABLE. The referee's corrections are self-labeled C-R1..C-R5, which COLLIDES with the phase-109 C-D5 ledger's C-R labels; in the amendment and registration they are recorded as **C-V1..C-V5** (content unchanged). The referee closed the round's BOX-3 (the lemma's endpoint strictness) with its own proof — the anchor-interlacing lemma is now theorem-grade.

---

## REPORT (VERBATIM)

# REFEREE REPORT — Phase 111 round 2 (K-BIND-1 kill claim)

**VERDICT: KILL CONFIRMED-AS-DERIVABILITY-NO-GO.** The mathematics of both prongs survives every rescue attempt I mounted, including independent numerics on 3,400 configurations and an independent proof of the anchor-interlacing lemma that closes BOX-3. But the registered kill wording, read against the record's own prior usage, supports only the scoped verdict: **no refereed fusion rule derivable from the current record can ever select the concatenated carrier — BIND-1 is dead as a derivable mechanism.** Exact status prescribed in §4 below.

---

## 1. Mandate (A): the anchor-interlacing lemma — CONFIRMED; BOX-3 CLOSED

I re-derived the lemma independently. For î = +1 (ccw order a₁,a₂,b₁,b₂), let K = [b₁,b₂], K′ = [a₁,a₂] (disjoint closed arcs, containing no repelling / no attracting anchors respectively). Then: γ₂ fixes b₂ and moves b₁ (which lies in the arc (a₂,b₂)) ccw toward b₂, so γ₂K = [γ₂b₁, b₂] ⊆ K; γ₁ fixes b₁ and moves both endpoints strictly into (b₁,b₂), so γ₁₂K ⊂ int K. Dually γ₁₂⁻¹ = γ₂⁻¹γ₁⁻¹ maps K′ strictly into int K′. Each arc therefore contains a fixed point; since a Möbius map preserving a nondegenerate proper closed arc setwise fixes its endpoints, the nested intersection ∩ₙγ₁₂ⁿK is a single point (else both fixed points would lie in K, contradicting the fixed point in the disjoint K′), which is the attracting point b₁₂; the point in K′ is repelling. **Endpoint strictness (the BOX-3 concession)** follows from one-step checks: γ₁₂(a₂) = γ₁(a₂) ∈ (a₂,b₁) ≠ a₂; γ₁₂(b₂) = γ₁(b₂) ∈ (b₁,b₂) ≠ b₂; γ₁₂(a₁), γ₁₂(b₁) similarly move. So a₁₂ ∈ open (a₁,a₂), b₁₂ ∈ open (b₁,b₂) — and the argument re-proves hyperbolicity of the crossing product independently of C-U1 (two disjoint strictly invariant arcs).

**Ramp membership, verified:** γ₁(a₂) ∈ (a₂,b₁) is the one-step check above (γ₂ fixes a₂; γ₁ moves a₂ ccw toward b₁ within P₁). γ₁(b₂) = γ₁₂(b₂) is exact (γ₂ fixes b₂ — the submission's identity confirmed); b₂ lies in the complement arc (b₁₂,a₁₂), where γ₁₂-dynamics moves points monotonically toward b₁₂, so γ₁(b₂) ∈ (b₁₂,b₂) — **outside** the leg. Hence the leg (a₁₂,b₁₂), running ccw through a₂ and b₁, contains exactly the ramps {−Q₁@b₁, +Q₂@γ₁a₂}: leg charge Q₂−Q₁. I separately derived the î = −1 channel (order a₁,b₂,b₁,a₂): a₁₂ ∈ (a₂,a₁), b₁₂ ∈ (b₂,b₁), in-leg ramps {+Q₁@a₁, −Q₂@γ₁b₂}, leg charge Q₁−Q₂. **î(Q₂−Q₁) confirmed in both channels; never Q₁+Q₂.**

**Independent numerics** (own script, own matrix construction — conjugation of diag(e^{ℓ/2},e^{−ℓ/2}) via C = [[q,p],[1,1]]; own sampling; scripts deleted): 1,500 random î=+1 and 1,500 î=−1 configurations, lengths log-uniform in [0.05, 20], random rotations, mixed-sign non-integer charges. Checks per configuration: hyperbolicity, a₁₂/b₁₂ arc membership, γ₁a₂ ∈ (a₂,b₁), γ₁b₂ ∈ (b₁₂,b₂), exact in-leg ramp set, leg charge = î(Q₂−Q₁), σ-membership (γ₁a₂′ ∈ P₁, γ₁b₂′ ∉ P₁ resp. mirrored), and the BOX-5 combinatorics. Result: **2,999/3,000 with the single failure (ℓ₁≈ℓ₂≈19.8) re-run at 80-digit precision and resolving PRO-lemma** — a double-precision artifact (b₁₂ and γ₁b₂ separated below float resolution). **Deliberate degenerate stress:** 400 configurations at 80-digit precision with anchor gaps down to 10⁻¹⁰ and lengths 10⁻³–30 (grazing and deep crossings): **400/400, zero non-hyperbolic products.** (Double-precision stress at gaps 10⁻⁸ produces failures that are all precision artifacts — the fixed-point quadratic loses all significant digits; a future registration should note the lemma needs no quantitative input, only strict cyclic order, so no genuine degenerate regime exists.) **Inverse-orientation channel:** 500/500 give leg charge Q₁−Q₂ for V(Q₁,γ₁)V(Q₂,γ₂⁻¹), confirming BOX-4 and the charge-conjugation consistency (the anti-defect (−Q₂,γ₂⁻¹) yields Q₁+Q₂ only on the different class S⁻). **G2 stands as a theorem.**

## 2. Mandate (B)(i)–(ii): implementer rigidity — CONFIRMED, and robust to the weakest reading

(i) Entry 6, Amendment A.4 (C-J3): Z(M_Λ) = L∞(S¹)^Λ⊗1 is the refereed statement, proved exactly via proper outerness of **every** g ≠ e on the fiber — precisely what the submission needs, including for γ₁² (torsion-free). The center is contained in the fiber even in the Schottky case where it is nontrivial; "trivial group support" survives. (ii) The inference is correct: V, W ∈ M_Λ with Ad V = Ad W on M_Λ gives Ad(W*V) = id, so W*V ∈ M_Λ′∩M_Λ = Z(M_Λ). I attacked the potential weakening — what if the refereed defect automorphism is only pinned on the fiber, leaving the M_Λ-extension a choice? It changes nothing: if Ad V agrees with Ad W merely on the fiber, W*V lies in the relative commutant of the fiber, and the group modes of that relative commutant die by essential freeness (x_g·(h∘g⁻¹) = h·x_g for all h ∈ L∞(S¹) forces x_g = 0 for g ≠ e, the fixed set being null). So even the fiber-only reading yields trivial group support. Note also the dressed V restricts on the fiber to Ad W∘α_γ|_fiber ≠ the defect automorphism, so no extension-ambiguity rescue exists. **P1 is a theorem given A1 + A2.** The order-independence claim (Ad-phases cancel) is refereed E2.

## 3. Mandate (C): gate audit — ALL FOUR GATES CONFIRMED

**G1α.** E4 is not carrier-silent: it is a same-axis entry whose refereed rate (Q₁+Q₂)²ℓ/2π carries the single axis's ℓ, and whose refereed composite is the state/automorphism Ad W(η₁+η₂) — carrier data included. A calculus whose group-part reading assigns γ₁² (length 2ℓ) **contradicts** a refereed number on E4's own domain; it is not an extension. The "primitive-class projection" dismissal is fair (not a homomorphism; concedes group part ≠ carrier). One pedantic correction for the amendment: the dressed same-axis fiber profile is η₁+η₂∘γ₁⁻¹, equal to E4's η₁+η₂ at E2-class level only (γ₁ fixes its own anchors; C-D5 ε-shrinking) — harmless, should be recorded. **G1β.** V₁V₂ = W(η₁)W(η₂∘γ₁⁻¹)λ_{γ₁²}, so Ad(V₁V₂) = Ad W(·)∘α_{γ₁²}; equality with Ad W(·) would make α_{γ₁²} inner, contradicting C-J3. Verified. **(iii)** Q = ∫_I η′ over the carrier's leg is the refereed charge reading (dictionary §1 preamble; E2 plateau class; C-D5 ramps). **G4.** The dressed-line commutator has group part γ₁γ₂′γ₁⁻¹γ₂′⁻¹ ≠ e (non-commuting pair), and λ_g, g ≠ e is non-central by C-J3 — Box 2's scalar-commutator central extension (the refereed internal recovery of D₁₂) is destroyed on dressed lines. Verified against entry 6 §1.5. **(v)** The crossed-product computation is correct under E6's stated conventions (λ_g(x⊗f)λ_g* = U(g)xU(g)*⊗f∘g⁻¹, BOX-1 Weyl covariance): V₁V₂ = e^{−iσ(η₁,η₂′∘γ₁⁻¹)/2}W(η₁+η₂′∘γ₁⁻¹)λ_{γ₁γ₂′}. The σ-phase: by the E2 jump formula, σ(η₁, η₂′∘γ₁⁻¹) = Q₂[η₁(γ₁a₂′)−η₁(γ₁b₂′)]; my lemma work gives γ₁a₂′ ∈ (a₂,b₁) ⊂ P₁ (value Q₁) and γ₁b₂′ ∈ (b₁₂,b₂) ∉ P₁ (value 0), so σ = Q₁Q₂ at î=+1, mirrored −Q₁Q₂ at î=−1: **σ = Q₁Q₂î confirmed analytically and by 3,000/3,000 membership checks.** The six-ramp Δη and the closure-failure claim (iii) follow. The no-re-dressing closure holds: the constituents' fiber parts are pinned to the canonical E2 classes (JOIN-4a″ makes the defect state-level; C-D5 reshaping never moves a ramp across a strict membership boundary), so the product's leg charge is forced.

## 4. Mandate (B)(iii): the bookkeeping ruling — the kill fires SCOPED, not as registered

The registered kill: *"BIND-1 dies if a refereed cross-axis fusion rule fails to select the concatenated class (e.g. provably totals-only with no carrier merge, or selects a different class)."* §3: *"If a cross-axis fusion rule is ever derived in the arena (A(I), the joint arena of E6, or the discrete formulation) and it selects the concatenated class, BIND-1's mechanism is real; if it selects the disjoint union (totals-only, no carrier merge), BIND-1 dies."* §7: *"BIND-1 lives or dies on whether a refereed fusion rule ever selects the concatenated carrier."*

Three findings. **(1)** The protasis requires a refereed cross-axis fusion rule to exist and fail. Round 1's referee already knew E4's composition is additive ("the exact root of the phase-110 no-go") and nevertheless entered "the arena is SILENT; K-BIND-1 remains fully open" — the record's own refereed usage distinguishes "the composition concept is additive" from "a refereed cross-axis fusion rule exists." Round 2's genuine additions are the M_Λ rigidity theorem and the gate-breaking of the unique alternative; these convert silence into "provably totals-only" **only under identification A1** (fusion = the record's composition concept) **and A2** (carrier read from M_Λ-internal implementers). A1 is overwhelmingly natural — it is E4's own refereed operation, extended verbatim — but the submission itself flags it as load-bearing, and a rule violating A1/A2 (a state-level or non-implementer-borne "binding map") is expressly conceded to be outside the kill's scope. **(2)** §7's "ever" quantifies over the open future; no finite derivation closes it absolutely, and demanding that reading would make K-BIND-1 unfireable in principle — contrary to its design (the "provably totals-only" parenthetical). The honest resolution is between the two readings. **(3)** Of §3's three arenas: A(I) cannot jointly host a crossing pair (refereed trichotomy: mutual inadmissibility), M_Λ is audited by this round, and **"the discrete formulation" does not exist in the record** — I verified the phrase occurs only in the registration itself; there is no constructed discrete arena with a composition concept, so the omission is vacuous.

**Ruling:** the kill fires as a **derivability no-go**, not as the unconditional registered sentence. What is proven: (a) under the record's fusion concept (A1+A2), cross-axis fusion is Ad W(η₁+η₂) and totals-only is a theorem with implementer rigidity; (b) the unique implementer-borne concatenating alternative is a different automorphism and provably anti-calibrated (G1, G2, G4). Jointly: **no refereed fusion rule derivable from the current record can ever select the concatenated carrier.** What is not proven: that no future refereed structure of non-fusion type could assign the composite the class P_g. **Status BIND-1 should carry: DEAD-AS-DERIVABLE (K-BIND-1 fired, scoped).** The registration's life-condition (§3/§7) was a fusion rule; fusion is now settled against it, so BIND-1 does not survive as "awaiting structure" in its registered form. Any future concatenating "binding map" is a new registration, not BIND-1 — and it is already constrained by this round's gates: it cannot be implementer-borne in M_Λ.

## 5. Mandate (D): survivors' audit

**(1)** Round-1 defect formula: already refereed, pure trace geometry, BIND-1-independent. Survives. **(2)** Anchor-interlacing lemma: survives and is upgraded — my §1 proof closes BOX-3 (endpoint strictness included); registrable as a theorem with no numerics caveat. **(3)** BOX-5 latent rate shift D_{γ₁}(pair) = (Q₁+îQ₂)²ℓ₁/2π: the combinatorial core is **correct and verified** (3,000/3,000 + analytic): interlacing puts exactly one partner anchor in I₁ — the repelling a₂ for î=+1, the attracting b₂ for î=−1 — and ∫_{I₁}(η₁+η₂)′ = Q₁+îQ₂. But the accumulation-argument extension is the **genuine bottleneck, and it is harder than the submission's "same shape as E4's split-independence" suggests**: the partner's second ramp sits in the complementary arc, and under γ₁-iteration it generates its own near-jump structure at the fixed points; in the line frame, same-scale logarithmic structures on the two sides of the repelling point have Ḣ^{1/2} cross-pairings of order n, **not** O(1), so the (Q₁+îQ₂)² coefficient cannot be obtained by citing E4's refereed split-independence (proved only for densities on one leg). A registration round needs: the two-arc line-frame computation, the precise truncation bookkeeping of E1's I-local cocycle (which must kill the complementary-side structures — the same mechanism that makes refereed E1 equal Q²ℓ/2π rather than 2Q²ℓ/2π), and referee numerics of the actual growth rate. The flag "modifies rates, not carriers — cannot rescue BIND-1 but feeds K-MASS-2's future (i)" is **correctly placed**: a length-carrying cross-axis coupling inside the refereed calculus is exactly what phase-110's no-go certified as missing, and it requires no carrier merger.

## 6. Corrections for the amendment ledger

- **C-R1 (verdict scoping).** The VERDICT BOX's "K-BIND-1 FIRES; BIND-1 dies as registered" is replaced by the scoped form of §4 above: fires as a derivability no-go; BIND-1 DEAD-AS-DERIVABLE; resurrection = new registration, already gate-constrained.
- **C-R2 (BOX-3 closed).** Endpoint strictness proved (this report, §1); the lemma is a theorem, numerics now corroborative only.
- **C-R3 (G1α wording).** Same-axis dressed fiber profile is η₁+η₂∘γ₁⁻¹ (E2-class-equal to η₁+η₂); state the class-level equality explicitly.
- **C-R4 (BOX-5 caution).** The O(1)-remainder analogy to E4 is insufficient as stated: complementary-arc ramps produce O(n) cross-terms absent truncation; the G2 theorem is unaffected (as the submission itself notes).
- **C-R5 (discrete formulation).** §3's third arena is uninstantiated in the record; the kill round's audit of A(I) + M_Λ is exhaustive over existing arenas.

**Summary sentence for the registration:** K-BIND-1 fires in scoped form — within the record's refereed fusion concept, cross-axis fusion is provably totals-only with implementer rigidity (C-J3), and the unique concatenating alternative is a different automorphism that provably anti-calibrates (leg charge î(Q₂−Q₁), doubled same-axis carrier, destroyed central-extension recovery); BIND-1 is dead as a derivable mechanism, and no surviving route to a concatenated carrier exists inside M_Λ's implementer calculus. Standing AI-referee caveat applies.
